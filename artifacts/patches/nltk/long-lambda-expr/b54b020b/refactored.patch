diff --git a/nltk/tgrep.py b/nltk/tgrep.py
index dead3d856..ba526ec49 100644
--- a/nltk/tgrep.py
+++ b/nltk/tgrep.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 #!/usr/bin/env python
 #
 # Natural Language Toolkit: TGrep search
@@ -433,12 +434,13 @@ def _tgrep_relation_action(_s, _l, tokens):
             )
         # A >, B      Synonymous with A >1 B.
         elif operator == ">," or operator == ">1":
-            retval = lambda n, m=None, l=None: (
-                hasattr(n, "parent")
-                and bool(n.parent())
-                and (n is n.parent()[0])
-                and predicate(n.parent(), m, l)
-            )
+            with EmissionsTracker(project_name='nltk-benchmark', experiment_id='nltk_LLE001_b54b020b', output_file='/root/ecooptimizer_benchmark/emissions/nltk/long-lambda-expr/b54b020b_refactored.csv') as tracker:
+                retval = lambda n, m=None, l=None: (
+                    hasattr(n, "parent")
+                    and bool(n.parent())
+                    and (n is n.parent()[0])
+                    and predicate(n.parent(), m, l)
+                )
         # A <N B      B is the Nth child of A (the first child is <1).
         elif operator[0] == "<" and operator[1:].isdigit():
             idx = int(operator[1:])
