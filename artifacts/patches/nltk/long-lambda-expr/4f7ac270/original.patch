diff --git a/nltk/tgrep.py b/nltk/tgrep.py
index dead3d856..45997bcee 100644
--- a/nltk/tgrep.py
+++ b/nltk/tgrep.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 #!/usr/bin/env python
 #
 # Natural Language Toolkit: TGrep search
@@ -467,9 +468,10 @@ def _tgrep_relation_action(_s, _l, tokens):
         # A <' B      B is the last child of A (also synonymous with A <-1 B).
         # A <- B      B is the last child of A (synonymous with A <-1 B).
         elif operator == "<'" or operator == "<-" or operator == "<-1":
-            retval = lambda n, m=None, l=None: (
-                _istree(n) and bool(list(n)) and predicate(n[-1], m, l)
-            )
+            with EmissionsTracker(project_name='nltk-benchmark', experiment_id='nltk_LLE001_4f7ac270', output_file='/root/ecooptimizer_benchmark/emissions/nltk/long-lambda-expr/4f7ac270.csv') as tracker:
+                retval = lambda n, m=None, l=None: (
+                    _istree(n) and bool(list(n)) and predicate(n[-1], m, l)
+                )
         # A >' B      A is the last child of B (also synonymous with A >-1 B).
         # A >- B      A is the last child of B (synonymous with A >-1 B).
         elif operator == ">'" or operator == ">-" or operator == ">-1":
