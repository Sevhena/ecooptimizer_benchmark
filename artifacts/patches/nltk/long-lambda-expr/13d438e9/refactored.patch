diff --git a/nltk/tgrep.py b/nltk/tgrep.py
index dead3d856..d1a49a0de 100644
--- a/nltk/tgrep.py
+++ b/nltk/tgrep.py
@@ -111,6 +111,7 @@ macro definitions to ``m`` and initialises ``l`` to an empty dictionary.
 
 import functools
 import re
+from codecarbon import EmissionsTracker
 
 try:
     import pyparsing
@@ -611,12 +612,13 @@ def _tgrep_relation_action(_s, _l, tokens):
             )
         # A $,, B     A is a sister of and follows B.
         elif operator == "$,," or operator == "%,,":
-            retval = lambda n, m=None, l=None: (
-                hasattr(n, "parent")
-                and hasattr(n, "parent_index")
-                and bool(n.parent())
-                and any(predicate(x, m, l) for x in n.parent()[: n.parent_index()])
-            )
+            with EmissionsTracker(project_name='nltk-benchmark', experiment_id='nltk_LLE001_13d438e9', output_file='/root/ecooptimizer_benchmark/emissions/nltk/long-lambda-expr/13d438e9_refactored.csv') as tracker:
+                retval = lambda n, m=None, l=None: (
+                    hasattr(n, "parent")
+                    and hasattr(n, "parent_index")
+                    and bool(n.parent())
+                    and any(predicate(x, m, l) for x in n.parent()[: n.parent_index()])
+                )
         else:
             raise TgrepException(f'cannot interpret tgrep operator "{operator}"')
     # now return the built function
