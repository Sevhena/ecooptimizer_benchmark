diff --git a/sklearn/model_selection/_validation.py b/sklearn/model_selection/_validation.py
index 8b70bf426..828009f04 100644
--- a/sklearn/model_selection/_validation.py
+++ b/sklearn/model_selection/_validation.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 """
 The :mod:`sklearn.model_selection._validation` module includes classes and
 functions to validate the model.
@@ -1976,23 +1977,24 @@ def learning_curve(
 
     if _routing_enabled():
         router = (
-            MetadataRouter(owner="learning_curve")
-            .add(
-                estimator=estimator,
-                # TODO(SLEP6): also pass metadata to the predict method for
-                # scoring?
-                method_mapping=MethodMapping()
-                .add(caller="fit", callee="fit")
-                .add(caller="fit", callee="partial_fit"),
-            )
-            .add(
-                splitter=cv,
-                method_mapping=MethodMapping().add(caller="fit", callee="split"),
-            )
-            .add(
-                scorer=scorer,
-                method_mapping=MethodMapping().add(caller="fit", callee="score"),
-            )
+            with EmissionsTracker(project_name='scikit-learn-benchmark', experiment_id='scikit-learn_LMC001_4bb1070c', output_file='/root/ecooptimizer_benchmark/emissions/scikit-learn/long-message-chain/4bb1070c.csv') as tracker:
+                MetadataRouter(owner="learning_curve")
+                .add(
+                    estimator=estimator,
+                    # TODO(SLEP6): also pass metadata to the predict method for
+                    # scoring?
+                    method_mapping=MethodMapping()
+                    .add(caller="fit", callee="fit")
+                    .add(caller="fit", callee="partial_fit"),
+                )
+                .add(
+                    splitter=cv,
+                    method_mapping=MethodMapping().add(caller="fit", callee="split"),
+                )
+                .add(
+                    scorer=scorer,
+                    method_mapping=MethodMapping().add(caller="fit", callee="score"),
+                )
         )
 
         try:
