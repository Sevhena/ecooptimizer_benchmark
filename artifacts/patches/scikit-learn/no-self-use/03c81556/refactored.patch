diff --git a/sklearn/feature_extraction/text.py b/sklearn/feature_extraction/text.py
index eb3226b01..15821cad6 100644
--- a/sklearn/feature_extraction/text.py
+++ b/sklearn/feature_extraction/text.py
@@ -26,6 +26,7 @@ from ..utils.fixes import _IS_32BIT
 from ..utils.validation import FLOAT_DTYPES, check_array, check_is_fitted, validate_data
 from ._hash import FeatureHasher
 from ._stop_words import ENGLISH_STOP_WORDS
+from codecarbon import track_emissions
 
 __all__ = [
     "ENGLISH_STOP_WORDS",
@@ -1190,7 +1191,9 @@ class CountVectorizer(_VectorizerMixin, BaseEstimator):
         self.binary = binary
         self.dtype = dtype
 
-    def _sort_features(self, X, vocabulary):
+    @track_emissions(project_name='scikit-learn-benchmark', experiment_id='scikit-learn_R6301_03c81556', output_file='/root/ecooptimizer_benchmark/emissions/scikit-learn/no-self-use/03c81556_refactored.csv')
+    @staticmethod
+    def _sort_features(X, vocabulary):
         """Sort features by name
 
         Returns a reordered matrix and modifies the vocabulary in place
@@ -1385,12 +1388,12 @@ class CountVectorizer(_VectorizerMixin, BaseEstimator):
             if max_doc_count < min_doc_count:
                 raise ValueError("max_df corresponds to < documents than min_df")
             if max_features is not None:
-                X = self._sort_features(X, vocabulary)
+                X = CountVectorizer._sort_features(X, vocabulary)
             X = self._limit_features(
                 X, vocabulary, max_doc_count, min_doc_count, max_features
             )
             if max_features is None:
-                X = self._sort_features(X, vocabulary)
+                X = CountVectorizer._sort_features(X, vocabulary)
             self.vocabulary_ = vocabulary
 
         return X
