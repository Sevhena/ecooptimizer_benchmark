diff --git a/numpy/_core/einsumfunc.py b/numpy/_core/einsumfunc.py
index 8e71e6d4b1..e46cd7da62 100644
--- a/numpy/_core/einsumfunc.py
+++ b/numpy/_core/einsumfunc.py
@@ -2,6 +2,7 @@
 Implementation of optimized einsum.
 
 """
+from codecarbon import EmissionsTracker
 import itertools
 import operator
 
@@ -1036,11 +1037,12 @@ def einsum_path(*operands, optimize='greedy', einsum_call=False):
     path_print += "%6s %24s %40s\n" % header
     path_print += "-" * 74
 
-    for n, contraction in enumerate(contraction_list):
-        inds, idx_rm, einsum_str, remaining, blas = contraction
-        remaining_str = ",".join(remaining) + "->" + output_subscript
-        path_run = (scale_list[n], einsum_str, remaining_str)
-        path_print += "\n%4d    %24s %40s" % path_run
+    with EmissionsTracker(project_name='numpy-benchmark', experiment_id='numpy_SCL001_050c312a', output_file='/root/ecooptimizer_benchmark/emissions/numpy/string-concat-loop/050c312a.csv') as tracker:
+        for n, contraction in enumerate(contraction_list):
+            inds, idx_rm, einsum_str, remaining, blas = contraction
+            remaining_str = ",".join(remaining) + "->" + output_subscript
+            path_run = (scale_list[n], einsum_str, remaining_str)
+            path_print += "\n%4d    %24s %40s" % path_run
 
     path = ['einsum_path'] + path
     return (path, path_print)
