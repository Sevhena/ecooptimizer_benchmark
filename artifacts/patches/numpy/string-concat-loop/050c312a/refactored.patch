diff --git a/numpy/_core/einsumfunc.py b/numpy/_core/einsumfunc.py
index 8e71e6d4b1..10c7b22e57 100644
--- a/numpy/_core/einsumfunc.py
+++ b/numpy/_core/einsumfunc.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 """
 Implementation of optimized einsum.
 
@@ -1036,11 +1037,14 @@ def einsum_path(*operands, optimize='greedy', einsum_call=False):
     path_print += "%6s %24s %40s\n" % header
     path_print += "-" * 74
 
-    for n, contraction in enumerate(contraction_list):
-        inds, idx_rm, einsum_str, remaining, blas = contraction
-        remaining_str = ",".join(remaining) + "->" + output_subscript
-        path_run = (scale_list[n], einsum_str, remaining_str)
-        path_print += "\n%4d    %24s %40s" % path_run
+    with EmissionsTracker(project_name='numpy-benchmark', experiment_id='numpy_SCL001_050c312a', output_file='/root/ecooptimizer_benchmark/emissions/numpy/string-concat-loop/050c312a_refactored.csv') as tracker:
+        path_print = [path_print]
+        for n, contraction in enumerate(contraction_list):
+            inds, idx_rm, einsum_str, remaining, blas = contraction
+            remaining_str = ",".join(remaining) + "->" + output_subscript
+            path_run = (scale_list[n], einsum_str, remaining_str)
+            path_print.append('\n%4d    %24s %40s' % path_run)
+        path_print = ''.join(path_print)
 
     path = ['einsum_path'] + path
     return (path, path_print)
@@ -1495,4 +1499,4 @@ def einsum(*operands, out=None, optimize=False, **kwargs):
     if specified_out:
         return out
     else:
-        return asanyarray(operands[0], order=output_order)
+        return asanyarray(operands[0], order=output_order)
\ No newline at end of file
diff --git a/vendored-meson/meson b/vendored-meson/meson
--- a/vendored-meson/meson
+++ b/vendored-meson/meson
@@ -1 +1 @@
-Subproject commit f754c4258805056ed7be09830d96af45215d341b
+Subproject commit f754c4258805056ed7be09830d96af45215d341b-dirty
