diff --git a/numpy/_core/einsumfunc.py b/numpy/_core/einsumfunc.py
index 8e71e6d4b1..6a6c1eb046 100644
--- a/numpy/_core/einsumfunc.py
+++ b/numpy/_core/einsumfunc.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 """
 Implementation of optimized einsum.
 
@@ -601,23 +602,25 @@ def _parse_einsum_input(operands):
 
         output_list = tmp_operands[-1] if len(tmp_operands) else None
         operands = [asanyarray(v) for v in operand_list]
-        subscripts = ""
+        subscripts = []
         last = len(subscript_list) - 1
-        for num, sub in enumerate(subscript_list):
-            for s in sub:
-                if s is Ellipsis:
-                    subscripts += "..."
-                else:
-                    try:
-                        s = operator.index(s)
-                    except TypeError as e:
-                        raise TypeError(
-                            "For this input type lists must contain "
-                            "either int or Ellipsis"
-                        ) from e
-                    subscripts += einsum_symbols[s]
-            if num != last:
-                subscripts += ","
+        with EmissionsTracker(project_name='numpy-benchmark', experiment_id='numpy_SCL001_58bf0058', output_file='/root/ecooptimizer_benchmark/emissions/numpy/string-concat-loop/58bf0058_refactored.csv') as tracker:
+            for num, sub in enumerate(subscript_list):
+                for s in sub:
+                    if s is Ellipsis:
+                        subscripts += "..."
+                    else:
+                        try:
+                            s = operator.index(s)
+                        except TypeError as e:
+                            raise TypeError(
+                                "For this input type lists must contain "
+                                "either int or Ellipsis"
+                            ) from e
+                        subscripts += einsum_symbols[s]
+                if num != last:
+                    subscripts.append(',')
+            subscripts = ''.join(subscripts)
 
         if output_list is not None:
             subscripts += "->"
@@ -1495,4 +1498,4 @@ def einsum(*operands, out=None, optimize=False, **kwargs):
     if specified_out:
         return out
     else:
-        return asanyarray(operands[0], order=output_order)
+        return asanyarray(operands[0], order=output_order)
\ No newline at end of file
