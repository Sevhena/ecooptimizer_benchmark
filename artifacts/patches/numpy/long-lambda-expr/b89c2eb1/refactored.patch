diff --git a/numpy/lib/_function_base_impl.py b/numpy/lib/_function_base_impl.py
index 63346088b6..566d6cccf1 100644
--- a/numpy/lib/_function_base_impl.py
+++ b/numpy/lib/_function_base_impl.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 import builtins
 import collections.abc
 import functools
@@ -4699,7 +4700,12 @@ def _discrete_interpolation_to_boundaries(index, gamma_condition_fun):
 def _closest_observation(n, quantiles):
     # "choose the nearest even order statistic at g=0" (H&F (1996) pp. 362).
     # Order is 1-based so for zero-based indexing round to nearest odd index.
-    gamma_fun = lambda gamma, index: (gamma == 0) & (np.floor(index) % 2 == 1)
+    with EmissionsTracker(project_name='numpy-benchmark', experiment_id='numpy_LLE001_b89c2eb1', output_file='/root/ecooptimizer_benchmark/emissions/numpy/long-lambda-expr/b89c2eb1_refactored.csv') as tracker:
+        def converted_lambda_4704(gamma, index):
+            result = (gamma == 0) & (np.floor(index) % 2 == 1)
+            return result
+
+        gamma_fun = converted_lambda_4704
     return _discrete_interpolation_to_boundaries((n * quantiles) - 1 - 0.5,
                                                  gamma_fun)
 
