diff --git a/src/paho/mqtt/properties.py b/src/paho/mqtt/properties.py
index f307b86..9c6dda5 100644
--- a/src/paho/mqtt/properties.py
+++ b/src/paho/mqtt/properties.py
@@ -17,6 +17,7 @@
 import struct
 
 from .packettypes import PacketTypes
+from codecarbon import EmissionsTracker
 
 
 class MQTTException(Exception):
@@ -290,15 +291,17 @@ class Properties:
             object.__setattr__(self, name, value)
 
     def __str__(self):
-        buffer = "["
+        buffer = ['[']
         first = True
-        for name in self.names.keys():
-            compressedName = name.replace(' ', '')
-            if hasattr(self, compressedName):
-                if not first:
-                    buffer += ", "
-                buffer += f"{compressedName} : {getattr(self, compressedName)}"
-                first = False
+        with EmissionsTracker(project_name='paho.mqtt.python-benchmark', experiment_id='paho.mqtt.python_SCL001_cbf43c4b', output_file='/root/ecooptimizer_benchmark/emissions/paho.mqtt.python/string-concat-loop/cbf43c4b_refactored.csv') as tracker:
+            for name in self.names.keys():
+                compressedName = name.replace(' ', '')
+                if hasattr(self, compressedName):
+                    if not first:
+                        buffer.append(', ')
+                    buffer.append(f'{compressedName} : {getattr(self, compressedName)}')
+                    first = False
+            buffer = ''.join(buffer)
         buffer += "]"
         return buffer
 
@@ -418,4 +421,4 @@ class Properties:
                 raise MQTTException(
                     f"Property '{property}' must not exist more than once")
             setattr(self, propname, value)
-        return self, propslen + VBIlen
+        return self, propslen + VBIlen
\ No newline at end of file
