diff --git a/src/paho/mqtt/client.py b/src/paho/mqtt/client.py
index 4ccc869..98a92ec 100644
--- a/src/paho/mqtt/client.py
+++ b/src/paho/mqtt/client.py
@@ -1,3 +1,4 @@
+from codecarbon import track_emissions
 # Copyright (c) 2012-2019 Roger Light and others
 #
 # All rights reserved. This program and the accompanying materials
@@ -4816,8 +4817,10 @@ class _WebsocketWrapper:
         self._readbuffer = bytearray()
         self.connected = True
 
+    @track_emissions(project_name='paho.mqtt.python-benchmark', experiment_id='paho.mqtt.python_R6301_59ee5cc8', output_file='/root/ecooptimizer_benchmark/emissions/paho.mqtt.python/no-self-use/59ee5cc8_refactored.csv')
+    @staticmethod
     def _create_frame(
-        self, opcode: int, data: bytearray, do_masking: int = 1
+        opcode: int, data: bytearray, do_masking: int = 1
     ) -> bytearray:
         header = bytearray()
         length = len(data)
@@ -4930,12 +4933,12 @@ class _WebsocketWrapper:
 
                 # respond to non-binary opcodes, their arrival is not guaranteed because of non-blocking sockets
                 if opcode == _WebsocketWrapper.OPCODE_CONNCLOSE:
-                    frame = self._create_frame(
+                    frame = _WebsocketWrapper._create_frame(
                         _WebsocketWrapper.OPCODE_CONNCLOSE, payload, 0)
                     self._socket.send(frame)
 
                 if opcode == _WebsocketWrapper.OPCODE_PING:
-                    frame = self._create_frame(
+                    frame = _WebsocketWrapper._create_frame(
                         _WebsocketWrapper.OPCODE_PONG, payload, 0)
                     self._socket.send(frame)
 
@@ -4956,7 +4959,7 @@ class _WebsocketWrapper:
         # if previous frame was sent successfully
         if len(self._sendbuffer) == 0:
             # create websocket frame
-            frame = self._create_frame(
+            frame = _WebsocketWrapper._create_frame(
                 _WebsocketWrapper.OPCODE_BINARY, bytearray(data))
             self._sendbuffer.extend(frame)
             self._requested_size = len(data)
