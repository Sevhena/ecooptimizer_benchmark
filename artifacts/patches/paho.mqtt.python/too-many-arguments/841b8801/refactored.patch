diff --git a/examples/publish_multiple.py b/examples/publish_multiple.py
index 7c19a13..839b9af 100755
--- a/examples/publish_multiple.py
+++ b/examples/publish_multiple.py
@@ -15,9 +15,22 @@
 
 # This shows an example of using the publish.multiple helper function.
 
+class DataParams_multiple_108:
+    def __init__(self, msgs, hostname = "localhost", port = 1883, client_id = "", keepalive = 60, will = None, auth = None, tls = None, protocol = paho.MQTTv311, transport = "tcp", proxy_args = None):
+        self.msgs = msgs
+        self.hostname = hostname
+        self.port = port
+        self.client_id = client_id
+        self.keepalive = keepalive
+        self.will = will
+        self.auth = auth
+        self.tls = tls
+        self.protocol = protocol
+        self.transport = transport
+        self.proxy_args = proxy_args
 import context  # Ensures paho is in PYTHONPATH
 
 import paho.mqtt.publish as publish
 
 msgs = [{'topic': "paho/test/multiple", 'payload': "multiple 1"}, ("paho/test/multiple", "multiple 2", 0, False)]
-publish.multiple(msgs, hostname="mqtt.eclipseprojects.io")
+publish.multiple(DataParams_multiple_108(msgs, hostname = "mqtt.eclipseprojects.io"))
diff --git a/src/paho/mqtt/publish.py b/src/paho/mqtt/publish.py
index 333c190..6069070 100644
--- a/src/paho/mqtt/publish.py
+++ b/src/paho/mqtt/publish.py
@@ -1,3 +1,4 @@
+from codecarbon import track_emissions
 # Copyright (c) 2014 Roger Light <roger@atchoo.org>
 #
 # All rights reserved. This program and the accompanying materials
@@ -67,6 +68,19 @@ if TYPE_CHECKING:
     MessageTuple = Tuple[str, paho.PayloadType, int, bool]
 
     MessagesList = List[Union[MessageDict, MessageTuple]]
+class DataParams_multiple_108:
+    def __init__(self, msgs, hostname = "localhost", port = 1883, client_id = "", keepalive = 60, will = None, auth = None, tls = None, protocol = paho.MQTTv311, transport = "tcp", proxy_args = None):
+        self.msgs = msgs
+        self.hostname = hostname
+        self.port = port
+        self.client_id = client_id
+        self.keepalive = keepalive
+        self.will = will
+        self.auth = auth
+        self.tls = tls
+        self.protocol = protocol
+        self.transport = transport
+        self.proxy_args = proxy_args
 
 
 def _do_publish(client: paho.Client):
@@ -103,19 +117,9 @@ def _on_publish(
         _do_publish(client)
 
 
+@track_emissions(project_name='paho.mqtt.python-benchmark', experiment_id='paho.mqtt.python_R0913_841b8801', output_file='/root/ecooptimizer_benchmark/emissions/paho.mqtt.python/too-many-arguments/841b8801_refactored.csv')
 def multiple(
-    msgs: MessagesList,
-    hostname: str = "localhost",
-    port: int = 1883,
-    client_id: str = "",
-    keepalive: int = 60,
-    will: MessageDict | None = None,
-    auth: AuthParameter | None = None,
-    tls: TLSParameter | None = None,
-    protocol: MQTTProtocolVersion = paho.MQTTv311,
-    transport: Literal["tcp", "websockets"] = "tcp",
-    proxy_args: Any | None = None,
-) -> None:
+    data_params) -> None:
     """Publish multiple messages to a broker, then disconnect cleanly.
 
     This function creates an MQTT client, connects to a broker and publishes a
@@ -179,52 +183,52 @@ def multiple(
     :param proxy_args: a dictionary that will be given to the client.
     """
 
-    if not isinstance(msgs, Iterable):
+    if not isinstance(data_params.msgs, Iterable):
         raise TypeError('msgs must be an iterable')
-    if len(msgs) == 0:
+    if len(data_params.msgs) == 0:
         raise ValueError('msgs is empty')
 
     client = paho.Client(
         CallbackAPIVersion.VERSION2,
-        client_id=client_id,
-        userdata=collections.deque(msgs),
-        protocol=protocol,
-        transport=transport,
+        data_params.client_id=data_params.client_id,
+        userdata=collections.deque(data_params.msgs),
+        data_params.protocol=data_params.protocol,
+        data_params.transport=data_params.transport,
     )
 
     client.enable_logger()
     client.on_publish = _on_publish
     client.on_connect = _on_connect  # type: ignore
 
-    if proxy_args is not None:
-        client.proxy_set(**proxy_args)
+    if data_params.proxy_args is not None:
+        client.proxy_set(**data_params.proxy_args)
 
-    if auth:
-        username = auth.get('username')
+    if data_params.auth:
+        username = data_params.auth.get('username')
         if username:
-            password = auth.get('password')
+            password = data_params.auth.get('password')
             client.username_pw_set(username, password)
         else:
             raise KeyError("The 'username' key was not found, this is "
                            "required for auth")
 
-    if will is not None:
-        client.will_set(**will)
+    if data_params.will is not None:
+        client.will_set(**data_params.will)
 
-    if tls is not None:
-        if isinstance(tls, dict):
-            insecure = tls.pop('insecure', False)
+    if data_params.tls is not None:
+        if isinstance(data_params.tls, dict):
+            insecure = data_params.tls.pop('insecure', False)
             # mypy don't get that tls no longer contains the key insecure
-            client.tls_set(**tls)  # type: ignore[misc]
+            client.tls_set(**data_params.tls)  # type: ignore[misc]
             if insecure:
                 # Must be set *after* the `client.tls_set()` call since it sets
                 # up the SSL context that `client.tls_insecure_set` alters.
                 client.tls_insecure_set(insecure)
         else:
             # Assume input is SSLContext object
-            client.tls_set_context(tls)
+            client.tls_set_context(data_params.tls)
 
-    client.connect(hostname, port, keepalive)
+    client.connect(data_params.hostname, data_params.port, data_params.keepalive)
     client.loop_forever()
 
 
@@ -302,5 +306,4 @@ def single(
 
     msg: MessageDict = {'topic':topic, 'payload':payload, 'qos':qos, 'retain':retain}
 
-    multiple([msg], hostname, port, client_id, keepalive, will, auth, tls,
-             protocol, transport, proxy_args)
+    multiple(DataParams_multiple_108([msg], hostname, port, client_id, keepalive, will, auth, tls, protocol, transport, proxy_args))
