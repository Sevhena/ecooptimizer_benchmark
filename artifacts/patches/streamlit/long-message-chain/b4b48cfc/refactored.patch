diff --git a/lib/streamlit/commands/page_config.py b/lib/streamlit/commands/page_config.py
index a18e80e3c..15fd2ee61 100644
--- a/lib/streamlit/commands/page_config.py
+++ b/lib/streamlit/commands/page_config.py
@@ -35,6 +35,7 @@ from streamlit.runtime.metrics_util import gather_metrics
 from streamlit.runtime.scriptrunner_utils.script_run_context import get_script_run_ctx
 from streamlit.string_util import is_emoji, validate_material_icon
 from streamlit.url_util import is_url
+from codecarbon import EmissionsTracker
 
 if TYPE_CHECKING:
     from typing_extensions import TypeGuard
@@ -58,7 +59,11 @@ RANDOM_EMOJIS: Final = list(
 
 
 def _lower_clean_dict_keys(dict: MenuItems) -> dict[str, Any]:
-    return {str(k).lower().strip(): v for k, v in dict.items()}
+    with EmissionsTracker(project_name='streamlit-benchmark', experiment_id='streamlit_LMC001_b4b48cfc', output_file='/root/ecooptimizer_benchmark/emissions/streamlit/long-message-chain/b4b48cfc_refactored.csv') as tracker:
+        intermediate_0 = str(k)
+        intermediate_1 = intermediate_0.lower()
+        _carbon_wrapped_308937fb = intermediate_1.strip()
+    return {_carbon_wrapped_308937fb: v for k, v in dict.items()}
 
 
 def _get_favicon_string(page_icon: PageIcon) -> str:
