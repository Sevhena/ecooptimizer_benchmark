diff --git a/django/test/html.py b/django/test/html.py
index 33d1d4dafb..6ea2d2457c 100644
--- a/django/test/html.py
+++ b/django/test/html.py
@@ -1,5 +1,6 @@
 """Compare two HTML documents."""
 
+from codecarbon import EmissionsTracker
 import html
 from html.parser import HTMLParser
 
@@ -165,11 +166,12 @@ class Element:
 
     def __str__(self):
         output = "<%s" % self.name
-        for key, value in self.attributes:
-            if value is not None:
-                output += ' %s="%s"' % (key, value)
-            else:
-                output += " %s" % key
+        with EmissionsTracker(project_name='django-benchmark', experiment_id='django_SCL001_1942e397', output_file='/root/ecooptimizer_benchmark/emissions/django/string-concat-loop/1942e397.csv') as tracker:
+            for key, value in self.attributes:
+                if value is not None:
+                    output += ' %s="%s"' % (key, value)
+                else:
+                    output += " %s" % key
         if self.children:
             output += ">\n"
             output += "".join(
