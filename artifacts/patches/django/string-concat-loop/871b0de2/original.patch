diff --git a/django/utils/numberformat.py b/django/utils/numberformat.py
index cf8b2d219c..fe5e880f81 100644
--- a/django/utils/numberformat.py
+++ b/django/utils/numberformat.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 from decimal import Decimal
 
 from django.conf import settings
@@ -93,13 +94,14 @@ def format(
         active_interval = intervals.pop(0)
         int_part_gd = ""
         cnt = 0
-        for digit in int_part[::-1]:
-            if cnt and cnt == active_interval:
-                if intervals:
-                    active_interval = intervals.pop(0) or active_interval
-                int_part_gd += thousand_sep[::-1]
-                cnt = 0
-            int_part_gd += digit
-            cnt += 1
+        with EmissionsTracker(project_name='django-benchmark', experiment_id='django_SCL001_871b0de2', output_file='/root/ecooptimizer_benchmark/emissions/django/string-concat-loop/871b0de2.csv') as tracker:
+            for digit in int_part[::-1]:
+                if cnt and cnt == active_interval:
+                    if intervals:
+                        active_interval = intervals.pop(0) or active_interval
+                    int_part_gd += thousand_sep[::-1]
+                    cnt = 0
+                int_part_gd += digit
+                cnt += 1
         int_part = int_part_gd[::-1]
     return sign + int_part + dec_part
