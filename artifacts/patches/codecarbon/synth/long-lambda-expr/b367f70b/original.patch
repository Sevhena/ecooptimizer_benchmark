diff --git a/eco_smells/long_lambda.py b/eco_smells/long_lambda.py
index 2204204..b7cfbeb 100644
--- a/eco_smells/long_lambda.py
+++ b/eco_smells/long_lambda.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 class LongLambdaSmells:
     @staticmethod
     def small_lambda():
@@ -12,14 +13,15 @@ class LongLambdaSmells:
 
     @staticmethod
     def medium_lambda():
-        process = lambda data: [
-            item
-            for item in data
-            if item["active"]
-            and item["value"] > 100
-            and item["category"] in ("A", "B", "C")
-            and not item.get("excluded", False)
-        ]
+        with EmissionsTracker(project_name='synth-benchmark', measure_power_secs=1, experiment_id='synth_LLE001_b367f70b', output_file='/root/ecooptimizer_benchmark/emissions/synth/long-lambda-expr/b367f70b.csv') as tracker:
+            process = lambda data: [
+                item
+                for item in data
+                if item["active"]
+                and item["value"] > 100
+                and item["category"] in ("A", "B", "C")
+                and not item.get("excluded", False)
+            ]
         test_data = [
             {"active": True, "value": 150, "category": "A", "excluded": False}
             for _ in range(50)
