diff --git a/eco_smells/long_lambda.py b/eco_smells/long_lambda.py
index 2204204..b0fa70c 100644
--- a/eco_smells/long_lambda.py
+++ b/eco_smells/long_lambda.py
@@ -1,3 +1,4 @@
+from codecarbon import track_emissions
 class LongLambdaSmells:
     @staticmethod
     def small_lambda():
@@ -11,21 +12,17 @@ class LongLambdaSmells:
 
 
     @staticmethod
+    @track_emissions(project_name='synth-benchmark', tracking_mode='process', measure_power_secs=1, experiment_id='synth_LLE001_c7496f4f', output_file='/root/ecooptimizer_benchmark/emissions/synth/long-lambda-expr/c7496f4f_refactored.csv')
     def medium_lambda():
-        process = lambda data: [
-            item
-            for item in data
-            if item["active"]
-            and item["value"] > 100
-            and item["category"] in ("A", "B", "C")
-            and not item.get("excluded", False)
-        ]
-        test_data = [
-            {"active": True, "value": 150, "category": "A", "excluded": False}
-            for _ in range(50)
-        ]
-        result = process(test_data)
-        return result
+        def process(data):
+            return [
+                item
+                for item in data
+                if item["active"]
+                and item["value"] > 100
+                and item["category"] in ("A", "B", "C")
+                and not item.get("excluded", False)
+            ]
 
     @staticmethod
     def large_lambda():
