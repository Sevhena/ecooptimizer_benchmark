diff --git a/eco_smells/long_lambda.py b/eco_smells/long_lambda.py
index 2204204..d3f8f2d 100644
--- a/eco_smells/long_lambda.py
+++ b/eco_smells/long_lambda.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 class LongLambdaSmells:
     @staticmethod
     def small_lambda():
@@ -41,18 +42,20 @@ class LongLambdaSmells:
 
     @staticmethod
     def extreme_lambda():
-        pipeline = lambda dataset: [
-            {
-                "id": item["id"],
-                "processed": True,
-                "value": item["value"] * 1.2
-                if item["type"] == "A"
-                else (item["value"] * 0.8 if item["type"] == "B" else item["value"]),
-                "status": "active" if item["value"] > 50 else "inactive",
-            }
-            for item in dataset
-            if item["valid"] and not item.get("deleted", False)
-        ]
+        with EmissionsTracker(project_name='synth-benchmark', tracking_mode='process', measure_power_secs=1, experiment_id='synth_LLE001_18ff6862', output_file='/root/ecooptimizer_benchmark/emissions/synth/long-lambda-expr/18ff6862_refactored.csv') as tracker:
+            def pipeline(dataset):
+                return [
+                    {
+                        "id": item["id"],
+                        "processed": True,
+                        "value": item["value"] * 1.2
+                        if item["type"] == "A"
+                        else (item["value"] * 0.8 if item["type"] == "B" else item["value"]),
+                        "status": "active" if item["value"] > 50 else "inactive",
+                    }
+                    for item in dataset
+                    if item["valid"] and not item.get("deleted", False)
+                ]
         test_data = [
             {
                 "id": i,
