diff --git a/eco_smells/long_lambda.py b/eco_smells/long_lambda.py
index 2204204..228b04e 100644
--- a/eco_smells/long_lambda.py
+++ b/eco_smells/long_lambda.py
@@ -1,12 +1,14 @@
+from codecarbon import EmissionsTracker
 class LongLambdaSmells:
     @staticmethod
     def small_lambda():
         """Lambda with string processing (>100 chars)"""
-        process_text = lambda text: (
-            text.strip().lower().replace(' ', '_') 
-            if len(text) > 3 
-            else text.upper() + '!'
-        )
+        with EmissionsTracker(project_name='synth-benchmark', tracking_mode='process', measure_power_secs=1, experiment_id='synth_LLE001_70892924', output_file='/root/ecooptimizer_benchmark/emissions/synth/long-lambda-expr/70892924.csv') as tracker:
+            process_text = lambda text: (
+                text.strip().lower().replace(' ', '_') 
+                if len(text) > 3 
+                else text.upper() + '!'
+            )
         return process_text("  Hello World  ")  # -> "hello_world"
 
 
