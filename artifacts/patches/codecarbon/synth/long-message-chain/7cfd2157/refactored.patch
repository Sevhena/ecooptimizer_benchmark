diff --git a/eco_smells/message_chains.py b/eco_smells/message_chains.py
index f79488b..b348f3b 100644
--- a/eco_smells/message_chains.py
+++ b/eco_smells/message_chains.py
@@ -2,6 +2,7 @@ import hashlib
 import statistics
 import random
 from datetime import datetime, timedelta
+from codecarbon import EmissionsTracker
 
 
 class MessageChainSmells:
@@ -147,13 +148,14 @@ class MessageChainSmells:
     def extreme_chain():
         """8-level chain with large data set"""
         return (
-            MessageChainSmells.DataPipeline()
-            .generate_initial_dataset(100_000)
-            .filter_active_users()
-            .add_computed_fields()
-            .calculate_demographics()
-            .compute_engagement_metrics()
-            .hash_sensitive_data()
-            .analyze_metadata()
-            .build_final_report()
+            with EmissionsTracker(project_name='synth-benchmark', tracking_mode='process', measure_power_secs=1, experiment_id='synth_LMC001_7cfd2157', output_file='/root/ecooptimizer_benchmark/emissions/synth/long-message-chain/7cfd2157_refactored.csv') as tracker:
+                MessageChainSmells.DataPipeline()
+                .generate_initial_dataset(100_000)
+                .filter_active_users()
+                .add_computed_fields()
+                .calculate_demographics()
+                .compute_engagement_metrics()
+                .hash_sensitive_data()
+                .analyze_metadata()
+                .build_final_report()
         )
