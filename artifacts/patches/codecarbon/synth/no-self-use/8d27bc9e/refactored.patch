diff --git a/eco_smells/member_ignoring.py b/eco_smells/member_ignoring.py
index 23aec60..32db8c5 100644
--- a/eco_smells/member_ignoring.py
+++ b/eco_smells/member_ignoring.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 # Class demonstrating the smell pattern
 class StringProcessor:
     def __init__(self):
@@ -8,7 +9,9 @@ class StringProcessor:
         return text[::-1]
 
     # Smelly method - instance method that doesn't use self
-    def count_vowels(self, text):
+    @track_emissions(project_name='synth-benchmark', measure_power_secs=1, experiment_id='synth_R6301_8d27bc9e', output_file='/root/ecooptimizer_benchmark/emissions/synth/no-self-use/8d27bc9e_refactored.csv')
+    @staticmethod
+    def count_vowels(text):
         return sum(1 for char in text.lower() if char in "aeiou")
 
     # Proper instance method using self
@@ -30,6 +33,6 @@ class MemberIgnoringSmells:
     @classmethod
     def necessary_instance_obj(cls):
         processor = StringProcessor()
-        vowels = processor.count_vowels(cls.attr_text)
+        vowels = StringProcessor.count_vowels(cls.attr_text)
         processor.update_and_process(cls.attr_text)
         return vowels
