diff --git a/eco_smells/member_ignoring.py b/eco_smells/member_ignoring.py
index 23aec60..0dfe813 100644
--- a/eco_smells/member_ignoring.py
+++ b/eco_smells/member_ignoring.py
@@ -1,3 +1,6 @@
+from codecarbon import EmissionsTracker
+
+
 # Class demonstrating the smell pattern
 class StringProcessor:
     def __init__(self):
@@ -23,8 +26,15 @@ class MemberIgnoringSmells:
 
     @classmethod
     def unecessary_instance_obj(cls):
-        processor = StringProcessor()
-        result = processor.reverse(cls.attr_text)
+        with EmissionsTracker(
+            project_name="synth-benchmark",
+            tracking_mode="process",
+            measure_power_secs=1,
+            experiment_id="synth_R6301_34a8d49c",
+            output_file="/root/ecooptimizer_benchmark/emissions/synth/no-self-use/34a8d49c.csv",
+        ) as tracker:
+            processor = StringProcessor()
+            result = processor.reverse(cls.attr_text)
         return result
 
     @classmethod
