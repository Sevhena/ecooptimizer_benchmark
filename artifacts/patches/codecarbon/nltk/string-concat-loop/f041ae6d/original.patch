diff --git a/nltk/stem/porter.py b/nltk/stem/porter.py
index 36c5bdf36..2bdf0a603 100644
--- a/nltk/stem/porter.py
+++ b/nltk/stem/porter.py
@@ -17,6 +17,7 @@ algorithm at
 which includes another Python implementation and other implementations
 in many languages.
 """
+from codecarbon import EmissionsTracker
 
 __docformat__ = "plaintext"
 
@@ -184,11 +185,12 @@ class PorterStemmer(StemmerI):
         # character in `stem` is a consonant or a vowel.
         # e.g. 'falafel' becomes 'cvcvcvc',
         #      'architecture' becomes 'vcccvcvccvcv'
-        for i in range(len(stem)):
-            if self._is_consonant(stem, i):
-                cv_sequence += "c"
-            else:
-                cv_sequence += "v"
+        with EmissionsTracker(project_name='nltk-benchmark', measure_power_secs=1, experiment_id='nltk_SCL001_f041ae6d', output_file='/root/ecooptimizer_benchmark/emissions/nltk/string-concat-loop/f041ae6d.csv') as tracker:
+            for i in range(len(stem)):
+                if self._is_consonant(stem, i):
+                    cv_sequence += "c"
+                else:
+                    cv_sequence += "v"
 
         # Count the number of 'vc' occurrences, which is equivalent to
         # the number of 'VC' occurrences in Porter's reduced form in the
