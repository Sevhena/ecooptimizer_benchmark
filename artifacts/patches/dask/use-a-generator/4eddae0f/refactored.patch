diff --git a/dask/array/overlap.py b/dask/array/overlap.py
index 4d52db810..0f1d5afd8 100644
--- a/dask/array/overlap.py
+++ b/dask/array/overlap.py
@@ -21,6 +21,7 @@ from dask.base import tokenize
 from dask.highlevelgraph import HighLevelGraph
 from dask.layers import ArrayOverlapLayer
 from dask.utils import derived_from
+from codecarbon import EmissionsTracker
 
 
 def _overlap_internal_chunks(original_chunks, axes):
@@ -443,7 +444,8 @@ def overlap(x, depth, boundary, *, allow_rechunk=True):
         )  # this is a no-op if x.chunks == new_chunks
 
     else:
-        original_chunks_too_small = any([min(c) < d for d, c in zip(depths, x.chunks)])
+        with EmissionsTracker(project_name='dask-benchmark', experiment_id='dask_R1729_4eddae0f', output_file='/root/ecooptimizer_benchmark/emissions/dask/use-a-generator/4eddae0f_refactored.csv') as tracker:
+            original_chunks_too_small = any(min(c) < d for d, c in zip(depths, x.chunks))
         if original_chunks_too_small:
             raise ValueError(
                 "Overlap depth is larger than smallest chunksize.\n"
