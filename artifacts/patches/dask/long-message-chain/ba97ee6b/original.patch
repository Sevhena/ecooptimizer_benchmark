diff --git a/dask/dataframe/groupby.py b/dask/dataframe/groupby.py
index 064b1e963..997bc07b7 100644
--- a/dask/dataframe/groupby.py
+++ b/dask/dataframe/groupby.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 from __future__ import annotations
 
 import collections
@@ -514,9 +515,10 @@ def _cov_agg(_t, levels, ddof, std=False, sort=False):
     total_muls = concat(muls).groupby(level=levels, sort=sort).sum()
     total_counts = concat(counts).groupby(level=levels).sum()
     result = (
-        concat([total_sums, total_muls, total_counts], axis=1)
-        .groupby(level=levels)
-        .apply(_cov_finalizer, cols=cols, std=std)
+        with EmissionsTracker(project_name='dask-benchmark', experiment_id='dask_LMC001_ba97ee6b', output_file='/root/ecooptimizer_benchmark/emissions/dask/long-message-chain/ba97ee6b.csv') as tracker:
+            concat([total_sums, total_muls, total_counts], axis=1)
+            .groupby(level=levels)
+            .apply(_cov_finalizer, cols=cols, std=std)
     )
 
     inv_col_mapping = {v: k for k, v in col_mapping.items()}
