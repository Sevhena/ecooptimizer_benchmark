diff --git a/dask/utils.py b/dask/utils.py
index 6edfda8f8..eb63a6694 100644
--- a/dask/utils.py
+++ b/dask/utils.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 from __future__ import annotations
 
 import codecs
@@ -1984,14 +1985,16 @@ def key_split(s):
         if not words[0][0].isalpha():
             result = words[0].split(",")[0].strip("_'()\"")
         else:
-            result = words[0]
-        for word in words[1:]:
-            if word.isalpha() and not (
-                len(word) == 8 and hex_pattern.match(word) is not None
-            ):
-                result += "-" + word
-            else:
-                break
+            result = [words[0]]
+        with EmissionsTracker(project_name='dask-benchmark', experiment_id='dask_SCL001_3cad881d', output_file='/root/ecooptimizer_benchmark/emissions/dask/string-concat-loop/3cad881d_refactored.csv') as tracker:
+            for word in words[1:]:
+                if word.isalpha() and not (
+                    len(word) == 8 and hex_pattern.match(word) is not None
+                ):
+                    result.append('-' + word)
+                else:
+                    break
+            result = ''.join(result)
         if len(result) == 32 and re.match(r"[a-f0-9]{32}", result):
             return "data"
         else:
@@ -2324,4 +2327,4 @@ class disable_gc(ContextDecorator):
     def __exit__(self, exc_type, exc_value, traceback):
         if self._gc_enabled:
             gc.enable()
-        return False
+        return False
\ No newline at end of file
