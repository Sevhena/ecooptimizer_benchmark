diff --git a/dask/dataframe/dask_expr/_reductions.py b/dask/dataframe/dask_expr/_reductions.py
index 53d549d32..5df698da9 100644
--- a/dask/dataframe/dask_expr/_reductions.py
+++ b/dask/dataframe/dask_expr/_reductions.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 from __future__ import annotations
 
 import functools
@@ -75,12 +76,13 @@ class Chunk(Blockwise):
             for dep in self.dependencies():
                 lines.extend(dep._tree_repr_lines(2))
 
-        for k, v in self._kwargs.items():
-            try:
-                if v != self.kind._defaults[k]:
+        with EmissionsTracker(project_name='dask-benchmark', experiment_id='dask_SCL001_bf9f2446', output_file='/root/ecooptimizer_benchmark/emissions/dask/string-concat-loop/bf9f2446.csv') as tracker:
+            for k, v in self._kwargs.items():
+                try:
+                    if v != self.kind._defaults[k]:
+                        header += f" {k}={v}"
+                except KeyError:
                     header += f" {k}={v}"
-            except KeyError:
-                header += f" {k}={v}"
 
         lines = [header] + lines
         lines = [" " * indent + line for line in lines]
