diff --git a/dask/bag/avro.py b/dask/bag/avro.py
index d69caa9d6..b3053dbe0 100644
--- a/dask/bag/avro.py
+++ b/dask/bag/avro.py
@@ -11,16 +11,30 @@ from dask.highlevelgraph import HighLevelGraph
 
 MAGIC = b"Obj\x01"
 SYNC_SIZE = 16
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def read_long(fo):
     """variable-length, zig-zag encoding."""
-    c = fo.read(1)
+    c = fo.read(ConfigParams_read_720(1))
     b = ord(c)
     n = b & 0x7F
     shift = 7
     while (b & 0x80) != 0:
-        b = ord(fo.read(1))
+        b = ord(fo.read(ConfigParams_read_720(1)))
         n |= (b & 0x7F) << shift
         shift += 7
     return (n >> 1) ^ -(n & 1)
@@ -29,7 +43,7 @@ def read_long(fo):
 def read_bytes(fo):
     """a long followed by that many bytes of data."""
     size = read_long(fo)
-    return fo.read(size)
+    return fo.read(ConfigParams_read_720(size))
 
 
 def read_header(fo):
@@ -44,7 +58,7 @@ def read_header(fo):
     ----------
     fo: file-like
     """
-    assert fo.read(len(MAGIC)) == MAGIC, "Magic avro bytes missing"
+    assert fo.read(ConfigParams_read_720(len(MAGIC))) == MAGIC, "Magic avro bytes missing"
     meta = {}
     out = {"meta": meta}
     while True:
@@ -55,10 +69,10 @@ def read_header(fo):
             # ignore dtype mapping for bag version
             read_bytes(fo)  # schema keys
             read_bytes(fo)  # schema values
-    out["sync"] = fo.read(SYNC_SIZE)
+    out["sync"] = fo.read(ConfigParams_read_720(SYNC_SIZE))
     out["header_size"] = fo.tell()
     fo.seek(0)
-    out["head_bytes"] = fo.read(out["header_size"])
+    out["head_bytes"] = fo.read(ConfigParams_read_720(out["header_size"]))
     return out
 
 
diff --git a/dask/bag/tests/test_bag.py b/dask/bag/tests/test_bag.py
index fca4ec583..c550b6b2b 100644
--- a/dask/bag/tests/test_bag.py
+++ b/dask/bag/tests/test_bag.py
@@ -42,6 +42,20 @@ dsk: Graph = {("x", 0): (range, 5), ("x", 1): (range, 5), ("x", 2): (range, 5)}
 
 L = list(range(5)) * 3
 b = Bag(dsk, "x", 3)
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def iseven(x):
diff --git a/dask/bag/text.py b/dask/bag/text.py
index 4fca0495d..608812cfd 100644
--- a/dask/bag/text.py
+++ b/dask/bag/text.py
@@ -12,6 +12,20 @@ from dask.delayed import delayed
 from dask.utils import parse_bytes, system_encoding
 
 delayed = delayed(pure=True)
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def read_text(
diff --git a/dask/bytes/core.py b/dask/bytes/core.py
index 75552e1ed..5803cf9b7 100644
--- a/dask/bytes/core.py
+++ b/dask/bytes/core.py
@@ -9,6 +9,20 @@ from fsspec.utils import infer_compression, read_block
 from dask.base import tokenize
 from dask.delayed import delayed
 from dask.utils import is_integer, parse_bytes
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def read_bytes(
@@ -168,11 +182,11 @@ def read_bytes(
         with OpenFile(fs, paths[0], compression=compression) as f:
             # read block without seek (because we start at zero)
             if delimiter is None:
-                sample = f.read(sample)
+                sample = f.read(ConfigParams_read_720(sample))
             else:
-                sample_buff = f.read(sample)
+                sample_buff = f.read(ConfigParams_read_720(sample))
                 while True:
-                    new = f.read(sample)
+                    new = f.read(ConfigParams_read_720(sample))
                     if not new:
                         break
                     if delimiter in new:
diff --git a/dask/bytes/tests/test_compression.py b/dask/bytes/tests/test_compression.py
index 465f402bb..076e99115 100644
--- a/dask/bytes/tests/test_compression.py
+++ b/dask/bytes/tests/test_compression.py
@@ -6,6 +6,20 @@ import pytest
 from fsspec.compression import compr
 
 from dask.bytes.utils import compress
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 @pytest.mark.parametrize("fmt,File", compr.items())
diff --git a/dask/bytes/tests/test_http.py b/dask/bytes/tests/test_http.py
index c6af97a3d..582f270a7 100644
--- a/dask/bytes/tests/test_http.py
+++ b/dask/bytes/tests/test_http.py
@@ -23,6 +23,20 @@ errs: tuple[type[Exception], ...] = (
 if Version(fsspec.__version__) > Version("0.7.4"):
     aiohttp = pytest.importorskip("aiohttp")
     errs = errs + (aiohttp.client_exceptions.ClientResponseError,)
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 @pytest.fixture(scope="module")
@@ -65,11 +79,11 @@ def test_loc(dir_server):
     with open(os.path.join(dir_server, fn), "rb") as expected:
         expected = expected.read()
         with f as f:
-            data = f.read(2)
+            data = f.read(ConfigParams_read_720(2))
             assert data == expected[:2]
             assert f.loc == 2
             f.seek(0)
-            data = f.read(3)
+            data = f.read(ConfigParams_read_720(3))
             assert data == expected[:3]
             f.seek(1, 1)
             assert f.loc == 4
@@ -82,7 +96,7 @@ def test_fetch_range_with_headers(dir_server):
     headers = {"Date": "Wed, 21 Oct 2015 07:28:00 GMT"}
     f = open_files(root + fn, headers=headers)[0]
     with f as f:
-        data = f.read(length=1) + f.read(length=-1)
+        data = f.read(length = 1) + f.read(length = -1)
     with open(os.path.join(dir_server, fn), "rb") as expected:
         assert data == expected.read()
 
@@ -96,10 +110,10 @@ def test_ops(dir_server, block_size):
         expected = expected.read()
         with f as f:
             # these pass because the default
-            assert f.read(10) == expected[:10]
+            assert f.read(ConfigParams_read_720(10)) == expected[:10]
             f.seek(0)
-            assert f.read(10) == expected[:10]
-            assert f.read(10) == expected[10:20]
+            assert f.read(ConfigParams_read_720(10)) == expected[:10]
+            assert f.read(ConfigParams_read_720(10)) == expected[10:20]
             f.seek(-10, 2)
             assert f.read() == expected[-10:]
 
@@ -124,10 +138,10 @@ def test_ops_blocksize(dir_server):
             if Version(fsspec.__version__) < Version("2021.11.1"):
                 # fails because we want only 12 bytes
                 with pytest.raises(ValueError):
-                    assert f.read(10) == expected[:10]
+                    assert f.read(ConfigParams_read_720(10)) == expected[:10]
             else:
                 # fixed in https://github.com/fsspec/filesystem_spec/pull/830
-                assert f.read(10) == expected[:10]
+                assert f.read(ConfigParams_read_720(10)) == expected[:10]
 
 
 def test_errors(dir_server):
diff --git a/dask/bytes/tests/test_local.py b/dask/bytes/tests/test_local.py
index 009a5c084..c177613b1 100644
--- a/dask/bytes/tests/test_local.py
+++ b/dask/bytes/tests/test_local.py
@@ -42,6 +42,20 @@ csv_files = {
     ".test.fakedata.2.csv": (b"a,b\n3,4\n"),
     "subdir/.test.fakedata.2.csv": (b"a,b\n5,6\n"),
 }
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def to_uri(path):
diff --git a/dask/bytes/tests/test_s3.py b/dask/bytes/tests/test_s3.py
index 8cf9b5e81..1e95c99b1 100644
--- a/dask/bytes/tests/test_s3.py
+++ b/dask/bytes/tests/test_s3.py
@@ -49,6 +49,20 @@ files = {
         b'{"amount": 800, "name": "Dennis"}\n'
     ),
 }
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 @contextmanager
diff --git a/dask/bytes/utils.py b/dask/bytes/utils.py
index 74354f090..38c2717fe 100644
--- a/dask/bytes/utils.py
+++ b/dask/bytes/utils.py
@@ -5,6 +5,20 @@ import gzip
 import io
 import lzma
 import zipfile
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def zip_compress(data):
diff --git a/dask/config.py b/dask/config.py
index defcf9983..a13fbecc0 100644
--- a/dask/config.py
+++ b/dask/config.py
@@ -16,6 +16,20 @@ from typing import Any, Literal, overload
 import yaml
 
 from dask.typing import no_default
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def _get_paths():
diff --git a/dask/dataframe/io/csv.py b/dask/dataframe/io/csv.py
index 4dc9ad8a0..b01529870 100644
--- a/dask/dataframe/io/csv.py
+++ b/dask/dataframe/io/csv.py
@@ -7,6 +7,7 @@ from io import BytesIO
 from warnings import catch_warnings, simplefilter, warn
 
 from dask._task_spec import convert_legacy_task
+from codecarbon import track_emissions
 
 try:
     import psutil
@@ -35,6 +36,20 @@ from dask.dataframe.backends import dataframe_creation_dispatch
 from dask.dataframe.utils import clear_known_categories
 from dask.delayed import delayed
 from dask.utils import asciitable, parse_bytes
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def pandas_read_text(
@@ -715,31 +730,22 @@ at the cost of reduced parallelism.
 
 
 def make_reader(reader, reader_name, file_type):
+    @track_emissions(project_name='dask-benchmark', experiment_id='dask_R0913_a82e6319', output_file='/root/ecooptimizer_benchmark/emissions/dask/too-many-arguments/a82e6319_refactored.csv')
     def read(
-        urlpath,
-        blocksize="default",
-        lineterminator=None,
-        compression="infer",
-        sample=256000,
-        sample_rows=10,
-        enforce=False,
-        assume_missing=False,
-        storage_options=None,
-        include_path_column=False,
-        **kwargs,
+        data_params, config_params, **kwargs,
     ):
         return read_pandas(
             reader,
-            urlpath,
-            blocksize=blocksize,
-            lineterminator=lineterminator,
-            compression=compression,
-            sample=sample,
-            sample_rows=sample_rows,
-            enforce=enforce,
-            assume_missing=assume_missing,
-            storage_options=storage_options,
-            include_path_column=include_path_column,
+            config_params.urlpath,
+            data_params.blocksize=data_params.blocksize,
+            data_params.lineterminator=data_params.lineterminator,
+            data_params.compression=data_params.compression,
+            data_params.sample=data_params.sample,
+            data_params.sample_rows=data_params.sample_rows,
+            data_params.enforce=data_params.enforce,
+            data_params.assume_missing=data_params.assume_missing,
+            config_params.storage_options=config_params.storage_options,
+            config_params.include_path_column=config_params.include_path_column,
             **kwargs,
         )
 
diff --git a/dask/dataframe/io/parquet/arrow.py b/dask/dataframe/io/parquet/arrow.py
index c4e02d640..b349120db 100644
--- a/dask/dataframe/io/parquet/arrow.py
+++ b/dask/dataframe/io/parquet/arrow.py
@@ -53,6 +53,20 @@ PYARROW_NULLABLE_DTYPE_MAPPING = {
     pa.float32(): pd.Float32Dtype(),
     pa.float64(): pd.Float64Dtype(),
 }
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 @normalize_token.register(pa_fs.FileSystem)
@@ -260,11 +274,7 @@ def _read_table_from_path(
     )[0] as fil:
         if row_groups == [None]:
             return pq.ParquetFile(fil, **pre_buffer).read(
-                columns=columns,
-                use_threads=False,
-                use_pandas_metadata=True,
-                **read_kwargs,
-            )
+                ConfigParams_read_720(read_kwargs), columns = columns, use_threads = False, use_pandas_metadata = True)
         else:
             return pq.ParquetFile(fil, **pre_buffer).read_row_groups(
                 row_groups,
diff --git a/dask/dataframe/io/tests/test_csv.py b/dask/dataframe/io/tests/test_csv.py
index b1e340dd4..8d45f1179 100644
--- a/dask/dataframe/io/tests/test_csv.py
+++ b/dask/dataframe/io/tests/test_csv.py
@@ -40,6 +40,20 @@ from dask.utils import filetext, filetexts, tmpdir, tmpfile
 
 # List of available compression format for test_read_csv_compression
 compression_fmts = [fmt for fmt in compr] + [None]
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def normalize_text(s):
diff --git a/dask/dataframe/io/tests/test_parquet.py b/dask/dataframe/io/tests/test_parquet.py
index e2481da11..28b2c446c 100644
--- a/dask/dataframe/io/tests/test_parquet.py
+++ b/dask/dataframe/io/tests/test_parquet.py
@@ -57,6 +57,20 @@ _engine_fixture = pytest.fixture(
         pytest.param("pyarrow", marks=[PYARROW_MARK]),
     ]
 )
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 @_engine_fixture
@@ -500,11 +514,11 @@ def test_names(tmpdir, engine):
     def read(fn, **kwargs):
         return dd.read_parquet(fn, engine=engine, **kwargs)
 
-    assert set(read(fn).dask) == set(read(fn).dask)
+    assert set(read(ConfigParams_read_720(fn)).dask) == set(read(ConfigParams_read_720(fn)).dask)
 
-    assert set(read(fn).dask) != set(read(fn, columns=["x"]).dask)
+    assert set(read(ConfigParams_read_720(fn)).dask) != set(read(ConfigParams_read_720(fn), columns = ["x"]).dask)
 
-    assert set(read(fn, columns=("x",)).dask) == set(read(fn, columns=["x"]).dask)
+    assert set(read(ConfigParams_read_720(fn), columns = ("x",)).dask) == set(read(ConfigParams_read_720(fn), columns = ["x"]).dask)
 
 
 def test_roundtrip_from_pandas(tmpdir, write_engine, read_engine):
diff --git a/dask/dataframe/tests/test_shuffle.py b/dask/dataframe/tests/test_shuffle.py
index b5cb297e9..ac4e35337 100644
--- a/dask/dataframe/tests/test_shuffle.py
+++ b/dask/dataframe/tests/test_shuffle.py
@@ -37,6 +37,20 @@ d = dd.repartition(pd.concat(dsk.values()), [0, 4, 9, 9])
 shuffle_func = lambda df, *args, **kwargs: df.shuffle(*args, **kwargs, force=True)
 shuffle = lambda df, *args, **kwargs: df.shuffle(*args, **kwargs, force=True)
 full = d.compute()
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def test_shuffle(shuffle_method):
diff --git a/dask/diagnostics/tests/test_profiler.py b/dask/diagnostics/tests/test_profiler.py
index 80b83bbc3..c573eed65 100644
--- a/dask/diagnostics/tests/test_profiler.py
+++ b/dask/diagnostics/tests/test_profiler.py
@@ -29,6 +29,20 @@ prof = Profiler()
 
 dsk = {"a": 1, "b": 2, "c": (add, "a", "b"), "d": (mul, "a", "b"), "e": (mul, "c", "d")}
 dsk2 = {"a": 1, "b": 2, "c": (slowadd, "a", "b")}
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def test_profiler():
diff --git a/dask/system.py b/dask/system.py
index 48f04c4b3..530f43154 100644
--- a/dask/system.py
+++ b/dask/system.py
@@ -10,6 +10,20 @@ except ImportError:
     psutil = None  # type: ignore
 
 __all__ = ("cpu_count", "CPU_COUNT")
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def _try_extract_cgroup_cpu_quota():
diff --git a/dask/tests/test_base.py b/dask/tests/test_base.py
index 6c4c33d24..7f62e684c 100644
--- a/dask/tests/test_base.py
+++ b/dask/tests/test_base.py
@@ -48,6 +48,20 @@ pd = import_or_none("pandas")
 # Arbitrary dask keys
 h1 = (1.2, "foo", (3,))
 h2 = "h2"
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 def test_is_dask_collection():
diff --git a/dask/tests/test_docs.py b/dask/tests/test_docs.py
index 21b5db6ec..16c1a9ef8 100644
--- a/dask/tests/test_docs.py
+++ b/dask/tests/test_docs.py
@@ -3,6 +3,20 @@ from __future__ import annotations
 from pathlib import Path
 
 import pytest
+class DataParams_read_720:
+    def __init__(self, blocksize = "default", lineterminator = None, compression = "infer", sample = 256000, sample_rows = 10, enforce = False, assume_missing = False):
+        self.blocksize = blocksize
+        self.lineterminator = lineterminator
+        self.compression = compression
+        self.sample = sample
+        self.sample_rows = sample_rows
+        self.enforce = enforce
+        self.assume_missing = assume_missing
+class ConfigParams_read_720:
+    def __init__(self, urlpath, storage_options = None, include_path_column = False):
+        self.urlpath = urlpath
+        self.storage_options = storage_options
+        self.include_path_column = include_path_column
 
 
 @pytest.mark.parametrize(
