diff --git a/dask/array/_reductions_generic.py b/dask/array/_reductions_generic.py
index 3dee26537..3ee69f6ea 100644
--- a/dask/array/_reductions_generic.py
+++ b/dask/array/_reductions_generic.py
@@ -20,24 +20,28 @@ from dask.base import tokenize
 from dask.blockwise import lol_tuples
 from dask.highlevelgraph import HighLevelGraph
 from dask.utils import funcname, getargspec, is_series_like
+from codecarbon import track_emissions
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
+@track_emissions(project_name='dask-benchmark', experiment_id='dask_R0913_b217bf24', output_file='/root/ecooptimizer_benchmark/emissions/dask/too-many-arguments/b217bf24_refactored.csv')
 def reduction(
-    x,
-    chunk,
-    aggregate,
-    axis=None,
-    keepdims=False,
-    dtype=None,
-    split_every=None,
-    combine=None,
-    name=None,
-    out=None,
-    concatenate=True,
-    output_size=1,
-    meta=None,
-    weights=None,
-):
+    data_params):
     """General version of reductions
 
     Parameters
@@ -137,54 +141,54 @@ def reduction(
         remove them.
 
     """
-    if axis is None:
-        axis = tuple(range(x.ndim))
-    if isinstance(axis, Integral):
-        axis = (axis,)
-    axis = validate_axis(axis, x.ndim)
+    if data_params.axis is None:
+        data_params.axis = tuple(range(data_params.x.ndim))
+    if isinstance(data_params.axis, Integral):
+        data_params.axis = (data_params.axis,)
+    data_params.axis = validate_axis(data_params.axis, data_params.x.ndim)
 
-    if dtype is None:
+    if data_params.dtype is None:
         raise ValueError("Must specify dtype")
-    if "dtype" in getargspec(chunk).args:
-        chunk = partial(chunk, dtype=dtype)
-    if "dtype" in getargspec(aggregate).args:
-        aggregate = partial(aggregate, dtype=dtype)
-    if is_series_like(x):
-        x = x.values
+    if "dtype" in getargspec(data_params.chunk).args:
+        data_params.chunk = partial(data_params.chunk, data_params.dtype=data_params.dtype)
+    if "dtype" in getargspec(data_params.aggregate).args:
+        data_params.aggregate = partial(data_params.aggregate, data_params.dtype=data_params.dtype)
+    if is_series_like(data_params.x):
+        data_params.x = data_params.x.values
 
     # Map chunk across all blocks
-    inds = tuple(range(x.ndim))
+    inds = tuple(range(data_params.x.ndim))
 
-    args = (x, inds)
-    if weights is not None:
+    args = (data_params.x, inds)
+    if data_params.weights is not None:
         # Broadcast weights to x and add to args
-        wgt = asanyarray(weights)
+        wgt = asanyarray(data_params.weights)
         try:
-            wgt = broadcast_to(wgt, x.shape)
+            wgt = broadcast_to(wgt, data_params.x.shape)
         except ValueError:
             raise ValueError(
                 f"Weights with shape {wgt.shape} are not broadcastable "
-                f"to x with shape {x.shape}"
+                f"to x with shape {data_params.x.shape}"
             )
 
         args += (wgt, inds)
 
     # The dtype of `tmp` doesn't actually matter, and may be incorrect.
     tmp = blockwise(
-        chunk, inds, *args, axis=axis, keepdims=True, token=name, dtype=dtype or float
+        data_params.chunk, inds, *args, data_params.axis=data_params.axis, data_params.keepdims=True, token=data_params.name, data_params.dtype=data_params.dtype or float
     )
     tmp._chunks = tuple(
-        (output_size,) * len(c) if i in axis else c for i, c in enumerate(tmp.chunks)
+        (data_params.output_size,) * len(c) if i in data_params.axis else c for i, c in enumerate(tmp.chunks)
     )
 
-    if meta is None and hasattr(x, "_meta"):
+    if data_params.meta is None and hasattr(data_params.x, "_meta"):
         try:
             reduced_meta = compute_meta(
-                chunk, x.dtype, x._meta, axis=axis, keepdims=True, computing_meta=True
+                data_params.chunk, data_params.x.data_params.dtype, data_params.x._meta, data_params.axis=data_params.axis, data_params.keepdims=True, computing_meta=True
             )
         except TypeError:
             reduced_meta = compute_meta(
-                chunk, x.dtype, x._meta, axis=axis, keepdims=True
+                data_params.chunk, data_params.x.data_params.dtype, data_params.x._meta, data_params.axis=data_params.axis, data_params.keepdims=True
             )
         except ValueError:
             pass
@@ -193,23 +197,23 @@ def reduction(
 
     result = _tree_reduce(
         tmp,
-        aggregate,
-        axis,
-        keepdims,
-        dtype,
-        split_every,
-        combine,
-        name=name,
-        concatenate=concatenate,
+        data_params.aggregate,
+        data_params.axis,
+        data_params.keepdims,
+        data_params.dtype,
+        data_params.split_every,
+        data_params.combine,
+        data_params.name=data_params.name,
+        data_params.concatenate=data_params.concatenate,
         reduced_meta=reduced_meta,
     )
-    if keepdims and output_size != 1:
+    if data_params.keepdims and data_params.output_size != 1:
         result._chunks = tuple(
-            (output_size,) if i in axis else c for i, c in enumerate(tmp.chunks)
+            (data_params.output_size,) if i in data_params.axis else c for i, c in enumerate(tmp.chunks)
         )
-    if meta is not None:
-        result._meta = meta
-    return handle_out(out, result)
+    if data_params.meta is not None:
+        result._meta = data_params.meta
+    return handle_out(data_params.out, result)
 
 
 def _tree_reduce(
diff --git a/dask/array/chunk.py b/dask/array/chunk.py
index ddefd6308..fe79b809b 100644
--- a/dask/array/chunk.py
+++ b/dask/array/chunk.py
@@ -11,6 +11,22 @@ import numpy as np
 from tlz import concat
 
 from dask.core import flatten
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 def keepdims_wrapper(a_callable):
@@ -136,7 +152,7 @@ def coarsen(reduction, x, axes, trim_excess=False, **kwargs):
     # (10, 10) -> (5, 2, 5, 2)
     newshape = tuple(concat([(x.shape[i] // axes[i], axes[i]) for i in range(x.ndim)]))
 
-    return reduction(x.reshape(newshape), axis=tuple(range(1, x.ndim * 2, 2)), **kwargs)
+    return reduction(DataParams_reduction_27(x.reshape(newshape), kwargs, axis = tuple(range(1, x.ndim * 2, 2))))
 
 
 def trim(x, axes=None):
diff --git a/dask/array/ma.py b/dask/array/ma.py
index cc00aaecc..87076ce38 100644
--- a/dask/array/ma.py
+++ b/dask/array/ma.py
@@ -11,6 +11,22 @@ from dask.array.routines import _average
 from dask.array.routines import nonzero as _nonzero
 from dask.tokenize import normalize_token
 from dask.utils import derived_from
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 @normalize_token.register(np.ma.masked_array)
@@ -184,15 +200,7 @@ def _chunk_count(x, axis=None, keepdims=None):
 @derived_from(np.ma)
 def count(a, axis=None, keepdims=False, split_every=None):
     return reduction(
-        a,
-        _chunk_count,
-        chunk.sum,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=np.intp,
-        split_every=split_every,
-        out=None,
-    )
+        DataParams_reduction_27(a, _chunk_count, chunk.sum, axis = axis, keepdims = keepdims, dtype = np.intp, split_every = split_every, out = None))
 
 
 @derived_from(np.ma.core)
diff --git a/dask/array/reductions.py b/dask/array/reductions.py
index 0458f583b..cbf2f5f61 100644
--- a/dask/array/reductions.py
+++ b/dask/array/reductions.py
@@ -47,6 +47,22 @@ if da._array_expr_enabled():
     from dask.array._array_expr import _tree_reduce, reduction
 else:
     from dask.array._reductions_generic import _tree_reduce, reduction
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 def divide(a, b, dtype=None):
@@ -68,15 +84,7 @@ def sum(a, axis=None, dtype=None, keepdims=False, split_every=None, out=None):
     if dtype is None:
         dtype = getattr(np.zeros(1, dtype=a.dtype).sum(), "dtype", object)
     result = reduction(
-        a,
-        chunk.sum,
-        chunk.sum,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=dtype,
-        split_every=split_every,
-        out=out,
-    )
+        DataParams_reduction_27(a, chunk.sum, chunk.sum, axis = axis, keepdims = keepdims, dtype = dtype, split_every = split_every, out = out))
     return result
 
 
@@ -87,31 +95,14 @@ def prod(a, axis=None, dtype=None, keepdims=False, split_every=None, out=None):
     else:
         dt = getattr(np.ones((1,), dtype=a.dtype).prod(), "dtype", object)
     return reduction(
-        a,
-        chunk.prod,
-        chunk.prod,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=dt,
-        split_every=split_every,
-        out=out,
-    )
+        DataParams_reduction_27(a, chunk.prod, chunk.prod, axis = axis, keepdims = keepdims, dtype = dt, split_every = split_every, out = out))
 
 
 @implements(np.min, np.amin)
 @derived_from(np)
 def min(a, axis=None, keepdims=False, split_every=None, out=None):
     return reduction(
-        a,
-        chunk_min,
-        chunk.min,
-        combine=chunk_min,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=a.dtype,
-        split_every=split_every,
-        out=out,
-    )
+        DataParams_reduction_27(a, chunk_min, chunk.min, combine = chunk_min, axis = axis, keepdims = keepdims, dtype = a.dtype, split_every = split_every, out = out))
 
 
 def chunk_min(x, axis=None, keepdims=None):
@@ -126,16 +117,7 @@ def chunk_min(x, axis=None, keepdims=None):
 @derived_from(np)
 def max(a, axis=None, keepdims=False, split_every=None, out=None):
     return reduction(
-        a,
-        chunk_max,
-        chunk.max,
-        combine=chunk_max,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=a.dtype,
-        split_every=split_every,
-        out=out,
-    )
+        DataParams_reduction_27(a, chunk_max, chunk.max, combine = chunk_max, axis = axis, keepdims = keepdims, dtype = a.dtype, split_every = split_every, out = out))
 
 
 def chunk_max(x, axis=None, keepdims=None):
@@ -149,29 +131,13 @@ def chunk_max(x, axis=None, keepdims=None):
 @derived_from(np)
 def any(a, axis=None, keepdims=False, split_every=None, out=None):
     return reduction(
-        a,
-        chunk.any,
-        chunk.any,
-        axis=axis,
-        keepdims=keepdims,
-        dtype="bool",
-        split_every=split_every,
-        out=out,
-    )
+        DataParams_reduction_27(a, chunk.any, chunk.any, axis = axis, keepdims = keepdims, dtype = "bool", split_every = split_every, out = out))
 
 
 @derived_from(np)
 def all(a, axis=None, keepdims=False, split_every=None, out=None):
     return reduction(
-        a,
-        chunk.all,
-        chunk.all,
-        axis=axis,
-        keepdims=keepdims,
-        dtype="bool",
-        split_every=split_every,
-        out=out,
-    )
+        DataParams_reduction_27(a, chunk.all, chunk.all, axis = axis, keepdims = keepdims, dtype = "bool", split_every = split_every, out = out))
 
 
 @derived_from(np)
@@ -181,15 +147,7 @@ def nansum(a, axis=None, dtype=None, keepdims=False, split_every=None, out=None)
     else:
         dt = getattr(chunk.nansum(np.ones((1,), dtype=a.dtype)), "dtype", object)
     return reduction(
-        a,
-        chunk.nansum,
-        chunk.sum,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=dt,
-        split_every=split_every,
-        out=out,
-    )
+        DataParams_reduction_27(a, chunk.nansum, chunk.sum, axis = axis, keepdims = keepdims, dtype = dt, split_every = split_every, out = out))
 
 
 @derived_from(np)
@@ -199,15 +157,7 @@ def nanprod(a, axis=None, dtype=None, keepdims=False, split_every=None, out=None
     else:
         dt = getattr(chunk.nansum(np.ones((1,), dtype=a.dtype)), "dtype", object)
     return reduction(
-        a,
-        chunk.nanprod,
-        chunk.prod,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=dt,
-        split_every=split_every,
-        out=out,
-    )
+        DataParams_reduction_27(a, chunk.nanprod, chunk.prod, axis = axis, keepdims = keepdims, dtype = dt, split_every = split_every, out = out))
 
 
 @derived_from(np)
@@ -271,15 +221,7 @@ def nanmin(a, axis=None, keepdims=False, split_every=None, out=None):
             "zero-size array to reduction operation fmin which has no identity"
         )
     return reduction(
-        a,
-        _nanmin_skip,
-        _nanmin_skip,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=a.dtype,
-        split_every=split_every,
-        out=out,
-    )
+        DataParams_reduction_27(a, _nanmin_skip, _nanmin_skip, axis = axis, keepdims = keepdims, dtype = a.dtype, split_every = split_every, out = out))
 
 
 def _nanmin_skip(x_chunk, axis, keepdims):
@@ -304,15 +246,7 @@ def nanmax(a, axis=None, keepdims=False, split_every=None, out=None):
             "zero-size array to reduction operation fmax which has no identity"
         )
     return reduction(
-        a,
-        _nanmax_skip,
-        _nanmax_skip,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=a.dtype,
-        split_every=split_every,
-        out=out,
-    )
+        DataParams_reduction_27(a, _nanmax_skip, _nanmax_skip, axis = axis, keepdims = keepdims, dtype = a.dtype, split_every = split_every, out = out))
 
 
 def _nanmax_skip(x_chunk, axis, keepdims):
@@ -388,17 +322,7 @@ def mean(a, axis=None, dtype=None, keepdims=False, split_every=None, out=None):
     else:
         dt = getattr(np.mean(np.zeros(shape=(1,), dtype=a.dtype)), "dtype", object)
     return reduction(
-        a,
-        mean_chunk,
-        mean_agg,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=dt,
-        split_every=split_every,
-        combine=mean_combine,
-        out=out,
-        concatenate=False,
-    )
+        DataParams_reduction_27(a, mean_chunk, mean_agg, axis = axis, keepdims = keepdims, dtype = dt, split_every = split_every, combine = mean_combine, out = out, concatenate = False))
 
 
 @derived_from(np)
@@ -408,17 +332,7 @@ def nanmean(a, axis=None, dtype=None, keepdims=False, split_every=None, out=None
     else:
         dt = getattr(np.mean(np.ones(shape=(1,), dtype=a.dtype)), "dtype", object)
     return reduction(
-        a,
-        partial(mean_chunk, sum=chunk.nansum, numel=nannumel),
-        mean_agg,
-        axis=axis,
-        keepdims=keepdims,
-        dtype=dt,
-        split_every=split_every,
-        out=out,
-        concatenate=False,
-        combine=partial(mean_combine, sum=chunk.nansum, numel=nannumel),
-    )
+        DataParams_reduction_27(a, partial(mean_chunk, sum=chunk.nansum, numel=nannumel), mean_agg, axis = axis, keepdims = keepdims, dtype = dt, split_every = split_every, out = out, concatenate = False, combine = partial(mean_combine, sum=chunk.nansum, numel=nannumel)))
 
 
 def moment_chunk(
@@ -618,19 +532,9 @@ def moment(
     implicit_complex_dtype = dtype is None and np.iscomplexobj(a)
 
     return reduction(
-        a,
-        partial(
+        DataParams_reduction_27(a, partial(
             moment_chunk, order=order, implicit_complex_dtype=implicit_complex_dtype
-        ),
-        partial(moment_agg, order=order, ddof=ddof),
-        axis=axis,
-        keepdims=keepdims,
-        dtype=dt,
-        split_every=split_every,
-        out=out,
-        concatenate=False,
-        combine=partial(moment_combine, order=order),
-    )
+        ), partial(moment_agg, order=order, ddof=ddof), axis = axis, keepdims = keepdims, dtype = dt, split_every = split_every, out = out, concatenate = False, combine = partial(moment_combine, order=order)))
 
 
 @derived_from(np)
@@ -643,18 +547,7 @@ def var(a, axis=None, dtype=None, keepdims=False, ddof=0, split_every=None, out=
     implicit_complex_dtype = dtype is None and np.iscomplexobj(a)
 
     return reduction(
-        a,
-        partial(moment_chunk, implicit_complex_dtype=implicit_complex_dtype),
-        partial(moment_agg, ddof=ddof),
-        axis=axis,
-        keepdims=keepdims,
-        dtype=dt,
-        split_every=split_every,
-        combine=moment_combine,
-        name="var",
-        out=out,
-        concatenate=False,
-    )
+        DataParams_reduction_27(a, partial(moment_chunk, implicit_complex_dtype=implicit_complex_dtype), partial(moment_agg, ddof=ddof), axis = axis, keepdims = keepdims, dtype = dt, split_every = split_every, combine = moment_combine, name = "var", out = out, concatenate = False))
 
 
 @derived_from(np)
@@ -669,22 +562,12 @@ def nanvar(
     implicit_complex_dtype = dtype is None and np.iscomplexobj(a)
 
     return reduction(
-        a,
-        partial(
+        DataParams_reduction_27(a, partial(
             moment_chunk,
             sum=chunk.nansum,
             numel=nannumel,
             implicit_complex_dtype=implicit_complex_dtype,
-        ),
-        partial(moment_agg, sum=np.nansum, ddof=ddof),
-        axis=axis,
-        keepdims=keepdims,
-        dtype=dt,
-        split_every=split_every,
-        combine=partial(moment_combine, sum=np.nansum),
-        out=out,
-        concatenate=False,
-    )
+        ), partial(moment_agg, sum=np.nansum, ddof=ddof), axis = axis, keepdims = keepdims, dtype = dt, split_every = split_every, combine = partial(moment_combine, sum=np.nansum), out = out, concatenate = False))
 
 
 def _sqrt(a):
@@ -1371,16 +1254,7 @@ def topk(a, k, axis=-1, split_every=None):
     aggregate = partial(chunk.topk_aggregate, k=k)
 
     return reduction(
-        a,
-        chunk=chunk_combine,
-        combine=chunk_combine,
-        aggregate=aggregate,
-        axis=axis,
-        keepdims=True,
-        dtype=a.dtype,
-        split_every=split_every,
-        output_size=abs(k),
-    )
+        DataParams_reduction_27(a, chunk = chunk_combine, combine = chunk_combine, aggregate = aggregate, axis = axis, keepdims = True, dtype = a.dtype, split_every = split_every, output_size = abs(k)))
 
 
 def argtopk(a, k, axis=-1, split_every=None):
@@ -1441,18 +1315,7 @@ def argtopk(a, k, axis=-1, split_every=None):
     meta = a._meta.astype(np.intp).reshape((0,) * (a.ndim - naxis + 1))
 
     return reduction(
-        a_plus_idx,
-        chunk=chunk_combine,
-        combine=chunk_combine,
-        aggregate=aggregate,
-        axis=axis,
-        keepdims=True,
-        dtype=np.intp,
-        split_every=split_every,
-        concatenate=False,
-        output_size=abs(k),
-        meta=meta,
-    )
+        DataParams_reduction_27(a_plus_idx, chunk = chunk_combine, combine = chunk_combine, aggregate = aggregate, axis = axis, keepdims = True, dtype = np.intp, split_every = split_every, concatenate = False, output_size = abs(k), meta = meta))
 
 
 @derived_from(np)
diff --git a/dask/array/routines.py b/dask/array/routines.py
index b0f60e690..0ffdc5617 100644
--- a/dask/array/routines.py
+++ b/dask/array/routines.py
@@ -49,6 +49,22 @@ from dask.utils import apply, derived_from, funcname, is_arraylike, is_cupy_type
 
 # save built-in for histogram functions which use range as a kwarg.
 _range = range
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 @derived_from(np)
@@ -388,8 +404,7 @@ def _sum_wo_cat(a, axis=None, dtype=None):
         return a.squeeze(axis)
 
     return reduction(
-        a, _chunk_sum, _chunk_sum, axis=axis, dtype=dtype, concatenate=False
-    )
+        DataParams_reduction_27(a, _chunk_sum, _chunk_sum, axis = axis, dtype = dtype, concatenate = False))
 
 
 def _matmul(a, b):
@@ -2343,7 +2358,7 @@ def coarsen(reduction, x, axes, trim_excess=False, **kwargs):
         for i, bds in enumerate(x.chunks)
     )
 
-    meta = reduction(np.empty((1,) * x.ndim, dtype=x.dtype), **kwargs)
+    meta = reduction(DataParams_reduction_27(np.empty((1,) * x.ndim, dtype=x.dtype), kwargs))
     graph = HighLevelGraph.from_collections(name, dsk, dependencies=[x])
     return Array(graph, name, chunks, meta=meta)
 
diff --git a/dask/array/tests/test_reductions.py b/dask/array/tests/test_reductions.py
index e694e27d5..6a4f4f2c5 100644
--- a/dask/array/tests/test_reductions.py
+++ b/dask/array/tests/test_reductions.py
@@ -16,6 +16,22 @@ import dask.config as config
 from dask.array.numpy_compat import ComplexWarning
 from dask.array.utils import assert_eq, same_keys
 from dask.core import get_deps
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 @pytest.mark.parametrize("dtype", ["f4", "i4"])
@@ -591,8 +607,7 @@ def test_reduction_names():
 def test_general_reduction_names():
     dtype = int
     a = da.reduction(
-        da.ones(10, dtype, chunks=2), np.sum, np.sum, dtype=dtype, name="foo"
-    )
+        DataParams_reduction_27(da.ones(10, dtype, chunks=2), np.sum, np.sum, dtype = dtype, name = "foo"))
     names, tokens = list(zip_longest(*[key[0].rsplit("-", 1) for key in a.dask]))
     assert set(names) == {"ones_like", "foo", "foo-partial", "foo-aggregate"}
     assert all(tokens)
@@ -864,15 +879,7 @@ def test_chunk_structure_independence(axes, split_every, chunks):
     np_array = np.arange(np.prod(shape)).reshape(*shape)
     x = da.from_array(np_array, chunks=chunks)
     reduced_x = da.reduction(
-        x,
-        lambda x, axis, keepdims: x,
-        lambda x, axis, keepdims: x,
-        keepdims=True,
-        axis=axes,
-        split_every=split_every,
-        dtype=x.dtype,
-        meta=x._meta,
-    )
+        DataParams_reduction_27(x, lambda x, axis, keepdims: x, lambda x, axis, keepdims: x, keepdims = True, axis = axes, split_every = split_every, dtype = x.dtype, meta = x._meta))
     assert_eq(reduced_x, np_array, check_chunks=False, check_shape=False)
 
 
@@ -894,20 +901,20 @@ def test_weighted_reduction():
     w = np.linspace(1, 2, 6).reshape(6, 1)
 
     # No weights (i.e. normal sum)
-    x = da.reduction(dx, w_sum, np.sum, dtype=dx.dtype)
+    x = da.reduction(DataParams_reduction_27(dx, w_sum, np.sum, dtype = dx.dtype))
     assert_eq(x, np.sum(a), check_shape=True)
 
     # Weighted sum
-    x = da.reduction(dx, w_sum, np.sum, dtype="f8", weights=w)
+    x = da.reduction(DataParams_reduction_27(dx, w_sum, np.sum, dtype = "f8", weights = w))
     assert_eq(x, np.sum(a * w), check_shape=True)
 
     # Non-broadcastable weights (short axis)
     with pytest.raises(ValueError):
-        da.reduction(dx, w_sum, np.sum, weights=[1, 2, 3])
+        da.reduction(DataParams_reduction_27(dx, w_sum, np.sum, weights = [1, 2, 3]))
 
     # Non-broadcastable weights (too many dims)
     with pytest.raises(ValueError):
-        da.reduction(dx, w_sum, np.sum, weights=[[[2]]])
+        da.reduction(DataParams_reduction_27(dx, w_sum, np.sum, weights = [[[2]]]))
 
 
 def test_cumreduction_no_rechunk_on_1d_array():
diff --git a/dask/bag/core.py b/dask/bag/core.py
index aa54972b4..53a83beb6 100644
--- a/dask/bag/core.py
+++ b/dask/bag/core.py
@@ -84,6 +84,22 @@ DEFAULT_GET = named_schedulers.get("processes", named_schedulers["sync"])
 no_result = type(
     "no_result", (object,), {"__slots__": (), "__reduce__": lambda self: "no_result"}
 )
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 def lazify_task(task, start=True):
@@ -914,20 +930,12 @@ class Bag(DaskMethodsMixin):
         combine = combine or binop
         if initial is not no_default:
             return self.reduction(
-                curry(_reduce, binop, initial=initial),
-                curry(_reduce, combine),
-                split_every=split_every,
-                out_type=out_type,
-            )
+                DataParams_reduction_27(curry(_reduce, binop, initial=initial), curry(_reduce, combine), split_every = split_every), out_type = out_type)
         else:
             from tlz.curried import reduce
 
             return self.reduction(
-                reduce(binop),
-                reduce(combine),
-                split_every=split_every,
-                out_type=out_type,
-            )
+                DataParams_reduction_27(reduce(binop), reduce(combine), split_every = split_every), out_type = out_type)
 
     def frequencies(self, split_every=None, sort=False):
         """Count number of occurrences of each distinct element.
@@ -938,12 +946,7 @@ class Bag(DaskMethodsMixin):
         {'Alice': 2, 'Bob', 1}
         """
         result = self.reduction(
-            frequencies,
-            merge_frequencies,
-            out_type=Bag,
-            split_every=split_every,
-            name="frequencies",
-        ).map_partitions(dictitems)
+            DataParams_reduction_27(frequencies, merge_frequencies, split_every = split_every, name = "frequencies"), out_type = Bag).map_partitions(dictitems)
         if sort:
             result = result.map_partitions(sorted, key=second, reverse=True)
         return result
@@ -968,12 +971,7 @@ class Bag(DaskMethodsMixin):
         else:
             func = partial(topk, k)
         return self.reduction(
-            func,
-            compose(func, toolz.concat),
-            out_type=Bag,
-            split_every=split_every,
-            name="topk",
-        )
+            DataParams_reduction_27(func, compose(func, toolz.concat), split_every = split_every, name = "topk"), out_type = Bag)
 
     def distinct(self, key=None):
         """Distinct elements of collection
@@ -1000,7 +998,7 @@ class Bag(DaskMethodsMixin):
         """
         func = chunk_distinct if key is None else partial(chunk_distinct, key=key)
         agg = merge_distinct if key is None else partial(merge_distinct, key=key)
-        return self.reduction(func, agg, out_type=Bag, name="distinct")
+        return self.reduction(DataParams_reduction_27(func, agg, name = "distinct"), out_type = Bag)
 
     def reduction(
         self, perpartition, aggregate, split_every=None, out_type=Item, name=None
@@ -1074,15 +1072,15 @@ class Bag(DaskMethodsMixin):
 
     def sum(self, split_every=None):
         """Sum all elements"""
-        return self.reduction(sum, sum, split_every=split_every)
+        return self.reduction(DataParams_reduction_27(sum, sum, split_every = split_every))
 
     def max(self, split_every=None):
         """Maximum element"""
-        return self.reduction(max, max, split_every=split_every)
+        return self.reduction(DataParams_reduction_27(max, max, split_every = split_every))
 
     def min(self, split_every=None):
         """Minimum element"""
-        return self.reduction(min, min, split_every=split_every)
+        return self.reduction(DataParams_reduction_27(min, min, split_every = split_every))
 
     def any(self, split_every=None):
         """Are any of the elements truthy?
@@ -1094,7 +1092,7 @@ class Bag(DaskMethodsMixin):
         >>> bool_bag.any().compute()
         True
         """
-        return self.reduction(any, any, split_every=split_every)
+        return self.reduction(DataParams_reduction_27(any, any, split_every = split_every))
 
     def all(self, split_every=None):
         """Are all elements truthy?
@@ -1106,7 +1104,7 @@ class Bag(DaskMethodsMixin):
         >>> bool_bag.all().compute()
         False
         """
-        return self.reduction(all, all, split_every=split_every)
+        return self.reduction(DataParams_reduction_27(all, all, split_every = split_every))
 
     def count(self, split_every=None):
         """Count the number of elements.
@@ -1118,7 +1116,7 @@ class Bag(DaskMethodsMixin):
         >>> numbers.count().compute()
         3
         """
-        return self.reduction(count, sum, split_every=split_every)
+        return self.reduction(DataParams_reduction_27(count, sum, split_every = split_every))
 
     def mean(self):
         """Arithmetic mean"""
@@ -1134,13 +1132,12 @@ class Bag(DaskMethodsMixin):
             totals, counts = list(zip(*x))
             return 1.0 * sum(totals) / sum(counts)
 
-        return self.reduction(mean_chunk, mean_aggregate, split_every=False)
+        return self.reduction(DataParams_reduction_27(mean_chunk, mean_aggregate, split_every = False))
 
     def var(self, ddof=0):
         """Variance"""
         return self.reduction(
-            chunk.var_chunk, partial(chunk.var_aggregate, ddof=ddof), split_every=False
-        )
+            DataParams_reduction_27(chunk.var_chunk, partial(chunk.var_aggregate, ddof=ddof), split_every = False))
 
     def std(self, ddof=0):
         """Standard deviation"""
diff --git a/dask/bag/random.py b/dask/bag/random.py
index 96a44e1bc..efad349c5 100644
--- a/dask/bag/random.py
+++ b/dask/bag/random.py
@@ -7,6 +7,22 @@ from functools import partial
 from itertools import islice
 
 from dask.bag.core import Bag
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 def sample(population, k, split_every=None):
@@ -114,11 +130,7 @@ def _sample(population, k, split_every):
     if k < 0:
         raise ValueError("Cannot take a negative number of samples")
     return population.reduction(
-        partial(_sample_map_partitions, k=k),
-        partial(_sample_reduce, k=k, replace=False),
-        out_type=Bag,
-        split_every=split_every,
-    )
+        DataParams_reduction_27(partial(_sample_map_partitions, k=k), partial(_sample_reduce, k=k, replace=False), split_every = split_every), out_type = Bag)
 
 
 def _finalize_sample(reduce_iter, k):
@@ -155,11 +167,7 @@ def _sample_map_partitions(population, k):
 
 def _sample_with_replacement(population, k, split_every):
     return population.reduction(
-        partial(_sample_with_replacement_map_partitions, k=k),
-        partial(_sample_reduce, k=k, replace=True),
-        out_type=Bag,
-        split_every=split_every,
-    )
+        DataParams_reduction_27(partial(_sample_with_replacement_map_partitions, k=k), partial(_sample_reduce, k=k, replace=True), split_every = split_every), out_type = Bag)
 
 
 def _sample_with_replacement_map_partitions(population, k):
diff --git a/dask/bag/tests/test_bag.py b/dask/bag/tests/test_bag.py
index fca4ec583..208a36537 100644
--- a/dask/bag/tests/test_bag.py
+++ b/dask/bag/tests/test_bag.py
@@ -42,6 +42,22 @@ dsk: Graph = {("x", 0): (range, 5), ("x", 1): (range, 5), ("x", 2): (range, 5)}
 
 L = list(range(5)) * 3
 b = Bag(dsk, "x", 3)
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 def iseven(x):
@@ -378,18 +394,18 @@ def test_reductions():
 
 def test_reduction_names():
     assert b.sum().name.startswith("sum")
-    assert b.reduction(sum, sum).name.startswith("sum")
+    assert b.reduction(DataParams_reduction_27(sum, sum)).name.startswith("sum")
     assert any(
-        isinstance(k, str) and k.startswith("max") for k in b.reduction(sum, max).dask
+        isinstance(k, str) and k.startswith("max") for k in b.reduction(DataParams_reduction_27(sum, max)).dask
     )
-    assert b.reduction(sum, sum, name="foo").name.startswith("foo")
+    assert b.reduction(DataParams_reduction_27(sum, sum, name = "foo")).name.startswith("foo")
 
 
 def test_tree_reductions():
     b = db.from_sequence(range(12))
-    c = b.reduction(sum, sum, split_every=2)
-    d = b.reduction(sum, sum, split_every=6)
-    e = b.reduction(sum, sum, split_every=5)
+    c = b.reduction(DataParams_reduction_27(sum, sum, split_every = 2))
+    d = b.reduction(DataParams_reduction_27(sum, sum, split_every = 6))
+    e = b.reduction(DataParams_reduction_27(sum, sum, split_every = 5))
 
     assert c.compute() == d.compute() == e.compute()
 
@@ -1524,7 +1540,7 @@ def test_reductions_are_lazy():
 
     b = Bag({("foo", 0): part()}, "foo", 1)
 
-    res = b.reduction(func, sum)
+    res = b.reduction(DataParams_reduction_27(func, sum))
 
     assert_eq(res, sum(range(10)))
 
diff --git a/dask/dataframe/dask_expr/tests/test_reductions.py b/dask/dataframe/dask_expr/tests/test_reductions.py
index 744e648cb..1acf524c0 100644
--- a/dask/dataframe/dask_expr/tests/test_reductions.py
+++ b/dask/dataframe/dask_expr/tests/test_reductions.py
@@ -11,6 +11,22 @@ from dask.utils import M
 
 # Set DataFrame backend for this module
 pd = _backend_library()
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 @pytest.fixture
@@ -446,19 +462,19 @@ def test_reduction_method():
     agg = lambda x: x.sum()
 
     # Output of chunk is a scalar
-    res = ddf.x.reduction(chunk, aggregate=agg)
+    res = ddf.x.reduction(DataParams_reduction_27(chunk, aggregate = agg))
     assert_eq(res, df.x.count())
 
     # Output of chunk is a series
-    res = ddf.reduction(chunk, aggregate=agg)
-    assert res._name == ddf.reduction(chunk, aggregate=agg)._name
+    res = ddf.reduction(DataParams_reduction_27(chunk, aggregate = agg))
+    assert res._name == ddf.reduction(DataParams_reduction_27(chunk, aggregate = agg))._name
     assert_eq(res, df.count())
 
     # Test with keywords
-    res2 = ddf.reduction(chunk, aggregate=agg, chunk_kwargs={"val": 25})
+    res2 = ddf.reduction(DataParams_reduction_27(chunk, aggregate = agg), chunk_kwargs = {"val": 25})
     assert (
         res2._name
-        == ddf.reduction(chunk, aggregate=agg, chunk_kwargs={"val": 25})._name
+        == ddf.reduction(DataParams_reduction_27(chunk, aggregate = agg), chunk_kwargs = {"val": 25})._name
     )
     assert res2._name != res._name
     assert_eq(res2, (df >= 25).sum())
@@ -467,7 +483,7 @@ def test_reduction_method():
     def sum_and_count(x):
         return pd.DataFrame({"sum": x.sum(), "count": x.count()})
 
-    res = ddf.reduction(sum_and_count, aggregate=lambda x: x.groupby(level=0).sum())
+    res = ddf.reduction(DataParams_reduction_27(sum_and_count, aggregate = lambda x: x.groupby(level=0).sum()))
 
     assert_eq(res, pd.DataFrame({"sum": df.sum(), "count": df.count()}))
 
@@ -486,24 +502,16 @@ def test_reduction_method_split_every():
         return x.sum() + constant + 2
 
     f = lambda n: ddf.reduction(
-        chunk,
-        aggregate=agg,
-        combine=combine,
-        chunk_kwargs=dict(constant=1.0),
-        combine_kwargs=dict(constant=2.0),
-        aggregate_kwargs=dict(constant=3.0),
-        split_every=n,
-    )
+        DataParams_reduction_27(chunk, aggregate = agg, combine = combine, split_every = n), chunk_kwargs = dict(constant=1.0), combine_kwargs = dict(constant=2.0), aggregate_kwargs = dict(constant=3.0))
 
     # Keywords are different for each step
     assert f(3).compute() == 60 + 15 + 7 * (2 + 1) + (3 + 2)
     # Keywords are same for each step
     res = ddf.reduction(
-        chunk, aggregate=agg, combine=combine, constant=3.0, split_every=3
-    )
+        DataParams_reduction_27(chunk, aggregate = agg, combine = combine, split_every = 3), constant = 3.0)
     assert res.compute() == 60 + 15 * 3 + 7 * (3 + 1) + (3 + 2)
     # No combine provided, combine is agg
-    res = ddf.reduction(chunk, aggregate=agg, constant=3.0, split_every=3)
+    res = ddf.reduction(DataParams_reduction_27(chunk, aggregate = agg, split_every = 3), constant = 3.0)
     assert res.compute() == 60 + 15 * 3 + 8 * (3 + 2)
 
     # split_every must be >= 2
@@ -513,19 +521,13 @@ def test_reduction_method_split_every():
     # combine_kwargs with no combine provided
     with pytest.raises(ValueError):
         ddf.reduction(
-            chunk,
-            aggregate=agg,
-            split_every=3,
-            chunk_kwargs=dict(constant=1.0),
-            combine_kwargs=dict(constant=2.0),
-            aggregate_kwargs=dict(constant=3.0),
-        )
+            DataParams_reduction_27(chunk, aggregate = agg, split_every = 3), chunk_kwargs = dict(constant=1.0), combine_kwargs = dict(constant=2.0), aggregate_kwargs = dict(constant=3.0))
 
 
 def test_reduction_split_every_false():
     pdf = pd.DataFrame({"a": [1]})
     df = from_pandas(pdf, npartitions=1)
-    result = df.reduction(chunk=lambda x: x, split_every=False)
+    result = df.reduction(DataParams_reduction_27(chunk = lambda x: x, split_every = False))
     assert_eq(result, pdf)
 
 
diff --git a/dask/dataframe/tests/test_dataframe.py b/dask/dataframe/tests/test_dataframe.py
index 4503f43a0..c6541049c 100644
--- a/dask/dataframe/tests/test_dataframe.py
+++ b/dask/dataframe/tests/test_dataframe.py
@@ -61,6 +61,22 @@ meta = make_meta(
 )
 d = dd.repartition(pd.concat(dsk.values()), divisions=[0, 5, 9, 9])
 full = d.compute()
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 def _drop_mean(df, col=None):
@@ -2594,19 +2610,19 @@ def test_reduction_method():
     agg = lambda x: x.sum()
 
     # Output of chunk is a scalar
-    res = ddf.x.reduction(chunk, aggregate=agg)
+    res = ddf.x.reduction(DataParams_reduction_27(chunk, aggregate = agg))
     assert_eq(res, df.x.count())
 
     # Output of chunk is a series
-    res = ddf.reduction(chunk, aggregate=agg)
-    assert res._name == ddf.reduction(chunk, aggregate=agg)._name
+    res = ddf.reduction(DataParams_reduction_27(chunk, aggregate = agg))
+    assert res._name == ddf.reduction(DataParams_reduction_27(chunk, aggregate = agg))._name
     assert_eq(res, df.count())
 
     # Test with keywords
-    res2 = ddf.reduction(chunk, aggregate=agg, chunk_kwargs={"val": 25})
+    res2 = ddf.reduction(DataParams_reduction_27(chunk, aggregate = agg), chunk_kwargs = {"val": 25})
     assert (
         res2._name
-        == ddf.reduction(chunk, aggregate=agg, chunk_kwargs={"val": 25})._name
+        == ddf.reduction(DataParams_reduction_27(chunk, aggregate = agg), chunk_kwargs = {"val": 25})._name
     )
     assert res2._name != res._name
     assert_eq(res2, (df >= 25).sum())
@@ -2615,7 +2631,7 @@ def test_reduction_method():
     def sum_and_count(x):
         return pd.DataFrame({"sum": x.sum(), "count": x.count()})
 
-    res = ddf.reduction(sum_and_count, aggregate=lambda x: x.groupby(level=0).sum())
+    res = ddf.reduction(DataParams_reduction_27(sum_and_count, aggregate = lambda x: x.groupby(level=0).sum()))
 
     assert_eq(res, pd.DataFrame({"sum": df.sum(), "count": df.count()}))
 
@@ -2634,14 +2650,7 @@ def test_reduction_method_split_every():
         return x.sum() + constant + 2
 
     f = lambda n: ddf.reduction(
-        chunk,
-        aggregate=agg,
-        combine=combine,
-        chunk_kwargs=dict(constant=1.0),
-        combine_kwargs=dict(constant=2.0),
-        aggregate_kwargs=dict(constant=3.0),
-        split_every=n,
-    )
+        DataParams_reduction_27(chunk, aggregate = agg, combine = combine, split_every = n), chunk_kwargs = dict(constant=1.0), combine_kwargs = dict(constant=2.0), aggregate_kwargs = dict(constant=3.0))
 
     assert_max_deps(f(3), 3)
     assert_max_deps(f(4), 4, False)
@@ -2656,11 +2665,10 @@ def test_reduction_method_split_every():
     assert f(3).compute() == 60 + 15 + 7 * (2 + 1) + (3 + 2)
     # Keywords are same for each step
     res = ddf.reduction(
-        chunk, aggregate=agg, combine=combine, constant=3.0, split_every=3
-    )
+        DataParams_reduction_27(chunk, aggregate = agg, combine = combine, split_every = 3), constant = 3.0)
     assert res.compute() == 60 + 15 * 3 + 7 * (3 + 1) + (3 + 2)
     # No combine provided, combine is agg
-    res = ddf.reduction(chunk, aggregate=agg, constant=3.0, split_every=3)
+    res = ddf.reduction(DataParams_reduction_27(chunk, aggregate = agg, split_every = 3), constant = 3.0)
     assert res.compute() == 60 + 15 * 3 + 8 * (3 + 2)
 
     # split_every must be >= 2
@@ -2670,13 +2678,7 @@ def test_reduction_method_split_every():
     # combine_kwargs with no combine provided
     with pytest.raises(ValueError):
         ddf.reduction(
-            chunk,
-            aggregate=agg,
-            split_every=3,
-            chunk_kwargs=dict(constant=1.0),
-            combine_kwargs=dict(constant=2.0),
-            aggregate_kwargs=dict(constant=3.0),
-        )
+            DataParams_reduction_27(chunk, aggregate = agg, split_every = 3), chunk_kwargs = dict(constant=1.0), combine_kwargs = dict(constant=2.0), aggregate_kwargs = dict(constant=3.0))
 
 
 def test_pipe():
diff --git a/dask/tests/test_distributed.py b/dask/tests/test_distributed.py
index ec6beff23..5657228b3 100644
--- a/dask/tests/test_distributed.py
+++ b/dask/tests/test_distributed.py
@@ -58,6 +58,22 @@ ignore_sync_scheduler_warning = pytest.mark.filterwarnings(
     "ignore:Running on a single-machine scheduler when a distributed client "
     "is active might lead to unexpected results."
 )
+class DataParams_reduction_27:
+    def __init__(self, x, chunk, aggregate, axis = None, keepdims = False, dtype = None, split_every = None, combine = None, name = None, out = None, concatenate = True, output_size = 1, meta = None, weights = None):
+        self.x = x
+        self.chunk = chunk
+        self.aggregate = aggregate
+        self.axis = axis
+        self.keepdims = keepdims
+        self.dtype = dtype
+        self.split_every = split_every
+        self.combine = combine
+        self.name = name
+        self.out = out
+        self.concatenate = concatenate
+        self.output_size = output_size
+        self.meta = meta
+        self.weights = weights
 
 
 def test_can_import_client():
@@ -856,12 +872,7 @@ async def test_non_recursive_df_reduce(c, s, a, b):
     series = pd.Series(data=[1] * N, index=range(2, N + 2))
     dask_series = dd.from_pandas(series, npartitions=34)
     result = dask_series.reduction(
-        chunk=lambda x: x,
-        aggregate=lambda x: SomeObject(x.sum().sum()),
-        split_every=False,
-        token="commit-dataset",
-        meta=object,
-    )
+        DataParams_reduction_27(chunk = lambda x: x, aggregate = lambda x: SomeObject(x.sum().sum()), split_every = False, meta = object), token = "commit-dataset")
 
     assert (await c.compute(result)).val == 170
 
