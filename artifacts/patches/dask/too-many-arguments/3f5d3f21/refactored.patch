diff --git a/dask/bytes/tests/test_s3.py b/dask/bytes/tests/test_s3.py
index 8cf9b5e81..8e96ff3f8 100644
--- a/dask/bytes/tests/test_s3.py
+++ b/dask/bytes/tests/test_s3.py
@@ -49,6 +49,22 @@ files = {
         b'{"amount": 800, "name": "Dennis"}\n'
     ),
 }
+class DataParams_to_csv_769:
+    def __init__(self, df, filename, single_file = False, encoding = "utf-8", mode = "wt", name_function = None, compression = None, compute = True, scheduler = None, header_first_partition_only = None, compute_kwargs = None):
+        self.df = df
+        self.filename = filename
+        self.single_file = single_file
+        self.encoding = encoding
+        self.mode = mode
+        self.name_function = name_function
+        self.compression = compression
+        self.compute = compute
+        self.scheduler = scheduler
+        self.header_first_partition_only = header_first_partition_only
+        self.compute_kwargs = compute_kwargs
+class ConfigParams_to_csv_769:
+    def __init__(self, storage_options = None):
+        self.storage_options = storage_options
 
 
 @contextmanager
@@ -211,7 +227,7 @@ def s3_with_yellow_tripdata(s3):
     df = sample.take(np.arange(5).repeat(10000))
     file = io.BytesIO()
     sfile = io.TextIOWrapper(file)
-    df.to_csv(sfile, index=False)
+    df.to_csv(DataParams_to_csv_769(sfile), index = False)
 
     key = "nyc-taxi/2015/yellow_tripdata_2015-01.csv"
     client = boto3.client("s3", endpoint_url="http://127.0.0.1:5555/")
diff --git a/dask/dataframe/dask_expr/_collection.py b/dask/dataframe/dask_expr/_collection.py
index 0e16f3ed9..778d91c62 100644
--- a/dask/dataframe/dask_expr/_collection.py
+++ b/dask/dataframe/dask_expr/_collection.py
@@ -144,6 +144,22 @@ from dask.utils import (
     typename,
 )
 from dask.widgets import get_template
+class DataParams_to_csv_769:
+    def __init__(self, df, filename, single_file = False, encoding = "utf-8", mode = "wt", name_function = None, compression = None, compute = True, scheduler = None, header_first_partition_only = None, compute_kwargs = None):
+        self.df = df
+        self.filename = filename
+        self.single_file = single_file
+        self.encoding = encoding
+        self.mode = mode
+        self.name_function = name_function
+        self.compression = compression
+        self.compute = compute
+        self.scheduler = scheduler
+        self.header_first_partition_only = header_first_partition_only
+        self.compute_kwargs = compute_kwargs
+class ConfigParams_to_csv_769:
+    def __init__(self, storage_options = None):
+        self.storage_options = storage_options
 
 #
 # Utilities to wrap Expr API
@@ -2434,7 +2450,7 @@ Expr={expr}"""
         """See dd.to_csv docstring for more information"""
         from dask.dataframe.io.csv import to_csv
 
-        return to_csv(self, filename, **kwargs)
+        return to_csv(DataParams_to_csv_769(self, filename, kwargs))
 
     def to_records(self, index=False, lengths=None):
         from dask.dataframe.dask_expr.io.records import to_records
diff --git a/dask/dataframe/dask_expr/io/tests/test_io.py b/dask/dataframe/dask_expr/io/tests/test_io.py
index 3911a7338..29e997c71 100644
--- a/dask/dataframe/dask_expr/io/tests/test_io.py
+++ b/dask/dataframe/dask_expr/io/tests/test_io.py
@@ -29,6 +29,22 @@ from dask.dataframe.utils import assert_eq, make_meta, pyarrow_strings_enabled
 
 # Set DataFrame backend for this module
 pd = _backend_library()
+class DataParams_to_csv_769:
+    def __init__(self, df, filename, single_file = False, encoding = "utf-8", mode = "wt", name_function = None, compression = None, compute = True, scheduler = None, header_first_partition_only = None, compute_kwargs = None):
+        self.df = df
+        self.filename = filename
+        self.single_file = single_file
+        self.encoding = encoding
+        self.mode = mode
+        self.name_function = name_function
+        self.compression = compression
+        self.compute = compute
+        self.scheduler = scheduler
+        self.header_first_partition_only = header_first_partition_only
+        self.compute_kwargs = compute_kwargs
+class ConfigParams_to_csv_769:
+    def __init__(self, storage_options = None):
+        self.storage_options = storage_options
 
 
 def _make_file(dir, format="parquet", df=None):
@@ -36,7 +52,7 @@ def _make_file(dir, format="parquet", df=None):
     if df is None:
         df = pd.DataFrame({c: range(10) for c in "abcde"})
     if format == "csv":
-        df.to_csv(fn)
+        df.to_csv(DataParams_to_csv_769(fn))
     elif format == "parquet":
         df.to_parquet(fn)
     else:
@@ -191,7 +207,7 @@ def test_io_culling(tmpdir, fmt):
         dd.from_pandas(pdf, 2).to_parquet(tmpdir)
         df = read_parquet(tmpdir)
     elif fmt == "csv":
-        dd.from_pandas(pdf, 2).to_csv(tmpdir)
+        dd.from_pandas(pdf, 2).to_csv(DataParams_to_csv_769(tmpdir))
         df = read_csv(tmpdir + "/*")
     else:
         df = from_pandas(pdf, 2)
diff --git a/dask/dataframe/io/csv.py b/dask/dataframe/io/csv.py
index 4dc9ad8a0..037b2af05 100644
--- a/dask/dataframe/io/csv.py
+++ b/dask/dataframe/io/csv.py
@@ -7,6 +7,7 @@ from io import BytesIO
 from warnings import catch_warnings, simplefilter, warn
 
 from dask._task_spec import convert_legacy_task
+from codecarbon import track_emissions
 
 try:
     import psutil
@@ -35,6 +36,22 @@ from dask.dataframe.backends import dataframe_creation_dispatch
 from dask.dataframe.utils import clear_known_categories
 from dask.delayed import delayed
 from dask.utils import asciitable, parse_bytes
+class DataParams_to_csv_769:
+    def __init__(self, df, filename, single_file = False, encoding = "utf-8", mode = "wt", name_function = None, compression = None, compute = True, scheduler = None, header_first_partition_only = None, compute_kwargs = None):
+        self.df = df
+        self.filename = filename
+        self.single_file = single_file
+        self.encoding = encoding
+        self.mode = mode
+        self.name_function = name_function
+        self.compression = compression
+        self.compute = compute
+        self.scheduler = scheduler
+        self.header_first_partition_only = header_first_partition_only
+        self.compute_kwargs = compute_kwargs
+class ConfigParams_to_csv_769:
+    def __init__(self, storage_options = None):
+        self.storage_options = storage_options
 
 
 def pandas_read_text(
@@ -760,24 +777,13 @@ read_fwf = make_reader(pd.read_fwf, "read_fwf", "fixed-width")
 
 def _write_csv(df, fil, *, depend_on=None, **kwargs):
     with fil as f:
-        df.to_csv(f, **kwargs)
+        df.to_csv(DataParams_to_csv_769(f, kwargs))
     return os.path.normpath(fil.path)
 
 
+@track_emissions(project_name='dask-benchmark', experiment_id='dask_R0913_3f5d3f21', output_file='/root/ecooptimizer_benchmark/emissions/dask/too-many-arguments/3f5d3f21_refactored.csv')
 def to_csv(
-    df,
-    filename,
-    single_file=False,
-    encoding="utf-8",
-    mode="wt",
-    name_function=None,
-    compression=None,
-    compute=True,
-    scheduler=None,
-    storage_options=None,
-    header_first_partition_only=None,
-    compute_kwargs=None,
-    **kwargs,
+    data_params, config_params, **kwargs,
 ):
     """
     Store Dask DataFrame to CSV files
@@ -892,30 +898,30 @@ def to_csv(
     --------
     fsspec.open_files
     """
-    df = df.optimize()
+    data_params.df = data_params.df.optimize()
 
-    if single_file and name_function is not None:
+    if data_params.single_file and data_params.name_function is not None:
         raise ValueError("name_function is not supported under the single file mode")
-    if header_first_partition_only is None:
-        header_first_partition_only = single_file
-    elif not header_first_partition_only and single_file:
+    if data_params.header_first_partition_only is None:
+        data_params.header_first_partition_only = data_params.single_file
+    elif not data_params.header_first_partition_only and data_params.single_file:
         raise ValueError(
             "header_first_partition_only cannot be False in the single file mode."
         )
     file_options = dict(
-        compression=compression,
-        encoding=encoding,
+        data_params.compression=data_params.compression,
+        data_params.encoding=data_params.encoding,
         newline="",
-        **(storage_options or {}),
+        **(config_params.storage_options or {}),
     )
     to_csv_chunk = delayed(_write_csv, pure=False)
-    dfs = df.to_delayed()
-    if single_file:
-        first_file = open_file(filename, mode=mode, **file_options)
+    dfs = data_params.df.to_delayed()
+    if data_params.single_file:
+        first_file = open_file(data_params.filename, data_params.mode=data_params.mode, **file_options)
         value = to_csv_chunk(dfs[0], first_file, **kwargs)
-        append_mode = mode if "a" in mode else mode + "a"
+        append_mode = data_params.mode if "a" in data_params.mode else data_params.mode + "a"
         append_mode = append_mode.replace("w", "").replace("x", "")
-        append_file = open_file(filename, mode=append_mode, **file_options)
+        append_file = open_file(data_params.filename, data_params.mode=append_mode, **file_options)
         kwargs["header"] = False
         for d in dfs[1:]:
             value = to_csv_chunk(d, append_file, depend_on=value, **kwargs)
@@ -923,47 +929,47 @@ def to_csv(
         files = [first_file]
     else:
         files = open_files(
-            filename,
-            mode=mode,
-            name_function=name_function,
-            num=df.npartitions,
+            data_params.filename,
+            data_params.mode=data_params.mode,
+            data_params.name_function=data_params.name_function,
+            num=data_params.df.npartitions,
             **file_options,
         )
         values = [to_csv_chunk(dfs[0], files[0], **kwargs)]
-        if header_first_partition_only:
+        if data_params.header_first_partition_only:
             kwargs["header"] = False
         values.extend(
             [to_csv_chunk(d, f, **kwargs) for d, f in zip(dfs[1:], files[1:])]
         )
-    if compute:
-        if compute_kwargs is None:
-            compute_kwargs = dict()
+    if data_params.compute:
+        if data_params.compute_kwargs is None:
+            data_params.compute_kwargs = dict()
 
-        if scheduler is not None:
+        if data_params.scheduler is not None:
             warn(
                 "The 'scheduler' keyword argument for `to_csv()` is deprecated and"
                 "will be removed in a future version. "
                 "Please use the `compute_kwargs` argument instead. "
-                f"For example, df.to_csv(..., compute_kwargs={{scheduler: {scheduler}}})",
+                f"For example, df.to_csv(..., compute_kwargs={{scheduler: {data_params.scheduler}}})",
                 FutureWarning,
             )
 
         if (
-            scheduler is not None
-            and compute_kwargs.get("scheduler") is not None
-            and compute_kwargs.get("scheduler") != scheduler
+            data_params.scheduler is not None
+            and data_params.compute_kwargs.get("scheduler") is not None
+            and data_params.compute_kwargs.get("scheduler") != data_params.scheduler
         ):
             raise ValueError(
                 f"Differing values for 'scheduler' have been passed in.\n"
-                f"scheduler argument: {scheduler}\n"
-                f"via compute_kwargs: {compute_kwargs.get('scheduler')}"
+                f"scheduler argument: {data_params.scheduler}\n"
+                f"via compute_kwargs: {data_params.compute_kwargs.get('scheduler')}"
             )
 
-        if scheduler is not None and compute_kwargs.get("scheduler") is None:
-            compute_kwargs["scheduler"] = scheduler
+        if data_params.scheduler is not None and data_params.compute_kwargs.get("scheduler") is None:
+            data_params.compute_kwargs["scheduler"] = data_params.scheduler
 
         import dask
 
-        return list(dask.compute(*values, **compute_kwargs))
+        return list(dask.data_params.compute(*values, **data_params.compute_kwargs))
     else:
         return values
diff --git a/dask/dataframe/io/tests/test_csv.py b/dask/dataframe/io/tests/test_csv.py
index b1e340dd4..42dec88aa 100644
--- a/dask/dataframe/io/tests/test_csv.py
+++ b/dask/dataframe/io/tests/test_csv.py
@@ -40,6 +40,22 @@ from dask.utils import filetext, filetexts, tmpdir, tmpfile
 
 # List of available compression format for test_read_csv_compression
 compression_fmts = [fmt for fmt in compr] + [None]
+class DataParams_to_csv_769:
+    def __init__(self, df, filename, single_file = False, encoding = "utf-8", mode = "wt", name_function = None, compression = None, compute = True, scheduler = None, header_first_partition_only = None, compute_kwargs = None):
+        self.df = df
+        self.filename = filename
+        self.single_file = single_file
+        self.encoding = encoding
+        self.mode = mode
+        self.name_function = name_function
+        self.compression = compression
+        self.compute = compute
+        self.scheduler = scheduler
+        self.header_first_partition_only = header_first_partition_only
+        self.compute_kwargs = compute_kwargs
+class ConfigParams_to_csv_769:
+    def __init__(self, storage_options = None):
+        self.storage_options = storage_options
 
 
 def normalize_text(s):
@@ -1167,7 +1183,7 @@ def test_encoding_gh601(encoding):
     test_df = pd.DataFrame({"a": ar, "b": br, "c": cr, "d": dr})
 
     with tmpfile(".csv") as fn:
-        test_df.to_csv(fn, encoding=encoding, index=False)
+        test_df.to_csv(DataParams_to_csv_769(fn, encoding = encoding), index = False)
 
         a = pd.read_csv(fn, encoding=encoding)
         d = dd.read_csv(fn, encoding=encoding, blocksize=1000)
@@ -1359,12 +1375,12 @@ def test_to_csv():
     for npartitions in [1, 2]:
         a = dd.from_pandas(df, npartitions)
         with tmpdir() as dn:
-            a.to_csv(dn, index=False)
+            a.to_csv(DataParams_to_csv_769(dn), index = False)
             result = dd.read_csv(os.path.join(dn, "*")).compute().reset_index(drop=True)
             assert_eq(result, df)
 
         with tmpdir() as dn:
-            r = a.to_csv(dn, index=False, compute=False)
+            r = a.to_csv(DataParams_to_csv_769(dn, compute = False), index = False)
             paths = dask.compute(*r, scheduler="sync")
             # this is a tuple rather than a list since it's the output of dask.compute
             assert paths == tuple(
@@ -1375,7 +1391,7 @@ def test_to_csv():
 
         with tmpdir() as dn:
             fn = os.path.join(dn, "data_*.csv")
-            paths = a.to_csv(fn, index=False)
+            paths = a.to_csv(DataParams_to_csv_769(fn), index = False)
             assert paths == [
                 os.path.join(dn, f"data_{n}.csv") for n in range(npartitions)
             ]
@@ -1389,7 +1405,7 @@ def test_to_csv_multiple_files_cornercases():
     with tmpdir() as dn:
         with pytest.raises(ValueError):
             fn = os.path.join(dn, "data_*_*.csv")
-            a.to_csv(fn)
+            a.to_csv(DataParams_to_csv_769(fn))
 
     df16 = pd.DataFrame(
         {
@@ -1417,25 +1433,25 @@ def test_to_csv_multiple_files_cornercases():
     a = dd.from_pandas(df16, 16)
     with tmpdir() as dn:
         fn = os.path.join(dn, "data_*.csv")
-        a.to_csv(fn, index=False)
+        a.to_csv(DataParams_to_csv_769(fn), index = False)
         result = dd.read_csv(fn).compute().reset_index(drop=True)
         assert_eq(result, df16)
 
     # test handling existing files when links are optimized out
     a = dd.from_pandas(df, 2)
     with tmpdir() as dn:
-        a.to_csv(dn, index=False)
+        a.to_csv(DataParams_to_csv_769(dn), index = False)
         fn = os.path.join(dn, "data_*.csv")
-        a.to_csv(fn, mode="w", index=False)
+        a.to_csv(DataParams_to_csv_769(fn, mode = "w"), index = False)
         result = dd.read_csv(fn).compute().reset_index(drop=True)
         assert_eq(result, df)
 
     # test handling existing files when links are optimized out
     a = dd.from_pandas(df16, 16)
     with tmpdir() as dn:
-        a.to_csv(dn, index=False)
+        a.to_csv(DataParams_to_csv_769(dn), index = False)
         fn = os.path.join(dn, "data_*.csv")
-        a.to_csv(fn, mode="w", index=False)
+        a.to_csv(DataParams_to_csv_769(fn, mode = "w"), index = False)
         result = dd.read_csv(fn).compute().reset_index(drop=True)
         assert_eq(result, df16)
 
@@ -1447,13 +1463,13 @@ def test_to_single_csv():
         a = dd.from_pandas(df, npartitions)
         with tmpdir() as dn:
             fn = os.path.join(dn, "test.csv")
-            a.to_csv(fn, index=False, single_file=True)
+            a.to_csv(DataParams_to_csv_769(fn, single_file = True), index = False)
             result = dd.read_csv(fn).compute().reset_index(drop=True)
             assert_eq(result, df)
 
         with tmpdir() as dn:
             fn = os.path.join(dn, "test.csv")
-            r = a.to_csv(fn, index=False, compute=False, single_file=True)
+            r = a.to_csv(DataParams_to_csv_769(fn, compute = False, single_file = True), index = False)
             dask.compute(r, scheduler="sync")
             result = dd.read_csv(fn).compute().reset_index(drop=True)
             assert_eq(result, df)
@@ -1468,7 +1484,7 @@ def test_to_single_csv_with_name_function():
             ValueError,
             match="name_function is not supported under the single file mode",
         ):
-            a.to_csv(fn, name_function=lambda x: x, index=False, single_file=True)
+            a.to_csv(DataParams_to_csv_769(fn, name_function = lambda x: x, single_file = True), index = False)
 
 
 def test_to_single_csv_with_header_first_partition_only():
@@ -1481,8 +1497,7 @@ def test_to_single_csv_with_header_first_partition_only():
             match="header_first_partition_only cannot be False in the single file mode.",
         ):
             a.to_csv(
-                fn, index=False, header_first_partition_only=False, single_file=True
-            )
+                DataParams_to_csv_769(fn, header_first_partition_only = False, single_file = True), index = False)
 
 
 def test_to_csv_with_single_file_and_exclusive_mode():
@@ -1490,7 +1505,7 @@ def test_to_csv_with_single_file_and_exclusive_mode():
     df = dd.from_pandas(df0, npartitions=2)
     with tmpdir() as directory:
         csv_path = os.path.join(directory, "test.csv")
-        df.to_csv(csv_path, index=False, mode="x", single_file=True)
+        df.to_csv(DataParams_to_csv_769(csv_path, mode = "x", single_file = True), index = False)
         result = dd.read_csv(os.path.join(directory, "*")).compute()
     assert_eq(result, df0, check_index=False)
 
@@ -1500,13 +1515,13 @@ def test_to_csv_single_file_exlusive_mode_no_overwrite():
     df = dd.from_pandas(df0, npartitions=2)
     with tmpdir() as directory:
         csv_path = os.path.join(str(directory), "test.csv")
-        df.to_csv(csv_path, index=False, mode="x", single_file=True)
+        df.to_csv(DataParams_to_csv_769(csv_path, mode = "x", single_file = True), index = False)
         assert os.path.exists(csv_path)
         # mode="x" should fail if file already exists
         with pytest.raises(FileExistsError):
-            df.to_csv(csv_path, index=False, mode="x", single_file=True)
+            df.to_csv(DataParams_to_csv_769(csv_path, mode = "x", single_file = True), index = False)
         # but mode="w" will overwrite an existing file
-        df.to_csv(csv_path, index=False, mode="w", single_file=True)
+        df.to_csv(DataParams_to_csv_769(csv_path, mode = "w", single_file = True), index = False)
 
 
 def test_to_single_csv_gzip():
@@ -1516,7 +1531,7 @@ def test_to_single_csv_gzip():
         a = dd.from_pandas(df, npartitions)
         with tmpdir() as dn:
             fn = os.path.join(dn, "test.csv.gz")
-            a.to_csv(fn, index=False, compression="gzip", single_file=True)
+            a.to_csv(DataParams_to_csv_769(fn, compression = "gzip", single_file = True), index = False)
             result = pd.read_csv(fn, compression="gzip").reset_index(drop=True)
             assert_eq(result, df)
 
@@ -1530,7 +1545,7 @@ def test_to_csv_gzip():
     for npartitions in [1, 2]:
         a = dd.from_pandas(df, npartitions)
         with tmpfile("csv") as fn:
-            a.to_csv(fn, compression="gzip")
+            a.to_csv(DataParams_to_csv_769(fn, compression = "gzip"))
             result = pd.read_csv(fn, index_col=0, compression="gzip")
             tm.assert_frame_equal(result, df)
 
@@ -1547,7 +1562,7 @@ def test_to_csv_nodir():
     df = dd.from_pandas(df0, npartitions=2)
     with tmpdir() as dir:
         dir0 = os.path.join(str(dir), "createme")
-        df.to_csv(dir0)
+        df.to_csv(DataParams_to_csv_769(dir0))
         assert "createme" in os.listdir(dir)
         assert os.listdir(dir0)
         result = dd.read_csv(os.path.join(dir0, "*")).compute()
@@ -1561,7 +1576,7 @@ def test_to_csv_simple():
     df = dd.from_pandas(df0, npartitions=2)
     with tmpdir() as dir:
         dir = str(dir)
-        df.to_csv(dir)
+        df.to_csv(DataParams_to_csv_769(dir))
         assert os.listdir(dir)
         result = dd.read_csv(os.path.join(dir, "*")).compute()
     assert (result.x.values == df0.x.values).all()
@@ -1579,16 +1594,9 @@ def test_to_csv_with_single_file_and_append_mode():
     with tmpdir() as dir:
         csv_path = os.path.join(dir, "test.csv")
         df0.to_csv(
-            csv_path,
-            index=False,
-        )
+            DataParams_to_csv_769(csv_path), index = False)
         df.to_csv(
-            csv_path,
-            mode="a",
-            header=False,
-            index=False,
-            single_file=True,
-        )
+            DataParams_to_csv_769(csv_path, mode = "a", single_file = True), header = False, index = False)
         result = dd.read_csv(os.path.join(dir, "*")).compute()
     expected = pd.concat([df0, df1])
     assert assert_eq(result, expected, check_index=False)
@@ -1599,7 +1607,7 @@ def test_to_csv_series():
     df = dd.from_pandas(df0, npartitions=2)
     with tmpdir() as dir:
         dir = str(dir)
-        df.to_csv(dir, header=False)
+        df.to_csv(DataParams_to_csv_769(dir), header = False)
         assert os.listdir(dir)
         result = dd.read_csv(os.path.join(dir, "*"), header=None, names=["x"]).compute()
     assert (result.x == df0).all()
@@ -1618,7 +1626,7 @@ def test_to_csv_with_get():
     ddf = dd.from_pandas(df, npartitions=2)
 
     with tmpdir() as dn:
-        ddf.to_csv(dn, index=False, compute_kwargs={"scheduler": my_get})
+        ddf.to_csv(DataParams_to_csv_769(dn, compute_kwargs = {"scheduler": my_get}), index = False)
         assert flag[0]
         result = dd.read_csv(os.path.join(dn, "*"))
         assert_eq(result, df, check_index=False)
@@ -1635,7 +1643,7 @@ def test_to_csv_warns_using_scheduler_argument():
 
     with tmpdir() as dn:
         with pytest.warns(FutureWarning):
-            ddf.to_csv(dn, index=False, scheduler=my_get)
+            ddf.to_csv(DataParams_to_csv_769(dn, scheduler = my_get), index = False)
 
 
 def test_to_csv_errors_using_multiple_scheduler_args():
@@ -1650,8 +1658,7 @@ def test_to_csv_errors_using_multiple_scheduler_args():
     with tmpdir() as dn:
         with pytest.raises(ValueError) and pytest.warns(FutureWarning):
             ddf.to_csv(
-                dn, index=False, scheduler=my_get, compute_kwargs={"scheduler": my_get}
-            )
+                DataParams_to_csv_769(dn, scheduler = my_get, compute_kwargs = {"scheduler": my_get}), index = False)
 
 
 def test_to_csv_keeps_all_non_scheduler_compute_kwargs():
@@ -1666,16 +1673,13 @@ def test_to_csv_keeps_all_non_scheduler_compute_kwargs():
 
     with tmpdir() as dn:
         ddf.to_csv(
-            dn,
-            index=False,
-            compute_kwargs={"scheduler": my_get, "test_kwargs_passed": "foobar"},
-        )
+            DataParams_to_csv_769(dn, compute_kwargs = {"scheduler": my_get, "test_kwargs_passed": "foobar"}), index = False)
 
 
 def test_to_csv_paths():
     df = pd.DataFrame({"A": range(10)})
     ddf = dd.from_pandas(df, npartitions=2)
-    paths = ddf.to_csv("foo*.csv")
+    paths = ddf.to_csv(DataParams_to_csv_769("foo*.csv"))
     assert paths[0].endswith("foo0.csv")
     assert paths[1].endswith("foo1.csv")
 
@@ -1689,7 +1693,7 @@ def test_to_csv_header_empty_dataframe(header, expected):
     ddfe = dd.from_pandas(dfe, npartitions=1)
 
     with tmpdir() as dn:
-        ddfe.to_csv(os.path.join(dn, "fooe*.csv"), index=False, header=header)
+        ddfe.to_csv(DataParams_to_csv_769(os.path.join(dn, "fooe*.csv")), index = False, header = header)
         assert not os.path.exists(os.path.join(dn, "fooe1.csv"))
         filename = os.path.join(dn, "fooe0.csv")
         with open(filename) as fp:
@@ -1720,11 +1724,7 @@ def test_to_csv_header(
         # Test NO header case
         # (header=False, header_first_chunk_only not passed)
         ddf.to_csv(
-            os.path.join(dn, "fooa*.csv"),
-            index=False,
-            header=header,
-            header_first_partition_only=header_first_partition_only,
-        )
+            DataParams_to_csv_769(os.path.join(dn, "fooa*.csv"), header_first_partition_only = header_first_partition_only), index = False, header = header)
         filename = os.path.join(dn, "fooa0.csv")
         with open(filename) as fp:
             line = fp.readline()
@@ -1750,7 +1750,7 @@ def test_to_csv_line_ending():
     #  considers either '\r\n' or '\n' as appropriate line endings,
     #  but not '\r\r\n'.
     with tmpdir() as dn:
-        ddf.to_csv(os.path.join(dn, "foo*.csv"), header=False, index=False)
+        ddf.to_csv(DataParams_to_csv_769(os.path.join(dn, "foo*.csv")), header = False, index = False)
         filename = os.path.join(dn, "foo0.csv")
         with open(filename, "rb") as f:
             raw = f.read()
@@ -1799,7 +1799,7 @@ def test_read_csv_groupby_get_group(tmpdir):
 
     path = os.path.join(str(tmpdir), "test.csv")
     df1 = pd.DataFrame([{"foo": 10, "bar": 4}])
-    df1.to_csv(path, index=False)
+    df1.to_csv(DataParams_to_csv_769(path), index = False)
 
     ddf1 = dd.read_csv(path)
     ddfs = ddf1.groupby("foo")
@@ -1815,7 +1815,7 @@ def test_csv_getitem_column_order(tmpdir):
     values = list(range(len(columns)))
 
     df1 = pd.DataFrame([{c: v for c, v in zip(columns, values)}])
-    df1.to_csv(path)
+    df1.to_csv(DataParams_to_csv_769(path))
 
     # Use disordered and duplicated column selection
     columns = list("hczzkylaape")
@@ -1878,7 +1878,7 @@ def test_select_with_include_path_column(tmpdir):
 
     temp_path = str(tmpdir) + "/"
     for i in range(6):
-        df.to_csv(f"{temp_path}file_{i}.csv", index=False)
+        df.to_csv(DataParams_to_csv_769(f"{temp_path}file_{i}.csv"), index = False)
 
     ddf = dd.read_csv(temp_path + "*.csv", include_path_column=True)
 
@@ -1911,7 +1911,7 @@ def test_names_with_header_0(tmpdir, use_names):
         names = usecols = None
 
     path = os.path.join(str(tmpdir), "input.csv")
-    pd.read_csv(csv, header=None).to_csv(path, index=False, header=False)
+    pd.read_csv(csv, header=None).to_csv(DataParams_to_csv_769(path), index = False, header = False)
     df = pd.read_csv(path, header=0, names=names, usecols=usecols)
     ddf = dd.read_csv(
         path,
diff --git a/dask/dataframe/io/tests/test_hdf.py b/dask/dataframe/io/tests/test_hdf.py
index 3dd0d1e07..fd1dc246d 100644
--- a/dask/dataframe/io/tests/test_hdf.py
+++ b/dask/dataframe/io/tests/test_hdf.py
@@ -21,6 +21,22 @@ from dask.utils import tmpdir, tmpfile
 # there's no support in upstream for writing HDF with extension dtypes yet.
 # see https://github.com/pandas-dev/pandas/issues/31199
 pytestmark = pytest.mark.skip_with_pyarrow_strings  # no support for hdf yet
+class DataParams_to_csv_769:
+    def __init__(self, df, filename, single_file = False, encoding = "utf-8", mode = "wt", name_function = None, compression = None, compute = True, scheduler = None, header_first_partition_only = None, compute_kwargs = None):
+        self.df = df
+        self.filename = filename
+        self.single_file = single_file
+        self.encoding = encoding
+        self.mode = mode
+        self.name_function = name_function
+        self.compression = compression
+        self.compute = compute
+        self.scheduler = scheduler
+        self.header_first_partition_only = header_first_partition_only
+        self.compute_kwargs = compute_kwargs
+class ConfigParams_to_csv_769:
+    def __init__(self, storage_options = None):
+        self.storage_options = storage_options
 
 
 def test_to_hdf():
@@ -571,7 +587,7 @@ def test_to_fmt_warns():
 
     with tmpdir() as dn:
         fn = os.path.join(dn, "data_*.csv")
-        a.to_csv(fn, name_function=str)
+        a.to_csv(DataParams_to_csv_769(fn, name_function = str))
 
 
 @pytest.mark.parametrize(
diff --git a/dask/tests/test_distributed.py b/dask/tests/test_distributed.py
index ec6beff23..47a244062 100644
--- a/dask/tests/test_distributed.py
+++ b/dask/tests/test_distributed.py
@@ -58,6 +58,22 @@ ignore_sync_scheduler_warning = pytest.mark.filterwarnings(
     "ignore:Running on a single-machine scheduler when a distributed client "
     "is active might lead to unexpected results."
 )
+class DataParams_to_csv_769:
+    def __init__(self, df, filename, single_file = False, encoding = "utf-8", mode = "wt", name_function = None, compression = None, compute = True, scheduler = None, header_first_partition_only = None, compute_kwargs = None):
+        self.df = df
+        self.filename = filename
+        self.single_file = single_file
+        self.encoding = encoding
+        self.mode = mode
+        self.name_function = name_function
+        self.compression = compression
+        self.compute = compute
+        self.scheduler = scheduler
+        self.header_first_partition_only = header_first_partition_only
+        self.compute_kwargs = compute_kwargs
+class ConfigParams_to_csv_769:
+    def __init__(self, storage_options = None):
+        self.storage_options = storage_options
 
 
 def test_can_import_client():
@@ -543,7 +559,7 @@ def test_blockwise_dataframe_io(c, tmpdir, io, from_futures):
         ddf0.to_parquet(str(tmpdir))
         ddf = dd.read_parquet(str(tmpdir))
     elif io == "csv":
-        ddf0.to_csv(str(tmpdir), index=False)
+        ddf0.to_csv(DataParams_to_csv_769(str(tmpdir)), index = False)
         ddf = dd.read_csv(os.path.join(str(tmpdir), "*"))
     elif io == "hdf":
         pytest.importorskip("tables")
diff --git a/dask/tests/test_layers.py b/dask/tests/test_layers.py
index 80241958b..dc181183f 100644
--- a/dask/tests/test_layers.py
+++ b/dask/tests/test_layers.py
@@ -32,6 +32,22 @@ class SchedulerImportCheck(SchedulerPlugin):
             else:
                 # Manually remove the target library
                 sys.modules.pop(mod)
+class DataParams_to_csv_769:
+    def __init__(self, df, filename, single_file = False, encoding = "utf-8", mode = "wt", name_function = None, compression = None, compute = True, scheduler = None, header_first_partition_only = None, compute_kwargs = None):
+        self.df = df
+        self.filename = filename
+        self.single_file = single_file
+        self.encoding = encoding
+        self.mode = mode
+        self.name_function = name_function
+        self.compression = compression
+        self.compute = compute
+        self.scheduler = scheduler
+        self.header_first_partition_only = header_first_partition_only
+        self.compute_kwargs = compute_kwargs
+class ConfigParams_to_csv_769:
+    def __init__(self, storage_options = None):
+        self.storage_options = storage_options
 
 
 def test_array_chunk_shape_dep():
@@ -155,7 +171,7 @@ def _read_csv(tmpdir):
     dd.from_pandas(
         pd.DataFrame({"a": range(10)}),
         npartitions=2,
-    ).to_csv(str(tmpdir))
+    ).to_csv(DataParams_to_csv_769(str(tmpdir)))
     return dd.read_csv(os.path.join(str(tmpdir), "*"))
 
 
