diff --git a/dask/bag/core.py b/dask/bag/core.py
index aa54972b4..23349de80 100644
--- a/dask/bag/core.py
+++ b/dask/bag/core.py
@@ -1,3 +1,4 @@
+from codecarbon import track_emissions
 from __future__ import annotations
 
 import io
@@ -84,6 +85,18 @@ DEFAULT_GET = named_schedulers.get("processes", named_schedulers["sync"])
 no_result = type(
     "no_result", (object,), {"__slots__": (), "__reduce__": lambda self: "no_result"}
 )
+class DataParams_to_avro_842:
+    def __init__(self, filename, schema, name_function = None, codec = "null", sync_interval = 16000, metadata = None, compute = True):
+        self.filename = filename
+        self.schema = schema
+        self.name_function = name_function
+        self.codec = codec
+        self.sync_interval = sync_interval
+        self.metadata = metadata
+        self.compute = compute
+class ConfigParams_to_avro_842:
+    def __init__(self, storage_options = None):
+        self.storage_options = storage_options
 
 
 def lazify_task(task, start=True):
@@ -837,6 +850,7 @@ class Bag(DaskMethodsMixin):
         )
 
     @wraps(to_avro)
+    @track_emissions(project_name='dask-benchmark', experiment_id='dask_R0913_17fcee15', output_file='/root/ecooptimizer_benchmark/emissions/dask/too-many-arguments/17fcee15_refactored.csv')
     def to_avro(
         self,
         filename,
@@ -850,17 +864,7 @@ class Bag(DaskMethodsMixin):
         **kwargs,
     ):
         return to_avro(
-            self,
-            filename,
-            schema,
-            name_function,
-            storage_options,
-            codec,
-            sync_interval,
-            metadata,
-            compute,
-            **kwargs,
-        )
+            DataParams_to_avro_842(self, filename, schema, storage_options, codec, sync_interval, metadata), ConfigParams_to_avro_842(name_function), kwargs)
 
     def fold(
         self, binop, combine=None, initial=no_default, split_every=None, out_type=Item
diff --git a/dask/bag/tests/test_avro.py b/dask/bag/tests/test_avro.py
index 0c7f613c8..9d1fc3410 100644
--- a/dask/bag/tests/test_avro.py
+++ b/dask/bag/tests/test_avro.py
@@ -23,6 +23,18 @@ schema = {
     "type": "record",
     "fields": [{"name": "name", "type": "string"}, {"name": "number", "type": "int"}],
 }
+class DataParams_to_avro_842:
+    def __init__(self, filename, schema, name_function = None, codec = "null", sync_interval = 16000, metadata = None, compute = True):
+        self.filename = filename
+        self.schema = schema
+        self.name_function = name_function
+        self.codec = codec
+        self.sync_interval = sync_interval
+        self.metadata = metadata
+        self.compute = compute
+class ConfigParams_to_avro_842:
+    def __init__(self, storage_options = None):
+        self.storage_options = storage_options
 
 
 def test_onefile_oneblock(tmpdir):
@@ -76,9 +88,9 @@ def test_roundtrip_simple(tmpdir):
         "type": "record",
         "fields": [{"name": "a", "type": "int"}],
     }
-    out = b.to_avro(fn, schema, compute=False)
+    out = b.to_avro(DataParams_to_avro_842(fn, schema, compute = False))
     assert isinstance(out[0], Delayed)
-    out = b.to_avro(fn, schema)
+    out = b.to_avro(DataParams_to_avro_842(fn, schema))
     assert len(out) == 2
     b2 = db.read_avro(fn)
     assert b.compute() == b2.compute()
@@ -91,7 +103,7 @@ def test_roundtrip(tmpdir, codec):
         pytest.importorskip("snappy")
     fn = os.path.join(tmpdir, "out*.avro")
     b = db.from_sequence(expected, npartitions=3)
-    b.to_avro(fn, schema=schema, codec=codec)
+    b.to_avro(DataParams_to_avro_842(fn, schema = schema, codec = codec))
     b2 = db.read_avro(fn)
     assert b.compute() == b2.compute()
 
@@ -101,20 +113,19 @@ def test_invalid_schema(tmpdir):
     b = db.from_sequence(expected, npartitions=3)
     fn = os.path.join(tmpdir, "out*.avro")
     with pytest.raises(AssertionError):
-        b.to_avro(fn, schema=[])
+        b.to_avro(DataParams_to_avro_842(fn, schema = []))
     with pytest.raises(AssertionError):
-        b.to_avro(fn, schema={})
+        b.to_avro(DataParams_to_avro_842(fn, schema = {}))
     with pytest.raises(AssertionError):
-        b.to_avro(fn, schema={"doc": "unknown"})
+        b.to_avro(DataParams_to_avro_842(fn, schema = {"doc": "unknown"}))
     with pytest.raises(AssertionError):
-        b.to_avro(fn, schema={"name": "test"})
+        b.to_avro(DataParams_to_avro_842(fn, schema = {"name": "test"}))
     with pytest.raises(AssertionError):
-        b.to_avro(fn, schema={"name": "test", "type": "wrong"})
+        b.to_avro(DataParams_to_avro_842(fn, schema = {"name": "test", "type": "wrong"}))
     with pytest.raises(AssertionError):
-        b.to_avro(fn, schema={"name": "test", "type": "record"})
+        b.to_avro(DataParams_to_avro_842(fn, schema = {"name": "test", "type": "record"}))
     with pytest.raises(AssertionError):
-        b.to_avro(fn, schema={"name": "test", "type": "record"})
+        b.to_avro(DataParams_to_avro_842(fn, schema = {"name": "test", "type": "record"}))
     with pytest.raises(AssertionError):
         b.to_avro(
-            fn, schema={"name": "test", "type": "record", "fields": [{"name": "a"}]}
-        )
+            DataParams_to_avro_842(fn, schema = {"name": "test", "type": "record", "fields": [{"name": "a"}]}))
