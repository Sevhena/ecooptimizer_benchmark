diff --git a/gpiozero/devices.py b/gpiozero/devices.py
index a27936b..3d2f548 100644
--- a/gpiozero/devices.py
+++ b/gpiozero/devices.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 # vim: set fileencoding=utf-8:
 #
 # GPIO Zero: a library for controlling the Raspberry Pi's GPIO pins
@@ -455,8 +456,11 @@ class CompositeDevice(Device):
 
     def __repr__(self):
         try:
+            cached_len = len(self._named)
             self._check_open()
-            named = len(self._named)
+            with EmissionsTracker(project_name='gpiozero-benchmark', experiment_id='gpiozero_CRC001_69612c60', output_file='/root/ecooptimizer_benchmark/emissions/gpiozero/cached-repeated-calls/69612c60_refactored.csv') as tracker:
+                with EmissionsTracker(project_name='gpiozero-benchmark', experiment_id='gpiozero_CRC001_69612c60', output_file='/root/ecooptimizer_benchmark/emissions/gpiozero/cached-repeated-calls/69612c60_refactored.csv') as tracker:
+                    named = len(self._named)
             names = ', '.join(self._order)
             unnamed = len(self) - len(self._named)
             if named > 0 and unnamed > 0:
