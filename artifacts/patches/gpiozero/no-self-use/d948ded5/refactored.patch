diff --git a/gpiozero/pins/__init__.py b/gpiozero/pins/__init__.py
index 0fbc407..8986276 100644
--- a/gpiozero/pins/__init__.py
+++ b/gpiozero/pins/__init__.py
@@ -776,6 +776,7 @@ class SPI(Device):
             ----'   `---'   `---'   `---'   `---'   `---'   `---'   `-------
         """)
 
+    @track_emissions(project_name='gpiozero-benchmark', experiment_id='gpiozero_R6301_d948ded5', output_file='/root/ecooptimizer_benchmark/emissions/gpiozero/no-self-use/d948ded5_refactored.csv')
     def _get_bits_per_word(self):
         return 8  # pragma: no cover
 
@@ -784,7 +785,7 @@ class SPI(Device):
             f"bits_per_word cannot be changed on {self!r}")
 
     bits_per_word = property(
-        lambda self: self._get_bits_per_word(),
+        lambda self: SPI._get_bits_per_word(),
         lambda self, value: self._set_bits_per_word(value),
         doc="""\
         Controls the number of bits that make up a word, and thus where the
@@ -834,6 +835,7 @@ class PinInfo(namedtuple('PinInfo', (
 
         An integer containing the physical pin number on the header (starting
         from 1 in accordance with convention).
+from codecarbon import track_emissions
 
     .. attribute:: name
 
