diff --git a/scipy/io/arff/_arffread.py b/scipy/io/arff/_arffread.py
index 65495b8d9..067764211 100644
--- a/scipy/io/arff/_arffread.py
+++ b/scipy/io/arff/_arffread.py
@@ -6,6 +6,7 @@ import numpy as np
 
 import csv
 import ctypes
+from codecarbon import EmissionsTracker
 
 """A module to read arff files."""
 
@@ -688,11 +689,12 @@ class MetaData:
     def __repr__(self):
         msg = ""
         msg += f"Dataset: {self.name}\n"
-        for i in self._attributes:
-            msg += f"\t{i}'s type is {self._attributes[i].type_name}"
-            if self._attributes[i].range:
-                msg += f", range is {str(self._attributes[i].range)}"
-            msg += '\n'
+        with EmissionsTracker(project_name='scipy-benchmark', experiment_id='scipy_SCL001_cfbd3f39', output_file='/root/ecooptimizer_benchmark/emissions/scipy/string-concat-loop/cfbd3f39.csv') as tracker:
+            for i in self._attributes:
+                msg += f"\t{i}'s type is {self._attributes[i].type_name}"
+                if self._attributes[i].range:
+                    msg += f", range is {str(self._attributes[i].range)}"
+                msg += '\n'
         return msg
 
     def __iter__(self):
