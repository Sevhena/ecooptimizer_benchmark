diff --git a/scipy/_lib/_ccallback.py b/scipy/_lib/_ccallback.py
index 1980d06f5..c839f5380 100644
--- a/scipy/_lib/_ccallback.py
+++ b/scipy/_lib/_ccallback.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 from . import _ccallback_c
 
 import ctypes
@@ -193,12 +194,14 @@ def _get_ctypes_func(func, signature=None):
 
     # Construct function signature
     if signature is None:
-        signature = _typename_from_ctypes(func.restype) + " ("
-        for j, arg in enumerate(func.argtypes):
-            if j == 0:
-                signature += _typename_from_ctypes(arg)
-            else:
-                signature += ", " + _typename_from_ctypes(arg)
+        signature = [_typename_from_ctypes(func.restype) + ' (']
+        with EmissionsTracker(project_name='scipy-benchmark', experiment_id='scipy_SCL001_fa242649', output_file='/root/ecooptimizer_benchmark/emissions/scipy/string-concat-loop/fa242649_refactored.csv') as tracker:
+            for j, arg in enumerate(func.argtypes):
+                if j == 0:
+                    signature.append(_typename_from_ctypes(arg))
+                else:
+                    signature.append(', ' + _typename_from_ctypes(arg))
+            signature = ''.join(signature)
         signature += ")"
 
     return func_ptr, signature
@@ -248,4 +251,4 @@ def _get_cffi_func(func, signature=None):
 
 def _get_cffi_data(data):
     # Get pointer
-    return ffi.cast('uintptr_t', data)
+    return ffi.cast('uintptr_t', data)
\ No newline at end of file
