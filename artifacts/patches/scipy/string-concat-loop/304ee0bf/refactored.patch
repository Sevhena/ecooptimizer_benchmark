diff --git a/scipy/stats/_distribution_infrastructure.py b/scipy/stats/_distribution_infrastructure.py
index fca270f8f..6d9a8318d 100644
--- a/scipy/stats/_distribution_infrastructure.py
+++ b/scipy/stats/_distribution_infrastructure.py
@@ -17,6 +17,7 @@ from scipy.optimize._bracket import _bracket_root, _bracket_minimum
 from scipy.optimize._chandrupatla import _chandrupatla, _chandrupatla_minimize
 from scipy.stats._probability_distribution import _ProbabilityDistribution
 from scipy.stats import qmc
+from codecarbon import EmissionsTracker
 
 # in case we need to distinguish between None and not specified
 # Typically this is used to determine whether the tolerance has been set by the
@@ -5360,8 +5361,11 @@ class Mixture(_ProbabilityDistribution):
         result = "Mixture(\n"
         result += "    [\n"
         with np.printoptions(threshold=10):
-            for component in self.components:
-                result += f"        {repr(component)},\n"
+            with EmissionsTracker(project_name='scipy-benchmark', experiment_id='scipy_SCL001_304ee0bf', output_file='/root/ecooptimizer_benchmark/emissions/scipy/string-concat-loop/304ee0bf_refactored.csv') as tracker:
+                result = [result]
+                for component in self.components:
+                    result.append(f'        {repr(component)},\n')
+                result = ''.join(result)
             result += "    ],\n"
             result += f"    weights={repr(self.weights)},\n"
         result += ")"
@@ -5747,4 +5751,4 @@ def log(X, /):
                    "support is non-negative.")
         raise NotImplementedError(message)
     return MonotonicTransformedDistribution(X, g=np.log, h=np.exp, dh=np.exp,
-                                            logdh=lambda u: u)
+                                            logdh=lambda u: u)
\ No newline at end of file
