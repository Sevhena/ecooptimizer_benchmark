diff --git a/scipy/signal/_signaltools.py b/scipy/signal/_signaltools.py
index e35c04d1f..e3a84d75d 100644
--- a/scipy/signal/_signaltools.py
+++ b/scipy/signal/_signaltools.py
@@ -33,6 +33,7 @@ from scipy._lib._array_api import (
 from scipy._lib.array_api_compat import is_array_api_obj
 import scipy._lib.array_api_compat.numpy as np_compat
 import scipy._lib.array_api_extra as xpx
+from codecarbon import EmissionsTracker
 
 
 __all__ = ['correlate', 'correlation_lags', 'correlate2d',
@@ -1397,7 +1398,9 @@ def choose_conv_method(in1, in2, mode='full', measure=False):
     # for integer input,
     # catch when more precision required than float provides (representing an
     # integer as float can lose precision in fftconvolve if larger than 2**52)
-    if any([_numeric_arrays([x], kinds='ui', xp=xp) for x in [volume, kernel]]):
+    with EmissionsTracker(project_name='scipy-benchmark', experiment_id='scipy_R1729_06fcdecd', output_file='/root/ecooptimizer_benchmark/emissions/scipy/use-a-generator/06fcdecd_refactored.csv') as tracker:
+        _carbon_wrapped_76d5f93a = any(_numeric_arrays([x], kinds='ui', xp=xp) for x in [volume, kernel])
+    if _carbon_wrapped_76d5f93a:
         max_value = int(xp.max(xp.abs(volume))) * int(xp.max(xp.abs(kernel)))
         max_value *= int(min(xp_size(volume), xp_size(kernel)))
         if max_value > 2**np.finfo('float').nmant - 1:
