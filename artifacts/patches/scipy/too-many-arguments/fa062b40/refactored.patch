diff --git a/scipy/optimize/_lbfgsb_py.py b/scipy/optimize/_lbfgsb_py.py
index 2d1f608f8..f98faef93 100644
--- a/scipy/optimize/_lbfgsb_py.py
+++ b/scipy/optimize/_lbfgsb_py.py
@@ -1,3 +1,4 @@
+from codecarbon import track_emissions
 """
 Functions
 ---------
@@ -88,13 +89,25 @@ task_messages = {
     712 : "N <= 0",
     713 : "INVALID NBD",
 }
-
-def fmin_l_bfgs_b(func, x0, fprime=None, args=(),
-                  approx_grad=0,
-                  bounds=None, m=10, factr=1e7, pgtol=1e-5,
-                  epsilon=1e-8,
-                  iprint=-1, maxfun=15000, maxiter=15000, disp=None,
-                  callback=None, maxls=20):
+class DataParams_fmin_l_bfgs_b_94:
+    def __init__(self, func, x0, fprime = None, args = (), approx_grad = 0, bounds = None, m = 10, factr = 1e7, pgtol = 1e-5, epsilon = 1e-8, maxfun = 15000, maxiter = 15000, callback = None, maxls = 20):
+        self.func = func
+        self.x0 = x0
+        self.fprime = fprime
+        self.args = args
+        self.approx_grad = approx_grad
+        self.bounds = bounds
+        self.m = m
+        self.factr = factr
+        self.pgtol = pgtol
+        self.epsilon = epsilon
+        self.maxfun = maxfun
+        self.maxiter = maxiter
+        self.callback = callback
+        self.maxls = maxls
+
+@track_emissions(project_name='scipy-benchmark', experiment_id='scipy_R0913_fa062b40', output_file='/root/ecooptimizer_benchmark/emissions/scipy/too-many-arguments/fa062b40_refactored.csv')
+def fmin_l_bfgs_b(data_params):
     """
     Minimize a function func using the L-BFGS-B algorithm.
 
@@ -253,28 +266,28 @@ def fmin_l_bfgs_b(func, x0, fprime=None, args=(),
     array([1.65990508, 5.31649385]), 15.721334516453945  # may vary
     """
     # handle fprime/approx_grad
-    if approx_grad:
-        fun = func
+    if data_params.approx_grad:
+        fun = data_params.func
         jac = None
-    elif fprime is None:
-        fun = MemoizeJac(func)
+    elif data_params.fprime is None:
+        fun = MemoizeJac(data_params.func)
         jac = fun.derivative
     else:
-        fun = func
-        jac = fprime
+        fun = data_params.func
+        jac = data_params.fprime
 
     # build options
-    callback = _wrap_callback(callback)
-    opts = {'maxcor': m,
-            'ftol': factr * np.finfo(float).eps,
-            'gtol': pgtol,
-            'eps': epsilon,
-            'maxfun': maxfun,
-            'maxiter': maxiter,
-            'callback': callback,
-            'maxls': maxls}
-
-    res = _minimize_lbfgsb(fun, x0, args=args, jac=jac, bounds=bounds,
+    data_params.callback = _wrap_callback(data_params.callback)
+    opts = {'maxcor': data_params.m,
+            'ftol': data_params.factr * np.finfo(float).eps,
+            'gtol': data_params.pgtol,
+            'eps': data_params.epsilon,
+            'maxfun': data_params.maxfun,
+            'maxiter': data_params.maxiter,
+            'callback': data_params.callback,
+            'maxls': data_params.maxls}
+
+    res = _minimize_lbfgsb(fun, data_params.x0, data_params.args=data_params.args, jac=jac, data_params.bounds=data_params.bounds,
                            **opts)
     d = {'grad': res['jac'],
          'task': res['message'],
diff --git a/scipy/optimize/tests/test_optimize.py b/scipy/optimize/tests/test_optimize.py
index 0a51dc290..bc8bddc84 100644
--- a/scipy/optimize/tests/test_optimize.py
+++ b/scipy/optimize/tests/test_optimize.py
@@ -39,6 +39,22 @@ from scipy._lib._array_api import make_xp_test_case
 from scipy._lib._util import MapWrapper
 
 lazy_xp_modules = [optimize]
+class DataParams_fmin_l_bfgs_b_94:
+    def __init__(self, func, x0, fprime = None, args = (), approx_grad = 0, bounds = None, m = 10, factr = 1e7, pgtol = 1e-5, epsilon = 1e-8, maxfun = 15000, maxiter = 15000, callback = None, maxls = 20):
+        self.func = func
+        self.x0 = x0
+        self.fprime = fprime
+        self.args = args
+        self.approx_grad = approx_grad
+        self.bounds = bounds
+        self.m = m
+        self.factr = factr
+        self.pgtol = pgtol
+        self.epsilon = epsilon
+        self.maxfun = maxfun
+        self.maxiter = maxiter
+        self.callback = callback
+        self.maxls = maxls
 
 
 def test_check_grad():
@@ -1053,9 +1069,7 @@ class TestOptimizeSimple(CheckOptimize):
 
     def test_l_bfgs_b(self):
         # limited-memory bound-constrained BFGS algorithm
-        retval = optimize.fmin_l_bfgs_b(self.func, self.startparams,
-                                        self.grad, args=(),
-                                        maxiter=self.maxiter)
+        retval = optimize.fmin_l_bfgs_b(DataParams_fmin_l_bfgs_b_94(self.func, self.startparams, self.grad, args = (), maxiter = self.maxiter))
 
         (params, fopt, d) = retval
 
@@ -1076,9 +1090,7 @@ class TestOptimizeSimple(CheckOptimize):
 
     def test_l_bfgs_b_numjac(self):
         # L-BFGS-B with numerical Jacobian
-        retval = optimize.fmin_l_bfgs_b(self.func, self.startparams,
-                                        approx_grad=True,
-                                        maxiter=self.maxiter)
+        retval = optimize.fmin_l_bfgs_b(DataParams_fmin_l_bfgs_b_94(self.func, self.startparams, approx_grad = True, maxiter = self.maxiter))
 
         (params, fopt, d) = retval
 
@@ -1090,8 +1102,7 @@ class TestOptimizeSimple(CheckOptimize):
         def fun(x):
             return self.func(x), self.grad(x)
 
-        retval = optimize.fmin_l_bfgs_b(fun, self.startparams,
-                                        maxiter=self.maxiter)
+        retval = optimize.fmin_l_bfgs_b(DataParams_fmin_l_bfgs_b_94(fun, self.startparams, maxiter = self.maxiter))
 
         (params, fopt, d) = retval
 
@@ -1192,14 +1203,14 @@ class TestOptimizeSimple(CheckOptimize):
         # Request a maxfun that stops at a particularly bad function
         # evaluation somewhere between 100 and 300 evaluations.
         low, medium, high = 30, 100, 300
-        optimize.fmin_l_bfgs_b(objfun, x0, fprime=g, maxfun=high)
+        optimize.fmin_l_bfgs_b(DataParams_fmin_l_bfgs_b_94(objfun, x0, fprime = g, maxfun = high))
         v, k = max((y, i) for i, y in enumerate(values[medium:]))
         maxfun = medium + k
         # If the minimization strategy is reasonable,
         # the minimize() result should not be worse than the best
         # of the first 30 function evaluations.
         target = min(values[:low])
-        xmin, fmin, d = optimize.fmin_l_bfgs_b(f, x0, fprime=g, maxfun=maxfun)
+        xmin, fmin, d = optimize.fmin_l_bfgs_b(DataParams_fmin_l_bfgs_b_94(f, x0, fprime = g, maxfun = maxfun))
         assert_array_less(fmin, target)
 
     def test_custom(self):
@@ -1784,16 +1795,13 @@ class TestLBFGSBBounds:
         return self.fun(x, p), self.jac(x, p)
 
     def test_l_bfgs_b_bounds(self):
-        x, f, d = optimize.fmin_l_bfgs_b(self.fun, [0, -1],
-                                         fprime=self.jac,
-                                         bounds=self.bounds)
+        x, f, d = optimize.fmin_l_bfgs_b(DataParams_fmin_l_bfgs_b_94(self.fun, [0, -1], fprime = self.jac, bounds = self.bounds))
         assert d['warnflag'] == 0, d['task']
         assert_allclose(x, self.solution, atol=1e-6)
 
     def test_l_bfgs_b_funjac(self):
         # L-BFGS-B with fun and jac combined and extra arguments
-        x, f, d = optimize.fmin_l_bfgs_b(self.fj, [0, -1], args=(2.0, ),
-                                         bounds=self.bounds)
+        x, f, d = optimize.fmin_l_bfgs_b(DataParams_fmin_l_bfgs_b_94(self.fj, [0, -1], args = (2.0, ), bounds = self.bounds))
         assert d['warnflag'] == 0, d['task']
         assert_allclose(x, self.solution, atol=1e-6)
 
