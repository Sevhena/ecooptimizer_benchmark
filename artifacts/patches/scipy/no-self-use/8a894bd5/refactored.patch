diff --git a/scipy/stats/_distn_infrastructure.py b/scipy/stats/_distn_infrastructure.py
index 82cb1e343..191fcd481 100644
--- a/scipy/stats/_distn_infrastructure.py
+++ b/scipy/stats/_distn_infrastructure.py
@@ -1,4 +1,5 @@
 #
+from codecarbon import track_emissions
 # Author:  Travis Oliphant  2002-2011 with contributions from
 #          SciPy Developers 2004-2011
 #
@@ -991,7 +992,9 @@ class rv_generic:
     # These are the methods you must define (standard form functions)
     # NB: generic _pdf, _logpdf, _cdf are different for
     # rv_continuous and rv_discrete hence are defined in there
-    def _argcheck(self, *args):
+    @track_emissions(project_name='scipy-benchmark', experiment_id='scipy_R6301_8a894bd5', output_file='/root/ecooptimizer_benchmark/emissions/scipy/no-self-use/8a894bd5_refactored.csv')
+    @staticmethod
+    def _argcheck(*args):
         """Default check for correct values on args and keywords.
 
         Returns condition array of 1's where arguments are correct and
@@ -1098,7 +1101,7 @@ class rv_generic:
         discrete = kwds.pop('discrete', None)
         rndm = kwds.pop('random_state', None)
         args, loc, scale, size = self._parse_args_rvs(*args, **kwds)
-        cond = logical_and(self._argcheck(*args), (scale >= 0))
+        cond = logical_and(rv_generic._argcheck(*args), (scale >= 0))
         if not np.all(cond):
             message = ("Domain error in arguments. The `scale` parameter must "
                        "be positive for all distributions, and many "
@@ -1164,7 +1167,7 @@ class rv_generic:
         # scale = 1 by construction for discrete RVs
         loc, scale = map(asarray, (loc, scale))
         args = tuple(map(asarray, args))
-        cond = self._argcheck(*args) & (scale > 0) & (loc == loc)
+        cond = rv_generic._argcheck(*args) & (scale > 0) & (loc == loc)
         output = []
         default = np.full(shape(cond), fill_value=self.badvalue)
 
@@ -1275,7 +1278,7 @@ class rv_generic:
         # NB: for discrete distributions scale=1 by construction in _parse_args
         loc, scale = map(asarray, (loc, scale))
         args = tuple(map(asarray, args))
-        cond0 = self._argcheck(*args) & (scale > 0) & (loc == loc)
+        cond0 = rv_generic._argcheck(*args) & (scale > 0) & (loc == loc)
         output = zeros(shape(cond0), 'd')
         place(output, (1-cond0), self.badvalue)
         goodargs = argsreduce(cond0, scale, *args)
@@ -1305,7 +1308,7 @@ class rv_generic:
         args = np.broadcast_arrays(*(*shapes, loc, scale))
         *shapes, loc, scale = args
 
-        i0 = np.logical_and(self._argcheck(*shapes), scale > 0)
+        i0 = np.logical_and(rv_generic._argcheck(*shapes), scale > 0)
         i1 = np.logical_and(i0, loc == 0)
         i2 = np.logical_and(i0, loc != 0)
 
@@ -1528,7 +1531,7 @@ class rv_generic:
         args, loc, scale = self._parse_args(*args, **kwargs)
         arrs = np.broadcast_arrays(*args, loc, scale)
         args, loc, scale = arrs[:-2], arrs[-2], arrs[-1]
-        cond = self._argcheck(*args) & (scale > 0)
+        cond = rv_generic._argcheck(*args) & (scale > 0)
         _a, _b = self._get_support(*args)
         if cond.all():
             return _a * scale + loc, _b * scale + loc
@@ -1549,7 +1552,7 @@ class rv_generic:
         parameters (including loc and scale).
         """
         loc, scale, args = self._unpack_loc_scale(theta)
-        if not self._argcheck(*args) or scale <= 0:
+        if not rv_generic._argcheck(*args) or scale <= 0:
             return inf
         x = (asarray(x)-loc) / scale
         n_log_scale = len(x) * log(scale)
@@ -1580,7 +1583,7 @@ class rv_generic:
         where theta are the parameters (including loc and scale)
         """
         loc, scale, args = self._unpack_loc_scale(theta)
-        if not self._argcheck(*args) or scale <= 0:
+        if not rv_generic._argcheck(*args) or scale <= 0:
             return inf
         x = asarray((x-loc) / scale)
         n_log_scale = len(x) * log(scale)
@@ -1593,7 +1596,7 @@ class rv_generic:
         Follows reference [1] of scipy.stats.fit
         """
         loc, scale, args = self._unpack_loc_scale(theta)
-        if not self._argcheck(*args) or scale <= 0:
+        if not rv_generic._argcheck(*args) or scale <= 0:
             return inf
         x = (np.sort(x) - loc)/scale
 
@@ -2063,7 +2066,7 @@ class rv_continuous(rv_generic):
         args = tuple(map(asarray, args))
         dtyp = np.promote_types(x.dtype, np.float64)
         x = np.asarray((x - loc)/scale, dtype=dtyp)
-        cond0 = self._argcheck(*args) & (scale > 0)
+        cond0 = rv_continuous._argcheck(*args) & (scale > 0)
         cond1 = self._support_mask(x, *args) & (scale > 0)
         cond = cond0 & cond1
         output = zeros(shape(cond), dtyp)
@@ -2104,7 +2107,7 @@ class rv_continuous(rv_generic):
         args = tuple(map(asarray, args))
         dtyp = np.promote_types(x.dtype, np.float64)
         x = np.asarray((x - loc)/scale, dtype=dtyp)
-        cond0 = self._argcheck(*args) & (scale > 0)
+        cond0 = rv_continuous._argcheck(*args) & (scale > 0)
         cond1 = self._support_mask(x, *args) & (scale > 0)
         cond = cond0 & cond1
         output = empty(shape(cond), dtyp)
@@ -2146,7 +2149,7 @@ class rv_continuous(rv_generic):
         _a, _b = self._get_support(*args)
         dtyp = np.promote_types(x.dtype, np.float64)
         x = np.asarray((x - loc)/scale, dtype=dtyp)
-        cond0 = self._argcheck(*args) & (scale > 0)
+        cond0 = rv_continuous._argcheck(*args) & (scale > 0)
         cond1 = self._open_support_mask(x, *args) & (scale > 0)
         cond2 = (x >= np.asarray(_b)) & cond0
         cond = cond0 & cond1
@@ -2187,7 +2190,7 @@ class rv_continuous(rv_generic):
         _a, _b = self._get_support(*args)
         dtyp = np.promote_types(x.dtype, np.float64)
         x = np.asarray((x - loc)/scale, dtype=dtyp)
-        cond0 = self._argcheck(*args) & (scale > 0)
+        cond0 = rv_continuous._argcheck(*args) & (scale > 0)
         cond1 = self._open_support_mask(x, *args) & (scale > 0)
         cond2 = (x >= _b) & cond0
         cond = cond0 & cond1
@@ -2229,7 +2232,7 @@ class rv_continuous(rv_generic):
         _a, _b = self._get_support(*args)
         dtyp = np.promote_types(x.dtype, np.float64)
         x = np.asarray((x - loc)/scale, dtype=dtyp)
-        cond0 = self._argcheck(*args) & (scale > 0)
+        cond0 = rv_continuous._argcheck(*args) & (scale > 0)
         cond1 = self._open_support_mask(x, *args) & (scale > 0)
         cond2 = cond0 & (x <= _a)
         cond = cond0 & cond1
@@ -2273,7 +2276,7 @@ class rv_continuous(rv_generic):
         _a, _b = self._get_support(*args)
         dtyp = np.promote_types(x.dtype, np.float64)
         x = np.asarray((x - loc)/scale, dtype=dtyp)
-        cond0 = self._argcheck(*args) & (scale > 0)
+        cond0 = rv_continuous._argcheck(*args) & (scale > 0)
         cond1 = self._open_support_mask(x, *args) & (scale > 0)
         cond2 = cond0 & (x <= _a)
         cond = cond0 & cond1
@@ -2313,7 +2316,7 @@ class rv_continuous(rv_generic):
         q, loc, scale = map(asarray, (q, loc, scale))
         args = tuple(map(asarray, args))
         _a, _b = self._get_support(*args)
-        cond0 = self._argcheck(*args) & (scale > 0) & (loc == loc)
+        cond0 = rv_continuous._argcheck(*args) & (scale > 0) & (loc == loc)
         cond1 = (0 < q) & (q < 1)
         cond2 = cond0 & (q == 0)
         cond3 = cond0 & (q == 1)
@@ -2358,7 +2361,7 @@ class rv_continuous(rv_generic):
         q, loc, scale = map(asarray, (q, loc, scale))
         args = tuple(map(asarray, args))
         _a, _b = self._get_support(*args)
-        cond0 = self._argcheck(*args) & (scale > 0) & (loc == loc)
+        cond0 = rv_continuous._argcheck(*args) & (scale > 0) & (loc == loc)
         cond1 = (0 < q) & (q < 1)
         cond2 = cond0 & (q == 1)
         cond3 = cond0 & (q == 0)
@@ -2430,7 +2433,7 @@ class rv_continuous(rv_generic):
         where theta are the parameters (including loc and scale)
         """
         loc, scale, args = self._unpack_loc_scale(theta)
-        if not self._argcheck(*args) or scale <= 0:
+        if not rv_continuous._argcheck(*args) or scale <= 0:
             return inf
         if isinstance(x, CensoredData):
             x = (x - loc) / scale
@@ -2522,7 +2525,7 @@ class rv_continuous(rv_generic):
 
     def _moment_error(self, theta, x, data_moments):
         loc, scale, args = self._unpack_loc_scale(theta)
-        if not self._argcheck(*args) or scale <= 0:
+        if not rv_continuous._argcheck(*args) or scale <= 0:
             return inf
 
         dist_moments = np.array([self.moment(i+1, *args, loc=loc, scale=scale)
@@ -2764,7 +2767,7 @@ class rv_continuous(rv_generic):
         vals = tuple(vals)
 
         loc, scale, shapes = self._unpack_loc_scale(vals)
-        if not (np.all(self._argcheck(*shapes)) and scale > 0):
+        if not (np.all(rv_continuous._argcheck(*shapes)) and scale > 0):
             raise FitError("Optimization converged to parameters that are "
                            "outside the range allowed by the distribution.")
 
@@ -2807,7 +2810,7 @@ class rv_continuous(rv_generic):
         loc_hat, scale_hat = self.fit_loc_scale(data, *args)
 
         # Compute the support according to the shape parameters.
-        self._argcheck(*args)
+        rv_continuous._argcheck(*args)
         _a, _b = self._get_support(*args)
         a, b = _a, _b
         support_width = b - a
@@ -3006,7 +3009,7 @@ class rv_continuous(rv_generic):
         """
         lockwds = {'loc': loc,
                    'scale': scale}
-        self._argcheck(*args)
+        rv_continuous._argcheck(*args)
         _a, _b = self._get_support(*args)
         if func is None:
             def fun(x, *args):
@@ -3513,7 +3516,7 @@ class rv_discrete(rv_generic):
         args = tuple(map(asarray, args))
         _a, _b = self._get_support(*args)
         k = asarray(k-loc)
-        cond0 = self._argcheck(*args)
+        cond0 = rv_discrete._argcheck(*args)
         cond1 = (k >= _a) & (k <= _b)
         if not isinstance(self, rv_sample):
             cond1 = cond1 & self._nonzero(k, *args)
@@ -3551,7 +3554,7 @@ class rv_discrete(rv_generic):
         args = tuple(map(asarray, args))
         _a, _b = self._get_support(*args)
         k = asarray(k-loc)
-        cond0 = self._argcheck(*args)
+        cond0 = rv_discrete._argcheck(*args)
         cond1 = (k >= _a) & (k <= _b)
         if not isinstance(self, rv_sample):
             cond1 = cond1 & self._nonzero(k, *args)
@@ -3590,7 +3593,7 @@ class rv_discrete(rv_generic):
         args = tuple(map(asarray, args))
         _a, _b = self._get_support(*args)
         k = asarray(k-loc)
-        cond0 = self._argcheck(*args)
+        cond0 = rv_discrete._argcheck(*args)
         cond1 = (k >= _a) & (k < _b)
         cond2 = (k >= _b)
         cond3 = np.isneginf(k)
@@ -3632,7 +3635,7 @@ class rv_discrete(rv_generic):
         args = tuple(map(asarray, args))
         _a, _b = self._get_support(*args)
         k = asarray(k-loc)
-        cond0 = self._argcheck(*args)
+        cond0 = rv_discrete._argcheck(*args)
         cond1 = (k >= _a) & (k < _b)
         cond2 = (k >= _b)
         cond = cond0 & cond1
@@ -3672,7 +3675,7 @@ class rv_discrete(rv_generic):
         args = tuple(map(asarray, args))
         _a, _b = self._get_support(*args)
         k = asarray(k-loc)
-        cond0 = self._argcheck(*args)
+        cond0 = rv_discrete._argcheck(*args)
         cond1 = (k >= _a) & (k < _b)
         cond2 = ((k < _a) | np.isneginf(k)) & cond0
         cond = cond0 & cond1 & np.isfinite(k)
@@ -3713,7 +3716,7 @@ class rv_discrete(rv_generic):
         args = tuple(map(asarray, args))
         _a, _b = self._get_support(*args)
         k = asarray(k-loc)
-        cond0 = self._argcheck(*args)
+        cond0 = rv_discrete._argcheck(*args)
         cond1 = (k >= _a) & (k < _b)
         cond2 = (k < _a) & cond0
         cond = cond0 & cond1
@@ -3751,7 +3754,7 @@ class rv_discrete(rv_generic):
         q, loc = map(asarray, (q, loc))
         args = tuple(map(asarray, args))
         _a, _b = self._get_support(*args)
-        cond0 = self._argcheck(*args) & (loc == loc)
+        cond0 = rv_discrete._argcheck(*args) & (loc == loc)
         cond1 = (q > 0) & (q < 1)
         cond2 = (q == 1) & cond0
         cond = cond0 & cond1
@@ -3791,7 +3794,7 @@ class rv_discrete(rv_generic):
         q, loc = map(asarray, (q, loc))
         args = tuple(map(asarray, args))
         _a, _b = self._get_support(*args)
-        cond0 = self._argcheck(*args) & (loc == loc)
+        cond0 = rv_discrete._argcheck(*args) & (loc == loc)
         cond1 = (q > 0) & (q < 1)
         cond2 = (q == 1) & cond0
         cond3 = (q == 0) & cond0
