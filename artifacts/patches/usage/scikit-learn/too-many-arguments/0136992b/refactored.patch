diff --git a/sklearn/neighbors/tests/test_nca.py b/sklearn/neighbors/tests/test_nca.py
index ebfb01d12e..47f3e5f8c8 100644
--- a/sklearn/neighbors/tests/test_nca.py
+++ b/sklearn/neighbors/tests/test_nca.py
@@ -31,6 +31,16 @@ iris_target = iris.target[perm]
 iris_data.flags.writeable = False
 iris_target.flags.writeable = False
 EPS = np.finfo(float).eps
+class DataParams__loss_grad_lbfgs_237:
+    def __init__(self, packed_coef_inter, X, y, sample_weight, activations, deltas, coef_grads, intercept_grads):
+        self.packed_coef_inter = packed_coef_inter
+        self.X = X
+        self.y = y
+        self.sample_weight = sample_weight
+        self.activations = activations
+        self.deltas = deltas
+        self.coef_grads = coef_grads
+        self.intercept_grads = intercept_grads
 
 
 def test_simple_example():
@@ -81,8 +91,7 @@ def test_toy_example_collapse_points():
         def callback(self, transformation, n_iter):
             """Stores the last value of the loss function"""
             self.loss, _ = self.fake_nca._loss_grad_lbfgs(
-                transformation, self.X, self.same_class_mask, -1.0
-            )
+                DataParams__loss_grad_lbfgs_237(transformation, self.X, self.same_class_mask, -1.0))
 
     loss_storer = LossStorer(X, y)
     nca = NeighborhoodComponentsAnalysis(random_state=42, callback=loss_storer.callback)
@@ -108,10 +117,10 @@ def test_finite_differences(global_random_seed):
     mask = y[:, np.newaxis] == y[np.newaxis, :]
 
     def fun(M):
-        return nca._loss_grad_lbfgs(M, X, mask)[0]
+        return nca._loss_grad_lbfgs(DataParams__loss_grad_lbfgs_237(M, X, mask))[0]
 
     def grad(M):
-        return nca._loss_grad_lbfgs(M, X, mask)[1]
+        return nca._loss_grad_lbfgs(DataParams__loss_grad_lbfgs_237(M, X, mask))[1]
 
     # compare the gradient to a finite difference approximation
     diff = check_grad(fun, grad, M.ravel())
diff --git a/sklearn/neural_network/_multilayer_perceptron.py b/sklearn/neural_network/_multilayer_perceptron.py
index a8a00fe3b4..af5766f70c 100644
--- a/sklearn/neural_network/_multilayer_perceptron.py
+++ b/sklearn/neural_network/_multilayer_perceptron.py
@@ -41,8 +41,19 @@ from ..utils.optimize import _check_optimize_result
 from ..utils.validation import _check_sample_weight, check_is_fitted, validate_data
 from ._base import ACTIVATIONS, DERIVATIVES, LOSS_FUNCTIONS
 from ._stochastic_optimizers import AdamOptimizer, SGDOptimizer
+from crtracker import MemoryCPUTracker
 
 _STOCHASTIC_SOLVERS = ["sgd", "adam"]
+class DataParams__loss_grad_lbfgs_237:
+    def __init__(self, packed_coef_inter, X, y, sample_weight, activations, deltas, coef_grads, intercept_grads):
+        self.packed_coef_inter = packed_coef_inter
+        self.X = X
+        self.y = y
+        self.sample_weight = sample_weight
+        self.activations = activations
+        self.deltas = deltas
+        self.coef_grads = coef_grads
+        self.intercept_grads = intercept_grads
 
 
 def _pack(coefs_, intercepts_):
@@ -232,17 +243,10 @@ class BaseMultilayerPerceptron(BaseEstimator, ABC):
 
         intercept_grads[layer] = np.sum(deltas[layer], axis=0) / sw_sum
 
+    @MemoryCPUTracker.track_usage(project_id='scikit-learn-benchmark', experiment_id='scikit-learn_R0913_0136992b', output_file='/root/ecooptimizer_benchmark/emissions/scikit-learn/too-many-arguments/0136992b_usage_refactored.csv')
     def _loss_grad_lbfgs(
         self,
-        packed_coef_inter,
-        X,
-        y,
-        sample_weight,
-        activations,
-        deltas,
-        coef_grads,
-        intercept_grads,
-    ):
+        data_params):
         """Compute the MLP loss function and its corresponding derivatives
         with respect to the different parameters given in the initialization.
 
@@ -286,11 +290,11 @@ class BaseMultilayerPerceptron(BaseEstimator, ABC):
         loss : float
         grad : array-like, shape (number of nodes of all layers,)
         """
-        self._unpack(packed_coef_inter)
-        loss, coef_grads, intercept_grads = self._backprop(
-            X, y, sample_weight, activations, deltas, coef_grads, intercept_grads
+        self._unpack(data_params.packed_coef_inter)
+        loss, data_params.coef_grads, data_params.intercept_grads = self._backprop(
+            data_params.X, data_params.y, data_params.sample_weight, data_params.activations, data_params.deltas, data_params.coef_grads, data_params.intercept_grads
         )
-        grad = _pack(coef_grads, intercept_grads)
+        grad = _pack(data_params.coef_grads, data_params.intercept_grads)
         return loss, grad
 
     def _backprop(
diff --git a/sklearn/neural_network/tests/test_mlp.py b/sklearn/neural_network/tests/test_mlp.py
index 9dddb78223..5d5c3a6286 100644
--- a/sklearn/neural_network/tests/test_mlp.py
+++ b/sklearn/neural_network/tests/test_mlp.py
@@ -60,6 +60,16 @@ iris = load_iris()
 
 X_iris = iris.data
 y_iris = iris.target
+class DataParams__loss_grad_lbfgs_237:
+    def __init__(self, packed_coef_inter, X, y, sample_weight, activations, deltas, coef_grads, intercept_grads):
+        self.packed_coef_inter = packed_coef_inter
+        self.X = X
+        self.y = y
+        self.sample_weight = sample_weight
+        self.activations = activations
+        self.deltas = deltas
+        self.coef_grads = coef_grads
+        self.intercept_grads = intercept_grads
 
 
 def test_alpha():
@@ -230,8 +240,7 @@ def test_gradient():
             # analytically compute the gradients
             def loss_grad_fun(t):
                 return mlp._loss_grad_lbfgs(
-                    t, X, Y, None, activations, deltas, coef_grads, intercept_grads
-                )
+                    DataParams__loss_grad_lbfgs_237(t, X, Y, None, activations, deltas, coef_grads, intercept_grads))
 
             [value, grad] = loss_grad_fun(theta)
             numgrad = np.zeros(np.size(theta))
