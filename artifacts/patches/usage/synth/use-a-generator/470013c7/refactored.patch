diff --git a/eco_smells/list_comp_predicate.py b/eco_smells/list_comp_predicate.py
index a4ad6ef..55d8f86 100644
--- a/eco_smells/list_comp_predicate.py
+++ b/eco_smells/list_comp_predicate.py
@@ -1,5 +1,6 @@
 import random
 from datetime import datetime, timedelta
+from crtracker import MemoryCPUTracker
 
 
 class ListCompPredicateSmells:
@@ -10,13 +11,14 @@ class ListCompPredicateSmells:
         return any([x > 90 and x % 7 == 0 for x in numbers])
 
     @staticmethod
+    @MemoryCPUTracker.track_usage(project_id='synth-benchmark', experiment_id='synth_R1729_470013c7', output_file='/root/ecooptimizer_benchmark/emissions/synth/use-a-generator/470013c7_usage_refactored.csv')
     def medium_case():
         """List comp inside all() with 10K elements"""
         users = [
             {"active": random.random() > 0.3, "score": random.gauss(50, 15)}
             for _ in range(10_000)
         ]
-        return all([u["active"] and u["score"] > 30 for u in users])
+        return all(u["active"] and u["score"] > 30 for u in users)
 
     @staticmethod
     def large_case():
