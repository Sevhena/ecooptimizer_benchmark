diff --git a/eco_smells/long_lambda.py b/eco_smells/long_lambda.py
index 2204204..a659ca5 100644
--- a/eco_smells/long_lambda.py
+++ b/eco_smells/long_lambda.py
@@ -1,3 +1,4 @@
+from crtracker import MemoryCPUTracker
 class LongLambdaSmells:
     @staticmethod
     def small_lambda():
@@ -40,27 +41,18 @@ class LongLambdaSmells:
         return result
 
     @staticmethod
+    @MemoryCPUTracker.track_usage(project_id='synth-benchmark', experiment_id='synth_LLE001_18ff6862', output_file='/root/ecooptimizer_benchmark/emissions/synth/long-lambda-expr/18ff6862_usage_refactored.csv')
     def extreme_lambda():
-        pipeline = lambda dataset: [
-            {
-                "id": item["id"],
-                "processed": True,
-                "value": item["value"] * 1.2
-                if item["type"] == "A"
-                else (item["value"] * 0.8 if item["type"] == "B" else item["value"]),
-                "status": "active" if item["value"] > 50 else "inactive",
-            }
-            for item in dataset
-            if item["valid"] and not item.get("deleted", False)
-        ]
-        test_data = [
-            {
-                "id": i,
-                "value": i * 10,
-                "type": "A" if i % 2 == 0 else "B",
-                "valid": True,
-            }
-            for i in range(200)
-        ]
-        result = pipeline(test_data)
-        return result
+        def pipeline(dataset):
+            return [
+                {
+                    "id": item["id"],
+                    "processed": True,
+                    "value": item["value"] * 1.2
+                    if item["type"] == "A"
+                    else (item["value"] * 0.8 if item["type"] == "B" else item["value"]),
+                    "status": "active" if item["value"] > 50 else "inactive",
+                }
+                for item in dataset
+                if item["valid"] and not item.get("deleted", False)
+            ]
