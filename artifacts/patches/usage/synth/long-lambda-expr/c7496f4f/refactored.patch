diff --git a/eco_smells/long_lambda.py b/eco_smells/long_lambda.py
index 2204204..91944f7 100644
--- a/eco_smells/long_lambda.py
+++ b/eco_smells/long_lambda.py
@@ -1,3 +1,4 @@
+from crtracker import MemoryCPUTracker
 class LongLambdaSmells:
     @staticmethod
     def small_lambda():
@@ -11,21 +12,17 @@ class LongLambdaSmells:
 
 
     @staticmethod
+    @MemoryCPUTracker.track_usage(project_id='synth-benchmark', experiment_id='synth_LLE001_c7496f4f', output_file='/root/ecooptimizer_benchmark/emissions/synth/long-lambda-expr/c7496f4f_usage_refactored.csv')
     def medium_lambda():
-        process = lambda data: [
-            item
-            for item in data
-            if item["active"]
-            and item["value"] > 100
-            and item["category"] in ("A", "B", "C")
-            and not item.get("excluded", False)
-        ]
-        test_data = [
-            {"active": True, "value": 150, "category": "A", "excluded": False}
-            for _ in range(50)
-        ]
-        result = process(test_data)
-        return result
+        def process(data):
+            return [
+                item
+                for item in data
+                if item["active"]
+                and item["value"] > 100
+                and item["category"] in ("A", "B", "C")
+                and not item.get("excluded", False)
+            ]
 
     @staticmethod
     def large_lambda():
