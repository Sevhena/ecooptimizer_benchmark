diff --git a/eco_smells/long_lambda.py b/eco_smells/long_lambda.py
index 2204204..4ec0fde 100644
--- a/eco_smells/long_lambda.py
+++ b/eco_smells/long_lambda.py
@@ -1,12 +1,15 @@
+from crtracker import MemoryCPUTracker
 class LongLambdaSmells:
     @staticmethod
     def small_lambda():
         """Lambda with string processing (>100 chars)"""
-        process_text = lambda text: (
-            text.strip().lower().replace(' ', '_') 
-            if len(text) > 3 
-            else text.upper() + '!'
-        )
+        with MemoryCPUTracker(project_id='synth-benchmark', experiment_id='synth_LLE001_0f527350', output_file='/root/ecooptimizer_benchmark/emissions/synth/long-lambda-expr/0f527350_usage_refactored.csv') as tracker:
+            def process_text(text):
+                return (
+                    text.strip().lower().replace(' ', '_') 
+                    if len(text) > 3 
+                    else text.upper() + '!'
+                )
         return process_text("  Hello World  ")  # -> "hello_world"
 
 
