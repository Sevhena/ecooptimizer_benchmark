diff --git a/eco_smells/long_lambda.py b/eco_smells/long_lambda.py
index 2204204..daf9f6b 100644
--- a/eco_smells/long_lambda.py
+++ b/eco_smells/long_lambda.py
@@ -1,3 +1,4 @@
+from crtracker import MemoryCPUTracker
 class LongLambdaSmells:
     @staticmethod
     def small_lambda():
@@ -41,18 +42,19 @@ class LongLambdaSmells:
 
     @staticmethod
     def extreme_lambda():
-        pipeline = lambda dataset: [
-            {
-                "id": item["id"],
-                "processed": True,
-                "value": item["value"] * 1.2
-                if item["type"] == "A"
-                else (item["value"] * 0.8 if item["type"] == "B" else item["value"]),
-                "status": "active" if item["value"] > 50 else "inactive",
-            }
-            for item in dataset
-            if item["valid"] and not item.get("deleted", False)
-        ]
+        with MemoryCPUTracker(project_id='synth-benchmark', experiment_id='synth_LLE001_b1fedaae', output_file='/root/ecooptimizer_benchmark/emissions/synth/long-lambda-expr/b1fedaae_usage.csv') as tracker:
+            pipeline = lambda dataset: [
+                {
+                    "id": item["id"],
+                    "processed": True,
+                    "value": item["value"] * 1.2
+                    if item["type"] == "A"
+                    else (item["value"] * 0.8 if item["type"] == "B" else item["value"]),
+                    "status": "active" if item["value"] > 50 else "inactive",
+                }
+                for item in dataset
+                if item["valid"] and not item.get("deleted", False)
+            ]
         test_data = [
             {
                 "id": i,
