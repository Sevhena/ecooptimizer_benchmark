diff --git a/scipy/ndimage/_interpolation.py b/scipy/ndimage/_interpolation.py
index 9527cdce7..972afcfdc 100644
--- a/scipy/ndimage/_interpolation.py
+++ b/scipy/ndimage/_interpolation.py
@@ -39,6 +39,7 @@ from scipy import special
 from . import _ni_support
 from . import _nd_image
 from ._ni_docstrings import docfiller
+from crtracker import MemoryCPUTracker
 
 
 __all__ = ['spline_filter1d', 'spline_filter', 'geometric_transform',
@@ -971,7 +972,9 @@ def rotate(input, angle, axes=(1, 0), reshape=True, output=None, order=3,
     if len(axes) != 2:
         raise ValueError('axes should contain exactly two values')
 
-    if not all([float(ax).is_integer() for ax in axes]):
+    with MemoryCPUTracker(project_id='scipy-benchmark', experiment_id='scipy_R1729_2bbdf916', output_file='/root/ecooptimizer_benchmark/emissions/scipy/use-a-generator/2bbdf916_usage_refactored.csv') as tracker:
+        _carbon_wrapped_2bc48cc2 = all([float(ax).is_integer() for ax in axes])
+    if not _carbon_wrapped_2bc48cc2:
         raise ValueError('axes should contain only integer values')
 
     if axes[0] < 0:
