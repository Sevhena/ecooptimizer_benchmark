diff --git a/django/utils/numberformat.py b/django/utils/numberformat.py
index cf8b2d219c..a36381ada7 100644
--- a/django/utils/numberformat.py
+++ b/django/utils/numberformat.py
@@ -2,6 +2,7 @@ from decimal import Decimal
 
 from django.conf import settings
 from django.utils.safestring import mark_safe
+from crtracker import MemoryCPUTracker
 
 
 def format(
@@ -91,15 +92,17 @@ def format(
             # grouping is a single value
             intervals = [grouping, 0]
         active_interval = intervals.pop(0)
-        int_part_gd = ""
+        int_part_gd = []
         cnt = 0
-        for digit in int_part[::-1]:
-            if cnt and cnt == active_interval:
-                if intervals:
-                    active_interval = intervals.pop(0) or active_interval
-                int_part_gd += thousand_sep[::-1]
-                cnt = 0
-            int_part_gd += digit
-            cnt += 1
+        with MemoryCPUTracker(project_id='django-benchmark', experiment_id='django_SCL001_871b0de2', output_file='/root/ecooptimizer_benchmark/emissions/django/string-concat-loop/871b0de2_usage_refactored.csv') as tracker:
+            for digit in int_part[::-1]:
+                if cnt and cnt == active_interval:
+                    if intervals:
+                        active_interval = intervals.pop(0) or active_interval
+                    int_part_gd.append(thousand_sep[::-1])
+                    cnt = 0
+                int_part_gd.append(digit)
+                cnt += 1
+            int_part_gd = ''.join(int_part_gd)
         int_part = int_part_gd[::-1]
-    return sign + int_part + dec_part
+    return sign + int_part + dec_part
\ No newline at end of file
