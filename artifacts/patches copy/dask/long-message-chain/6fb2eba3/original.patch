diff --git a/dask/array/einsumfuncs.py b/dask/array/einsumfuncs.py
index 1d55f4e87..00e182a53 100644
--- a/dask/array/einsumfuncs.py
+++ b/dask/array/einsumfuncs.py
@@ -7,6 +7,7 @@ import numpy as np
 from dask.array._shuffle import _calculate_new_chunksizes
 from dask.array.core import asarray, blockwise, einsum_lookup
 from dask.utils import cached_max, derived_from
+from codecarbon import EmissionsTracker
 
 einsum_symbols = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ"
 einsum_symbols_set = set(einsum_symbols)
@@ -113,7 +114,8 @@ def parse_einsum_input(operands):
 
     # Parse ellipses
     if "." in subscripts:
-        used = subscripts.replace(".", "").replace(",", "").replace("->", "")
+        with EmissionsTracker(project_name='dask-benchmark', experiment_id='dask_LMC001_6fb2eba3', output_file='/root/ecooptimizer_benchmark/emissions/dask/long-message-chain/6fb2eba3.csv') as tracker:
+            used = subscripts.replace(".", "").replace(",", "").replace("->", "")
         unused = list(einsum_symbols_set - set(used))
         ellipse_inds = "".join(unused)
         longest = 0
