diff --git a/dask/bytes/tests/test_s3.py b/dask/bytes/tests/test_s3.py
index 8cf9b5e81..104560d98 100644
--- a/dask/bytes/tests/test_s3.py
+++ b/dask/bytes/tests/test_s3.py
@@ -49,6 +49,27 @@ files = {
         b'{"amount": 800, "name": "Dennis"}\n'
     ),
 }
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 
 @contextmanager
@@ -466,8 +487,7 @@ def test_parquet(s3, engine, s3so, metadata_file):
     )
     df = dd.from_pandas(data, chunksize=500)
     df.to_parquet(
-        url, engine=engine, storage_options=s3so, write_metadata_file=metadata_file
-    )
+        DataParams_to_parquet_339(url, engine = engine, write_metadata_file = metadata_file), ConfigParams_to_parquet_339(storage_options = s3so))
 
     files = [f.split("/")[-1] for f in s3.ls(url)]
     if metadata_file:
@@ -562,20 +582,9 @@ def test_parquet_append(s3, engine, s3so):
     )
     df = dd.from_pandas(data, chunksize=500)
     df.to_parquet(
-        url,
-        engine=engine,
-        storage_options=s3so,
-        write_index=False,
-        write_metadata_file=True,
-    )
+        DataParams_to_parquet_339(url, engine = engine, write_index = False, write_metadata_file = True), ConfigParams_to_parquet_339(storage_options = s3so))
     df.to_parquet(
-        url,
-        engine=engine,
-        storage_options=s3so,
-        write_index=False,
-        append=True,
-        ignore_divisions=True,
-    )
+        DataParams_to_parquet_339(url, engine = engine, write_index = False, append = True, ignore_divisions = True), ConfigParams_to_parquet_339(storage_options = s3so))
 
     files = [f.split("/")[-1] for f in s3.ls(url)]
     assert "_common_metadata" in files
@@ -606,12 +615,7 @@ def test_parquet_wstoragepars(s3, s3so, engine):
     data = pd.DataFrame({"i32": np.array([0, 5, 2, 5])})
     df = dd.from_pandas(data, chunksize=500)
     df.to_parquet(
-        url,
-        engine=engine,
-        write_index=False,
-        storage_options=s3so,
-        write_metadata_file=True,
-    )
+        DataParams_to_parquet_339(url, engine = engine, write_index = False, write_metadata_file = True), ConfigParams_to_parquet_339(storage_options = s3so))
 
     dd.read_parquet(
         url,
diff --git a/dask/dataframe/dask_expr/_collection.py b/dask/dataframe/dask_expr/_collection.py
index 0e16f3ed9..5e6efb75f 100644
--- a/dask/dataframe/dask_expr/_collection.py
+++ b/dask/dataframe/dask_expr/_collection.py
@@ -144,6 +144,27 @@ from dask.utils import (
     typename,
 )
 from dask.widgets import get_template
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 #
 # Utilities to wrap Expr API
@@ -3311,7 +3332,7 @@ class DataFrame(FrameBase):
     def to_parquet(self, path, **kwargs):
         from dask.dataframe.dask_expr.io.parquet import to_parquet
 
-        return to_parquet(self, path, **kwargs)
+        return to_parquet(DataParams_to_parquet_339(self, kwargs), ConfigParams_to_parquet_339(path))
 
     @derived_from(pd.DataFrame)
     def select_dtypes(self, include=None, exclude=None):
diff --git a/dask/dataframe/dask_expr/io/parquet.py b/dask/dataframe/dask_expr/io/parquet.py
index 4563d238c..0fefbd052 100644
--- a/dask/dataframe/dask_expr/io/parquet.py
+++ b/dask/dataframe/dask_expr/io/parquet.py
@@ -64,6 +64,28 @@ from dask.delayed import delayed
 from dask.tokenize import _tokenize_deterministic, normalize_token, tokenize
 from dask.typing import Key
 from dask.utils import apply, funcname, natural_sort_key, parse_bytes, typename
+from codecarbon import track_emissions
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 
 @normalize_token.register(pa.fs.FileInfo)
@@ -334,25 +356,9 @@ class ToParquetBarrier(Expr):
             return {(self._name, 0): (lambda x: None, self.frame.__dask_keys__())}
 
 
+@track_emissions(project_name='dask-benchmark', experiment_id='dask_R0913_b791c549', output_file='/root/ecooptimizer_benchmark/emissions/dask/too-many-arguments/b791c549_refactored.csv')
 def to_parquet(
-    df,
-    path,
-    compression="snappy",
-    write_index=True,
-    append=False,
-    overwrite=False,
-    ignore_divisions=False,
-    partition_on=None,
-    storage_options=None,
-    custom_metadata=None,
-    write_metadata_file=None,
-    compute=True,
-    compute_kwargs=None,
-    schema="infer",
-    name_function=None,
-    filesystem=None,
-    engine=None,
-    **kwargs,
+    data_params, config_params, **kwargs,
 ):
     """Store Dask.dataframe to Parquet files
 
@@ -465,50 +471,50 @@ def to_parquet(
     """
     from dask.dataframe.dask_expr._collection import new_collection
 
-    engine = _set_parquet_engine(engine=engine, meta=df._meta)
-    compute_kwargs = compute_kwargs or {}
+    data_params.engine = _set_parquet_engine(data_params.engine=data_params.engine, meta=data_params.df._meta)
+    data_params.compute_kwargs = data_params.compute_kwargs or {}
 
-    partition_on = partition_on or []
-    if isinstance(partition_on, str):
-        partition_on = [partition_on]
+    data_params.partition_on = data_params.partition_on or []
+    if isinstance(data_params.partition_on, str):
+        data_params.partition_on = [data_params.partition_on]
 
-    if set(partition_on) - set(df.columns):
+    if set(data_params.partition_on) - set(data_params.df.columns):
         raise ValueError(
             "Partitioning on non-existent column. "
             "partition_on=%s ."
-            "columns=%s" % (str(partition_on), str(list(df.columns)))
+            "columns=%s" % (str(data_params.partition_on), str(list(data_params.df.columns)))
         )
 
-    if df.columns.inferred_type not in {"string", "empty"}:
+    if data_params.df.columns.inferred_type not in {"string", "empty"}:
         raise ValueError("parquet doesn't support non-string column names")
 
-    if isinstance(engine, str):
-        engine = get_engine(engine)
+    if isinstance(data_params.engine, str):
+        data_params.engine = get_engine(data_params.engine)
 
-    if hasattr(path, "name"):
-        path = stringify_path(path)
+    if hasattr(config_params.path, "name"):
+        config_params.path = stringify_path(config_params.path)
 
-    fs, _paths, _, _ = engine.extract_filesystem(
-        path,
-        filesystem=filesystem,
+    fs, _paths, _, _ = data_params.engine.extract_filesystem(
+        config_params.path,
+        data_params.filesystem=data_params.filesystem,
         dataset_options={},
         open_file_options={},
-        storage_options=storage_options,
+        config_params.storage_options=config_params.storage_options,
     )
     assert len(_paths) == 1, "only one path"
-    path = _paths[0]
+    config_params.path = _paths[0]
 
-    if overwrite:
-        if append:
+    if data_params.overwrite:
+        if data_params.append:
             raise ValueError("Cannot use both `overwrite=True` and `append=True`!")
 
-        if fs.exists(path) and fs.isdir(path):
+        if fs.exists(config_params.path) and fs.isdir(config_params.path):
             # Check for any previous parquet ops reading from a file in the
             # output directory, since deleting those files now would result in
             # errors or incorrect results.
-            for read_op in df.expr.find_operations(ReadParquet):
-                read_path_with_slash = str(read_op.path).rstrip("/") + "/"
-                write_path_with_slash = path.rstrip("/") + "/"
+            for read_op in data_params.df.expr.find_operations(ReadParquet):
+                read_path_with_slash = str(read_op.config_params.path).rstrip("/") + "/"
+                write_path_with_slash = config_params.path.rstrip("/") + "/"
                 if read_path_with_slash.startswith(write_path_with_slash):
                     raise ValueError(
                         "Cannot overwrite a path that you are reading "
@@ -518,32 +524,32 @@ def to_parquet(
             # Don't remove the directory if it's the current working directory
             if _is_local_fs(fs):
                 working_dir = fs.expand_path(".")[0]
-                if path.rstrip("/") == working_dir.rstrip("/"):
+                if config_params.path.rstrip("/") == working_dir.rstrip("/"):
                     raise ValueError(
                         "Cannot clear the contents of the current working directory!"
                     )
 
             # It's safe to clear the output directory
-            fs.rm(path, recursive=True)
+            fs.rm(config_params.path, recursive=True)
 
         # Clear read_parquet caches in case we are
         # also reading from the overwritten path
         _cached_plan.clear()
 
     # Always skip divisions checks if divisions are unknown
-    if not df.known_divisions:
-        ignore_divisions = True
+    if not data_params.df.known_divisions:
+        data_params.ignore_divisions = True
 
     # Save divisions and corresponding index name. This is necessary,
     # because we may be resetting the index to write the file
-    division_info = {"divisions": df.divisions, "name": df.index.name}
+    division_info = {"divisions": data_params.df.divisions, "name": data_params.df.index.name}
     if division_info["name"] is None:
         # As of 0.24.2, pandas will rename an index with name=None
         # when df.reset_index() is called.  The default name is "index",
         # but dask will always change the name to the NONE_LABEL constant
-        if NONE_LABEL not in df.columns:
+        if NONE_LABEL not in data_params.df.columns:
             division_info["name"] = NONE_LABEL
-        elif write_index:
+        elif data_params.write_index:
             raise ValueError(
                 "Index must have a name if __null_dask_index__ is a column."
             )
@@ -558,8 +564,8 @@ def to_parquet(
     # a "special" name if the string is already used as a "real" column name.
     reserved_names = []
     for name in ["index", "level_0"]:
-        if name not in df.columns:
-            reserved_names.append(name)
+        if name not in data_params.df.columns:
+            reserved_names.data_params.append(name)
 
     # If write_index==True (default), reset the index and record the
     # name of the original index in `index_cols` (we will set the name
@@ -569,19 +575,19 @@ def to_parquet(
     # index preservation itself).  The column index
     # will be written to "pandas metadata" if write_index=True
     index_cols = []
-    if write_index:
-        real_cols = set(df.columns)
-        none_index = list(df._meta.index.names) == [None]
-        df = df.reset_index()
+    if data_params.write_index:
+        real_cols = set(data_params.df.columns)
+        none_index = list(data_params.df._meta.index.names) == [None]
+        data_params.df = data_params.df.reset_index()
         if none_index:
-            rename_columns = {c: NONE_LABEL for c in df.columns if c in reserved_names}
-            df = df.rename(columns=rename_columns)
-        index_cols = [c for c in set(df.columns) - real_cols]
+            rename_columns = {c: NONE_LABEL for c in data_params.df.columns if c in reserved_names}
+            data_params.df = data_params.df.rename(columns=rename_columns)
+        index_cols = [c for c in set(data_params.df.columns) - real_cols]
     else:
         # Not writing index - might as well drop it
-        df = df.reset_index(drop=True)
+        data_params.df = data_params.df.reset_index(drop=True)
 
-    if custom_metadata and b"pandas" in custom_metadata.keys():
+    if data_params.custom_metadata and b"pandas" in data_params.custom_metadata.keys():
         raise ValueError(
             "User-defined key/value metadata (custom_metadata) can not "
             "contain a b'pandas' key.  This key is reserved by Pandas, "
@@ -591,31 +597,31 @@ def to_parquet(
 
     # Engine-specific initialization steps to write the dataset.
     # Possibly create parquet metadata, and load existing stuff if appending
-    i_offset, fmd, metadata_file_exists, extra_write_kwargs = engine.initialize_write(
-        df,
+    i_offset, fmd, metadata_file_exists, extra_write_kwargs = data_params.engine.initialize_write(
+        data_params.df,
         fs,
-        path,
-        append=append,
-        ignore_divisions=ignore_divisions,
-        partition_on=partition_on,
+        config_params.path,
+        data_params.append=data_params.append,
+        data_params.ignore_divisions=data_params.ignore_divisions,
+        data_params.partition_on=data_params.partition_on,
         division_info=division_info,
         index_cols=index_cols,
-        schema=schema,
-        custom_metadata=custom_metadata,
+        data_params.schema=data_params.schema,
+        data_params.custom_metadata=data_params.custom_metadata,
         **kwargs,
     )
 
     # By default we only write a metadata file when appending if one already
     # exists
-    if append and write_metadata_file is None:
-        write_metadata_file = metadata_file_exists
+    if data_params.append and data_params.write_metadata_file is None:
+        data_params.write_metadata_file = metadata_file_exists
 
     # Check that custom name_function is valid,
     # and that it will produce unique names
-    if name_function is not None:
-        if not callable(name_function):
+    if data_params.name_function is not None:
+        if not callable(data_params.name_function):
             raise ValueError("``name_function`` must be a callable with one argument.")
-        filenames = [name_function(i + i_offset) for i in range(df.npartitions)]
+        filenames = [data_params.name_function(i + i_offset) for i in range(data_params.df.npartitions)]
         if len(set(filenames)) < len(filenames):
             raise ValueError("``name_function`` must produce unique filenames.")
 
@@ -636,34 +642,34 @@ def to_parquet(
         with ctx:
             out = new_collection(
                 ToParquet(
-                    df,
-                    path,
+                    data_params.df,
+                    config_params.path,
                     fs,
                     fmd,
-                    engine,
+                    data_params.engine,
                     i_offset,
-                    partition_on,
-                    write_metadata_file,
-                    name_function,
+                    data_params.partition_on,
+                    data_params.write_metadata_file,
+                    data_params.name_function,
                     toolz.merge(
                         kwargs,
                         {
-                            "compression": compression,
-                            "custom_metadata": custom_metadata,
+                            "compression": data_params.compression,
+                            "custom_metadata": data_params.custom_metadata,
                         },
                         extra_write_kwargs,
                     ),
-                    append,
+                    data_params.append,
                 )
             )
 
-    if compute:
-        out = out.compute(**compute_kwargs)
+    if data_params.compute:
+        out = out.data_params.compute(**data_params.compute_kwargs)
 
     # Invalidate the filesystem listing cache for the output path after write.
     # We do this before returning, even if `compute=False`. This helps ensure
     # that reading files that were just written succeeds.
-    fs.invalidate_cache(path)
+    fs.invalidate_cache(config_params.path)
 
     return out
 
diff --git a/dask/dataframe/dask_expr/io/tests/test_distributed.py b/dask/dataframe/dask_expr/io/tests/test_distributed.py
index e5cd30da2..a56c67b5f 100644
--- a/dask/dataframe/dask_expr/io/tests/test_distributed.py
+++ b/dask/dataframe/dask_expr/io/tests/test_distributed.py
@@ -15,6 +15,27 @@ from dask.dataframe.dask_expr import read_parquet
 from dask.dataframe.dask_expr.tests._util import _backend_library, assert_eq
 
 pd = _backend_library()
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 
 @pytest.fixture(params=["arrow"])
@@ -26,14 +47,14 @@ def _make_file(dir, df=None, filename="myfile.parquet", **kwargs):
     fn = os.path.join(str(dir), filename)
     if df is None:
         df = pd.DataFrame({c: range(10) for c in "abcde"})
-    df.to_parquet(fn, **kwargs)
+    df.to_parquet(DataParams_to_parquet_339(fn), ConfigParams_to_parquet_339(kwargs))
     return fn
 
 
 @gen_cluster(client=True, clean_kwargs={"threads": False})
 async def test_io_fusion_merge(c, s, a, b, tmpdir):
     pdf = pd.DataFrame({c: range(100) for c in "abcdefghij"})
-    p = dd.from_pandas(pdf, 2).to_parquet(tmpdir, compute=False)
+    p = dd.from_pandas(pdf, 2).to_parquet(DataParams_to_parquet_339(tmpdir, compute = False))
     _ = await c.gather(c.compute(p))
 
     df = dd.read_parquet(tmpdir).merge(
diff --git a/dask/dataframe/dask_expr/io/tests/test_io.py b/dask/dataframe/dask_expr/io/tests/test_io.py
index 3911a7338..6b92a3e7f 100644
--- a/dask/dataframe/dask_expr/io/tests/test_io.py
+++ b/dask/dataframe/dask_expr/io/tests/test_io.py
@@ -29,6 +29,27 @@ from dask.dataframe.utils import assert_eq, make_meta, pyarrow_strings_enabled
 
 # Set DataFrame backend for this module
 pd = _backend_library()
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 
 def _make_file(dir, format="parquet", df=None):
@@ -38,7 +59,7 @@ def _make_file(dir, format="parquet", df=None):
     if format == "csv":
         df.to_csv(fn)
     elif format == "parquet":
-        df.to_parquet(fn)
+        df.to_parquet(DataParams_to_parquet_339(fn))
     else:
         ValueError(f"{format} not a supported format")
     return fn
@@ -138,7 +159,7 @@ def test_read_csv_keywords(tmpdir):
 
 def test_io_fusion_blockwise(tmpdir):
     pdf = pd.DataFrame({c: range(10) for c in "abcdefghijklmn"})
-    dd.from_pandas(pdf, 3).to_parquet(tmpdir)
+    dd.from_pandas(pdf, 3).to_parquet(DataParams_to_parquet_339(tmpdir))
     read_parq = read_parquet(tmpdir)
     df = read_parq["a"].fillna(10).optimize()
     assert df.npartitions == 2
@@ -156,7 +177,7 @@ def test_io_fusion_blockwise(tmpdir):
 
 def test_repartition_io_fusion_blockwise(tmpdir):
     pdf = pd.DataFrame({c: range(10) for c in "ab"})
-    dd.from_pandas(pdf, 10).to_parquet(tmpdir)
+    dd.from_pandas(pdf, 10).to_parquet(DataParams_to_parquet_339(tmpdir))
     df = read_parquet(tmpdir)["a"]
     df = df.repartition(npartitions=lambda x: max(x // 2, 1)).optimize()
     assert df.npartitions == 2
@@ -165,8 +186,8 @@ def test_repartition_io_fusion_blockwise(tmpdir):
 def test_io_fusion_merge(tmpdir):
     pdf = pd.DataFrame({c: range(10) for c in "ab"})
     pdf2 = pd.DataFrame({c: range(10) for c in "uvwxyz"})
-    dd.from_pandas(pdf, 10).to_parquet(tmpdir)
-    dd.from_pandas(pdf2, 10).to_parquet(tmpdir + "x")
+    dd.from_pandas(pdf, 10).to_parquet(DataParams_to_parquet_339(tmpdir))
+    dd.from_pandas(pdf2, 10).to_parquet(DataParams_to_parquet_339(tmpdir + "x"))
     df = read_parquet(tmpdir)
     df2 = read_parquet(tmpdir + "x")
     result = df.merge(df2, left_on="a", right_on="w")[["a", "b", "u"]]
@@ -179,7 +200,7 @@ def test_io_fusion_merge(tmpdir):
 
 def test_io_fusion_zero(tmpdir):
     pdf = pd.DataFrame({c: range(10) for c in "ab"})
-    dd.from_pandas(pdf, 10).to_parquet(tmpdir)
+    dd.from_pandas(pdf, 10).to_parquet(DataParams_to_parquet_339(tmpdir))
     result = read_parquet(tmpdir, columns=[])
     assert_eq(result, pdf[[]])
 
@@ -188,7 +209,7 @@ def test_io_fusion_zero(tmpdir):
 def test_io_culling(tmpdir, fmt):
     pdf = pd.DataFrame({c: range(10) for c in "abcde"})
     if fmt == "parquet":
-        dd.from_pandas(pdf, 2).to_parquet(tmpdir)
+        dd.from_pandas(pdf, 2).to_parquet(DataParams_to_parquet_339(tmpdir))
         df = read_parquet(tmpdir)
     elif fmt == "csv":
         dd.from_pandas(pdf, 2).to_csv(tmpdir)
@@ -297,7 +318,7 @@ def test_combine_similar_no_projection_on_one_branch(tmpdir):
 @pytest.mark.parametrize("enforce_metadata", [True, False])
 def test_from_map(tmpdir, meta, label, allow_projection, enforce_metadata):
     pdf = pd.DataFrame({c: range(10) for c in "abcdefghijklmn"})
-    dd.from_pandas(pdf, 3).to_parquet(tmpdir, write_index=False)
+    dd.from_pandas(pdf, 3).to_parquet(DataParams_to_parquet_339(tmpdir, write_index = False))
     files = sorted(glob.glob(str(tmpdir) + "/*.parquet"))
     if allow_projection:
         func = pd.read_parquet
@@ -469,7 +490,7 @@ def test_from_dict():
 def test_normalize_token_parquet_filemetadata_and_schema(tmpdir, normalizer):
     df = pd.DataFrame({c: range(10) for c in "abcde"})
     path = tmpdir / "df.parquet"
-    df.to_parquet(path)
+    df.to_parquet(DataParams_to_parquet_339(path))
 
     meta = pq.read_metadata(path)
 
@@ -531,7 +552,7 @@ def test_from_array_partition_pruning():
 
 def test_map_partitions_assign_fusedio(tmpdir):
     data = {"country": ["se", "dk", "no"], "value": [0.1, 0.2, 0.3]}
-    pd.DataFrame(data).to_parquet(tmpdir, partition_cols=["country"])
+    pd.DataFrame(data).to_parquet(DataParams_to_parquet_339(tmpdir), partition_cols = ["country"])
     df = dd.read_parquet(tmpdir)
     y_pred = df.map_partitions(len)
     df["y_pred"] = y_pred
diff --git a/dask/dataframe/dask_expr/io/tests/test_parquet.py b/dask/dataframe/dask_expr/io/tests/test_parquet.py
index 35d0db215..e70495caf 100644
--- a/dask/dataframe/dask_expr/io/tests/test_parquet.py
+++ b/dask/dataframe/dask_expr/io/tests/test_parquet.py
@@ -22,13 +22,34 @@ from dask.dataframe.dask_expr.io.parquet import (
 )
 from dask.dataframe.utils import assert_eq
 from dask.utils import key_split
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 
 def _make_file(dir, df=None, filename="myfile.parquet", **kwargs):
     fn = os.path.join(str(dir), filename)
     if df is None:
         df = pd.DataFrame({c: range(10) for c in "abcde"})
-    df.to_parquet(fn, **kwargs)
+    df.to_parquet(DataParams_to_parquet_339(fn), ConfigParams_to_parquet_339(kwargs))
     return fn
 
 
@@ -104,20 +125,20 @@ def test_to_parquet(tmpdir, write_metadata_file):
     df = from_pandas(pdf, npartitions=2)
 
     # Check basic parquet round trip
-    df.to_parquet(tmpdir, write_metadata_file=write_metadata_file)
+    df.to_parquet(DataParams_to_parquet_339(tmpdir, write_metadata_file = write_metadata_file))
     df2 = read_parquet(tmpdir, calculate_divisions=True)
     assert_eq(df, df2)
 
     # Check overwrite behavior
     df["new"] = df["x"] + 1
-    df.to_parquet(tmpdir, overwrite=True, write_metadata_file=write_metadata_file)
+    df.to_parquet(DataParams_to_parquet_339(tmpdir, overwrite = True, write_metadata_file = write_metadata_file))
     df2 = read_parquet(tmpdir, calculate_divisions=True)
     assert_eq(df, df2)
 
     # Check that we cannot overwrite a path we are
     # reading from in the same graph
     with pytest.raises(ValueError, match="Cannot overwrite"):
-        df2.to_parquet(tmpdir, overwrite=True)
+        df2.to_parquet(DataParams_to_parquet_339(tmpdir, overwrite = True))
 
 
 @pytest.mark.xfail(not PYARROW_GE_1500, reason="requires 15.0.0")
@@ -214,7 +235,7 @@ def test_partition_pruning(tmpdir):
             ),
             npartitions=2,
         )
-        df.to_parquet(tmpdir, partition_on=["a"])
+        df.to_parquet(DataParams_to_parquet_339(tmpdir, partition_on = ["a"]))
         ddf = read_parquet(tmpdir, filesystem=filesystem)
         ddf_filtered = read_parquet(
             tmpdir, filters=[[("a", "==", 1)]], filesystem=filesystem
@@ -328,7 +349,7 @@ def test_aggregate_rg_stats_to_file(tmpdir):
     filesystem = fs.LocalFileSystem()
     fn = str(tmpdir)
     ddf = from_pandas(pd.DataFrame({"a": range(10)}), npartitions=1)
-    ddf.to_parquet(fn)
+    ddf.to_parquet(DataParams_to_parquet_339(fn))
     ddf = read_parquet(fn, filesystem=filesystem)
     frag = ddf._expr.fragments[0]
     # Make sure this doesn't raise. We'll test the actual aggregation below
@@ -572,7 +593,7 @@ def test_combine_statistics():
 
 def test_index_only_from_parquet(tmpdir):
     pdf = pd.DataFrame({"foo": range(5)}, index=range(50, 55))
-    pdf.to_parquet(tmpdir + "/test.parquet")
+    pdf.to_parquet(DataParams_to_parquet_339(tmpdir + "/test.parquet"))
     result = read_parquet(tmpdir + "/test.parquet").index
     assert_eq(result, pdf.index)
 
@@ -586,7 +607,7 @@ def test_timestamp_divisions(tmpdir):
         }
     )
     pdf["Timestamp"] = pd.to_datetime(pdf.Date) + pd.to_timedelta(pdf.Time)
-    pdf.to_parquet(tmpdir + "/test.parquet")
+    pdf.to_parquet(DataParams_to_parquet_339(tmpdir + "/test.parquet"))
     df = read_parquet(
         tmpdir + "/test.parquet", index="Timestamp", calculate_divisions=True
     )["Volume"]
@@ -602,7 +623,7 @@ def test_read_parquet_index_projection(tmpdir):
         columns=["val1", "val2", "val3", "val4", "val5", "val6", "val7", "tsprv"],
     )
     df = df.repartition(npartitions=201)
-    df.to_parquet(tmpdir + "/index", write_index=False)
+    df.to_parquet(DataParams_to_parquet_339(tmpdir + "/index", write_index = False))
 
     df = read_parquet(tmpdir + "/index", calculate_divisions=True)
     result = df.assign(dts=df.index - df.tsprv)
diff --git a/dask/dataframe/dask_expr/tests/test_partitioning_knowledge.py b/dask/dataframe/dask_expr/tests/test_partitioning_knowledge.py
index 1689dc1f0..4acd275c6 100644
--- a/dask/dataframe/dask_expr/tests/test_partitioning_knowledge.py
+++ b/dask/dataframe/dask_expr/tests/test_partitioning_knowledge.py
@@ -9,6 +9,27 @@ from dask.dataframe.dask_expr.tests._util import _backend_library, assert_eq
 
 # Set DataFrame backend for this module
 pd = _backend_library()
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 
 @pytest.mark.xfail(
@@ -18,7 +39,7 @@ def test_groupby_implicit_divisions(tmpdir):
     pdf1 = pd.DataFrame({"a": range(10), "bb": 1})
 
     df1 = from_pandas(pdf1, npartitions=2)
-    df1.to_parquet(tmpdir / "df1.parquet")
+    df1.to_parquet(DataParams_to_parquet_339(tmpdir / "df1.parquet"))
     df1 = read_parquet(
         tmpdir / "df1.parquet", filesystem="pyarrow", calculate_divisions=True
     )
diff --git a/dask/dataframe/io/tests/test_parquet.py b/dask/dataframe/io/tests/test_parquet.py
index e2481da11..e6b4573dc 100644
--- a/dask/dataframe/io/tests/test_parquet.py
+++ b/dask/dataframe/io/tests/test_parquet.py
@@ -57,6 +57,27 @@ _engine_fixture = pytest.fixture(
         pytest.param("pyarrow", marks=[PYARROW_MARK]),
     ]
 )
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 
 @_engine_fixture
@@ -119,7 +140,7 @@ def test_local(tmpdir, write_engine, read_engine, has_metadata):
     df = dd.from_pandas(data, chunksize=500)
 
     kwargs = {"write_metadata_file": True} if has_metadata else {}
-    df.to_parquet(tmp, write_index=False, engine=write_engine, **kwargs)
+    df.to_parquet(DataParams_to_parquet_339(tmp, write_index = False, engine = write_engine), ConfigParams_to_parquet_339(kwargs))
 
     files = os.listdir(tmp)
     assert ("_common_metadata" in files) == has_metadata
@@ -144,7 +165,7 @@ def test_empty(tmpdir, write_engine, read_engine, index):
         df = df.set_index("a", drop=True)
     ddf = dd.from_pandas(df, npartitions=2)
 
-    ddf.to_parquet(fn, write_index=index, engine=write_engine, write_metadata_file=True)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, write_index = index, engine = write_engine, write_metadata_file = True))
     read_df = dd.read_parquet(fn, engine=read_engine)
     assert_eq(ddf, read_df)
 
@@ -154,7 +175,7 @@ def test_simple(tmpdir, write_engine, read_engine):
     df = pd.DataFrame({"a": ["a", "b", "b"], "b": [4, 5, 6]})
     df = df.set_index("a", drop=True)
     ddf = dd.from_pandas(df, npartitions=2)
-    ddf.to_parquet(fn, engine=write_engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = write_engine))
     read_df = dd.read_parquet(
         fn, index=["a"], engine=read_engine, calculate_divisions=True
     )
@@ -167,8 +188,7 @@ def test_delayed_no_metadata(tmpdir, write_engine, read_engine):
     df = df.set_index("a", drop=True)
     ddf = dd.from_pandas(df, npartitions=2)
     ddf.to_parquet(
-        fn, engine=write_engine, compute=False, write_metadata_file=False
-    ).compute()
+        DataParams_to_parquet_339(fn, engine = write_engine, compute = False, write_metadata_file = False)).compute()
     files = os.listdir(fn)
     assert "_metadata" not in files
     read_df = dd.read_parquet(
@@ -182,7 +202,7 @@ def test_delayed_no_metadata(tmpdir, write_engine, read_engine):
 
 def test_read_glob(tmpdir, write_engine, read_engine):
     tmp_path = str(tmpdir)
-    ddf.to_parquet(tmp_path, engine=write_engine)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path, engine = write_engine))
     if os.path.exists(os.path.join(tmp_path, "_metadata")):
         os.unlink(os.path.join(tmp_path, "_metadata"))
     files = os.listdir(tmp_path)
@@ -199,7 +219,7 @@ def test_read_glob(tmpdir, write_engine, read_engine):
 
 def test_calculate_divisions_false(tmpdir, write_engine, read_engine):
     tmp_path = str(tmpdir)
-    ddf.to_parquet(tmp_path, write_index=False, engine=write_engine)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path, write_index = False, engine = write_engine))
 
     ddf2 = dd.read_parquet(
         tmp_path,
@@ -212,7 +232,7 @@ def test_calculate_divisions_false(tmpdir, write_engine, read_engine):
 
 def test_read_list(tmpdir, write_engine, read_engine):
     tmpdir = str(tmpdir)
-    ddf.to_parquet(tmpdir, engine=write_engine)
+    ddf.to_parquet(DataParams_to_parquet_339(tmpdir, engine = write_engine))
     files = sorted(
         (
             os.path.join(tmpdir, f)
@@ -230,7 +250,7 @@ def test_read_list(tmpdir, write_engine, read_engine):
 
 def test_columns_auto_index(tmpdir, write_engine, read_engine):
     fn = str(tmpdir)
-    ddf.to_parquet(fn, engine=write_engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = write_engine))
 
     # ### Empty columns ###
     # With divisions if supported
@@ -267,7 +287,7 @@ def test_columns_auto_index(tmpdir, write_engine, read_engine):
 
 def test_columns_index(tmpdir, write_engine, read_engine):
     fn = str(tmpdir)
-    ddf.to_parquet(fn, engine=write_engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = write_engine))
 
     # With Index
     # ----------
@@ -352,7 +372,7 @@ def test_columns_index(tmpdir, write_engine, read_engine):
 
 def test_nonsense_column(tmpdir, engine):
     fn = str(tmpdir)
-    ddf.to_parquet(fn, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
     with pytest.raises((ValueError, KeyError)):
         dd.read_parquet(fn, columns=["nonsense"], engine=engine)
     with pytest.raises((Exception, KeyError)):
@@ -361,7 +381,7 @@ def test_nonsense_column(tmpdir, engine):
 
 def test_columns_no_index(tmpdir, write_engine, read_engine):
     fn = str(tmpdir)
-    ddf.to_parquet(fn, engine=write_engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = write_engine))
     ddf2 = ddf.reset_index()
 
     # No Index
@@ -405,7 +425,7 @@ def test_columns_no_index(tmpdir, write_engine, read_engine):
 
 def test_calculate_divisions_no_index(tmpdir, write_engine, read_engine):
     fn = str(tmpdir)
-    ddf.to_parquet(fn, engine=write_engine, write_index=False)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = write_engine, write_index = False))
 
     df = dd.read_parquet(fn, engine=read_engine, index=False)
     assert df.index.name is None
@@ -474,14 +494,14 @@ def test_no_index(tmpdir, write_engine, read_engine):
     fn = str(tmpdir)
     df = pd.DataFrame({"a": [1, 2, 3], "b": [4, 5, 6]})
     ddf = dd.from_pandas(df, npartitions=2)
-    ddf.to_parquet(fn, engine=write_engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = write_engine))
     ddf2 = dd.read_parquet(fn, engine=read_engine)
     assert_eq(df, ddf2, check_index=False)
 
 
 def test_read_series(tmpdir, engine):
     fn = str(tmpdir)
-    ddf.to_parquet(fn, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
     ddf2 = dd.read_parquet(
         fn, columns=["x"], index="myindex", engine=engine, calculate_divisions=True
     )
@@ -495,7 +515,7 @@ def test_read_series(tmpdir, engine):
 
 def test_names(tmpdir, engine):
     fn = str(tmpdir)
-    ddf.to_parquet(fn, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
 
     def read(fn, **kwargs):
         return dd.read_parquet(fn, engine=engine, **kwargs)
@@ -511,7 +531,7 @@ def test_roundtrip_from_pandas(tmpdir, write_engine, read_engine):
     fn = str(tmpdir.join("test.parquet"))
     dfp = df.copy()
     dfp.index.name = "index"
-    dfp.to_parquet(fn, engine=write_engine)
+    dfp.to_parquet(DataParams_to_parquet_339(fn, engine = write_engine))
     ddf = dd.read_parquet(fn, index="index", engine=read_engine)
     assert_eq(dfp, ddf)
 
@@ -530,7 +550,7 @@ def test_roundtrip_nullable_dtypes(tmp_path):
         }
     )
     ddf = dd.from_pandas(df, npartitions=2)
-    ddf.to_parquet(tmp_path)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path))
     ddf2 = dd.read_parquet(tmp_path)
     assert_eq(df, ddf2)
 
@@ -548,7 +568,7 @@ def test_use_nullable_dtypes_with_types_mapper(tmp_path, engine):
         }
     )
     ddf = dd.from_pandas(df, npartitions=3)
-    ddf.to_parquet(tmp_path, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path, engine = engine))
 
     types_mapper = {
         pa.int64(): pd.Float32Dtype(),
@@ -568,7 +588,7 @@ def test_categorical(tmpdir, write_engine, read_engine):
     tmp = str(tmpdir)
     df = pd.DataFrame({"x": ["a", "b", "c"] * 100}, dtype="category")
     ddf = dd.from_pandas(df, npartitions=3)
-    dd.to_parquet(ddf, tmp, engine=write_engine)
+    dd.to_parquet(DataParams_to_parquet_339(ddf, engine = write_engine), ConfigParams_to_parquet_339(tmp))
 
     ddf2 = dd.read_parquet(tmp, categories="x", engine=read_engine)
     assert ddf2.compute().x.cat.categories.tolist() == ["a", "b", "c"]
@@ -602,11 +622,11 @@ def test_append(tmpdir, engine, metadata_file):
     half = len(df) // 2
     ddf1 = dd.from_pandas(df.iloc[:half], chunksize=100)
     ddf2 = dd.from_pandas(df.iloc[half:], chunksize=100)
-    ddf1.to_parquet(tmp, engine=engine, write_metadata_file=metadata_file)
+    ddf1.to_parquet(DataParams_to_parquet_339(tmp, engine = engine, write_metadata_file = metadata_file))
     if metadata_file:
         with open(str(tmpdir.join("_metadata")), "rb") as f:
             metadata1 = f.read()
-    ddf2.to_parquet(tmp, append=True, engine=engine)
+    ddf2.to_parquet(DataParams_to_parquet_339(tmp, append = True, engine = engine))
     if metadata_file:
         with open(str(tmpdir.join("_metadata")), "rb") as f:
             metadata2 = f.read()
@@ -634,8 +654,8 @@ def test_append_create(tmpdir, engine):
     half = len(df) // 2
     ddf1 = dd.from_pandas(df.iloc[:half], chunksize=100)
     ddf2 = dd.from_pandas(df.iloc[half:], chunksize=100)
-    ddf1.to_parquet(tmp_path, append=True, engine=engine)
-    ddf2.to_parquet(tmp_path, append=True, engine=engine)
+    ddf1.to_parquet(DataParams_to_parquet_339(tmp_path, append = True, engine = engine))
+    ddf2.to_parquet(DataParams_to_parquet_339(tmp_path, append = True, engine = engine))
 
     ddf3 = dd.read_parquet(tmp_path, engine=engine)
     assert_eq(df, ddf3)
@@ -669,15 +689,9 @@ def test_append_with_partition(tmpdir):
 
     dd_df0 = dd.from_pandas(df0, npartitions=1)
     dd_df1 = dd.from_pandas(df1, npartitions=1)
-    dd.to_parquet(dd_df0, tmp, partition_on=["lon"], engine="pyarrow")
+    dd.to_parquet(DataParams_to_parquet_339(dd_df0, partition_on = ["lon"], engine = "pyarrow"), ConfigParams_to_parquet_339(tmp))
     dd.to_parquet(
-        dd_df1,
-        tmp,
-        partition_on=["lon"],
-        append=True,
-        ignore_divisions=True,
-        engine="pyarrow",
-    )
+        DataParams_to_parquet_339(dd_df1, partition_on = ["lon"], append = True, ignore_divisions = True, engine = "pyarrow"), ConfigParams_to_parquet_339(tmp))
 
     out = dd.read_parquet(
         tmp, engine="pyarrow", index="index", calculate_divisions=True
@@ -700,7 +714,7 @@ def test_partition_on_cats(tmpdir, engine):
         }
     )
     d = dd.from_pandas(d, 2)
-    d.to_parquet(tmp, partition_on=["b"], engine=engine)
+    d.to_parquet(DataParams_to_parquet_339(tmp, partition_on = ["b"], engine = engine))
     df = dd.read_parquet(tmp, engine=engine)
     assert set(df.b.cat.categories) == {"x", "y", "z"}
 
@@ -718,7 +732,7 @@ def test_partition_on_cats_pyarrow(tmpdir, stats, meta):
         }
     )
     d = dd.from_pandas(d, 2)
-    d.to_parquet(tmp, partition_on=["b"], write_metadata_file=meta)
+    d.to_parquet(DataParams_to_parquet_339(tmp, partition_on = ["b"], write_metadata_file = meta))
     df = dd.read_parquet(tmp, calculate_divisions=stats)
     assert set(df.b.cat.categories) == {"x", "y", "z"}
 
@@ -735,7 +749,7 @@ def test_partition_parallel_metadata(tmpdir, engine):
         }
     )
     d = dd.from_pandas(d, 2)
-    d.to_parquet(tmp, partition_on=["b"], engine=engine, write_metadata_file=False)
+    d.to_parquet(DataParams_to_parquet_339(tmp, partition_on = ["b"], engine = engine, write_metadata_file = False))
     df = dd.read_parquet(
         tmp, engine=engine, calculate_divisions=True, metadata_task_size=1
     )
@@ -752,7 +766,7 @@ def test_partition_on_cats_2(tmpdir, engine):
         }
     )
     d = dd.from_pandas(d, 2)
-    d.to_parquet(tmp, partition_on=["b", "c"], engine=engine)
+    d.to_parquet(DataParams_to_parquet_339(tmp, partition_on = ["b", "c"], engine = engine))
     df = dd.read_parquet(tmp, engine=engine)
     assert set(df.b.cat.categories) == {"x", "y", "z"}
     assert set(df.c.cat.categories) == {"x", "y", "z"}
@@ -786,17 +800,16 @@ def test_append_wo_index(tmpdir, engine, metadata_file):
     half = len(df) // 2
     ddf1 = dd.from_pandas(df.iloc[:half], chunksize=100)
     ddf2 = dd.from_pandas(df.iloc[half:], chunksize=100)
-    ddf1.to_parquet(tmp, engine=engine, write_metadata_file=metadata_file)
+    ddf1.to_parquet(DataParams_to_parquet_339(tmp, engine = engine, write_metadata_file = metadata_file))
 
     with pytest.raises(ValueError) as excinfo:
-        ddf2.to_parquet(tmp, write_index=False, append=True, engine=engine)
+        ddf2.to_parquet(DataParams_to_parquet_339(tmp, write_index = False, append = True, engine = engine))
     assert "Appended columns" in str(excinfo.value)
 
     tmp = str(tmpdir.join("tmp2.parquet"))
     ddf1.to_parquet(
-        tmp, write_index=False, engine=engine, write_metadata_file=metadata_file
-    )
-    ddf2.to_parquet(tmp, write_index=False, append=True, engine=engine)
+        DataParams_to_parquet_339(tmp, write_index = False, engine = engine, write_metadata_file = metadata_file))
+    ddf2.to_parquet(DataParams_to_parquet_339(tmp, write_index = False, append = True, engine = engine))
 
     ddf3 = dd.read_parquet(tmp, index="f", engine=engine)
     assert_eq(df.set_index("f"), ddf3)
@@ -832,12 +845,12 @@ def test_append_overlapping_divisions(tmpdir, engine, metadata_file, index, offs
     )
     ddf1 = dd.from_pandas(df, chunksize=100)
     ddf2 = dd.from_pandas(df.set_index(df.index + offset), chunksize=100)
-    ddf1.to_parquet(tmp, engine=engine, write_metadata_file=metadata_file)
+    ddf1.to_parquet(DataParams_to_parquet_339(tmp, engine = engine, write_metadata_file = metadata_file))
 
     with pytest.raises(ValueError, match="overlap with previously written divisions"):
-        ddf2.to_parquet(tmp, engine=engine, append=True)
+        ddf2.to_parquet(DataParams_to_parquet_339(tmp, engine = engine, append = True))
 
-    ddf2.to_parquet(tmp, engine=engine, append=True, ignore_divisions=True)
+    ddf2.to_parquet(DataParams_to_parquet_339(tmp, engine = engine, append = True, ignore_divisions = True))
 
 
 def test_append_known_divisions_to_unknown_divisions_works(tmpdir, engine):
@@ -860,8 +873,8 @@ def test_append_known_divisions_to_unknown_divisions_works(tmpdir, engine):
     # sorted, then we want to skip erroring for overlapping divisions. Setting
     # `write_metadata_file=True` ensures this test works the same across both
     # engines.
-    ddf1.to_parquet(tmp, engine=engine, write_metadata_file=True)
-    ddf2.to_parquet(tmp, engine=engine, append=True)
+    ddf1.to_parquet(DataParams_to_parquet_339(tmp, engine = engine, write_metadata_file = True))
+    ddf2.to_parquet(DataParams_to_parquet_339(tmp, engine = engine, append = True))
 
     res = dd.read_parquet(tmp, engine=engine)
     sol = pd.concat([df1, df2])
@@ -880,14 +893,14 @@ def test_append_different_columns(tmpdir, engine, metadata_file):
     ddf2 = dd.from_pandas(df2, chunksize=2)
     ddf3 = dd.from_pandas(df3, chunksize=2)
 
-    ddf1.to_parquet(tmp, engine=engine, write_metadata_file=metadata_file)
+    ddf1.to_parquet(DataParams_to_parquet_339(tmp, engine = engine, write_metadata_file = metadata_file))
 
     with pytest.raises(ValueError) as excinfo:
-        ddf2.to_parquet(tmp, engine=engine, append=True)
+        ddf2.to_parquet(DataParams_to_parquet_339(tmp, engine = engine, append = True))
     assert "Appended columns" in str(excinfo.value)
 
     with pytest.raises(ValueError) as excinfo:
-        ddf3.to_parquet(tmp, engine=engine, append=True)
+        ddf3.to_parquet(DataParams_to_parquet_339(tmp, engine = engine, append = True))
     assert "Appended dtypes" in str(excinfo.value)
 
 
@@ -906,8 +919,8 @@ def test_append_dict_column(tmpdir):
     schema = {"value": pa.struct([("x", pa.int32())])}
 
     # Write ddf1 to tmp, and then append it again
-    ddf1.to_parquet(tmp, append=True, schema=schema)
-    ddf1.to_parquet(tmp, append=True, schema=schema, ignore_divisions=True)
+    ddf1.to_parquet(DataParams_to_parquet_339(tmp, append = True, schema = schema))
+    ddf1.to_parquet(DataParams_to_parquet_339(tmp, append = True, schema = schema, ignore_divisions = True))
 
     # Read back all data (ddf1 + ddf1)
     ddf2 = dd.read_parquet(tmp)
@@ -968,7 +981,7 @@ def test_ordering(tmpdir, write_engine, read_engine):
         columns=["c", "a", "b"],
     )
     ddf = dd.from_pandas(df, npartitions=2)
-    dd.to_parquet(ddf, tmp, engine=write_engine)
+    dd.to_parquet(DataParams_to_parquet_339(ddf, engine = write_engine), ConfigParams_to_parquet_339(tmp))
 
     ddf2 = dd.read_parquet(tmp, index="myindex", engine=read_engine)
     assert_eq(ddf, ddf2, check_divisions=False)
@@ -980,7 +993,7 @@ def test_read_parquet_custom_columns(tmpdir, engine):
         {"i32": np.arange(1000, dtype=np.int32), "f": np.arange(1000, dtype=np.float64)}
     )
     df = dd.from_pandas(data, chunksize=50)
-    df.to_parquet(tmp, engine=engine)
+    df.to_parquet(DataParams_to_parquet_339(tmp, engine = engine))
 
     df2 = dd.read_parquet(
         tmp, columns=["i32", "f"], engine=engine, calculate_divisions=True
@@ -1051,7 +1064,7 @@ def test_roundtrip(tmpdir, df, write_kwargs, read_kwargs, engine):
         df.index.name = "index"
     ddf = dd.from_pandas(df, npartitions=2)
 
-    dd.to_parquet(ddf, tmp, engine=engine, **write_kwargs)
+    dd.to_parquet(DataParams_to_parquet_339(ddf, write_kwargs, engine = engine), ConfigParams_to_parquet_339(tmp))
     ddf2 = dd.read_parquet(
         tmp, index=df.index.name, engine=engine, calculate_divisions=True, **read_kwargs
     )
@@ -1067,7 +1080,7 @@ def test_categories(tmpdir, engine):
         ddf = dd.from_pandas(df, npartitions=2)
         ddf["y"] = ddf.y.astype("category")
 
-    ddf.to_parquet(fn, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
     ddf2 = dd.read_parquet(
         fn, categories=["y"], engine=engine, calculate_divisions=True
     )
@@ -1100,7 +1113,7 @@ def test_categories_unnamed_index(tmpdir, engine):
     ddf = dd.from_pandas(df, npartitions=1)
     ddf = ddf.categorize(columns=["B"])
 
-    ddf.to_parquet(tmpdir, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(tmpdir, engine = engine))
     ddf2 = dd.read_parquet(tmpdir, engine=engine)
 
     assert_eq(ddf.index, ddf2.index, check_divisions=False)
@@ -1112,7 +1125,7 @@ def test_empty_partition(tmpdir, engine):
     ddf = dd.from_pandas(df, npartitions=5)
 
     ddf2 = ddf[ddf.a <= 5]
-    ddf2.to_parquet(fn, engine=engine)
+    ddf2.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
 
     # Pyarrow engine will not filter out empty
     # partitions unless calculate_divisions=True
@@ -1128,7 +1141,7 @@ def test_timestamp_index(tmpdir, engine, write_metadata):
     df = dd._compat.makeTimeDataFrame()
     df.index.name = "foo"
     ddf = dd.from_pandas(df, npartitions=5)
-    ddf.to_parquet(fn, engine=engine, write_metadata_file=write_metadata)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine, write_metadata_file = write_metadata))
     ddf2 = dd.read_parquet(fn, engine=engine, calculate_divisions=True)
     assert_eq(ddf, ddf2)
 
@@ -1183,7 +1196,7 @@ def test_to_parquet_pyarrow_w_inconsistent_schema_by_partition_succeeds_w_manual
             ("partition_column", pa.int64()),
         ]
     )
-    ddf.to_parquet(str(tmpdir), partition_on="partition_column", schema=schema)
+    ddf.to_parquet(DataParams_to_parquet_339(str(tmpdir), partition_on = "partition_column", schema = schema))
     ddf_after_write = (
         dd.read_parquet(str(tmpdir), calculate_divisions=False)
         .compute()
@@ -1252,7 +1265,7 @@ def test_pyarrow_schema_inference(tmpdir, index, schema):
     else:
         df = dd.from_pandas(df, npartitions=2)
 
-    df.to_parquet(tmpdir, schema=schema)
+    df.to_parquet(DataParams_to_parquet_339(tmpdir, schema = schema))
     df_out = dd.read_parquet(tmpdir, calculate_divisions=True)
     assert_eq(df, df_out)
 
@@ -1267,7 +1280,7 @@ def test_pyarrow_schema_mismatch_error(tmpdir):
     )
 
     with pytest.raises(ValueError) as rec:
-        ddf.to_parquet(str(tmpdir))
+        ddf.to_parquet(DataParams_to_parquet_339(str(tmpdir)))
 
     msg = str(rec.value)
     assert "Failed to convert partition to expected pyarrow schema" in msg
@@ -1282,7 +1295,7 @@ def test_pyarrow_schema_mismatch_explicit_schema_none(tmpdir):
     ddf = dd.from_delayed(
         [dask.delayed(df1), dask.delayed(df2)], meta=df1, verify_meta=False
     )
-    ddf.to_parquet(str(tmpdir), schema=None)
+    ddf.to_parquet(DataParams_to_parquet_339(str(tmpdir), schema = None))
     res = dd.read_parquet(tmpdir)
     sol = pd.concat([df1, df2])
     # Only checking that the data was written correctly, we don't care about
@@ -1302,7 +1315,7 @@ def test_partition_on(tmpdir, engine):
         }
     )
     d = dd.from_pandas(df, npartitions=2)
-    d.to_parquet(tmpdir, partition_on=["a1", "a2"], engine=engine)
+    d.to_parquet(DataParams_to_parquet_339(tmpdir, partition_on = ["a1", "a2"], engine = engine))
     # Note #1: Cross-engine functionality is missing
     # Note #2: The index is not preserved in pyarrow when partition_on is used
     out = dd.read_parquet(
@@ -1330,7 +1343,7 @@ def test_partition_on_duplicates(tmpdir, engine):
     d = dd.from_pandas(df, npartitions=2)
 
     for _ in range(2):
-        d.to_parquet(tmpdir, partition_on=["a1", "a2"], engine=engine)
+        d.to_parquet(DataParams_to_parquet_339(tmpdir, partition_on = ["a1", "a2"], engine = engine))
 
     out = dd.read_parquet(tmpdir, engine=engine).compute()
 
@@ -1359,7 +1372,7 @@ def test_partition_on_string(tmpdir, partition_on):
             }
         )
         d = dd.from_pandas(df, npartitions=2)
-        d.to_parquet(tmpdir, partition_on=partition_on, write_index=False)
+        d.to_parquet(DataParams_to_parquet_339(tmpdir, partition_on = partition_on, write_index = False))
         out = dd.read_parquet(tmpdir, index=False, calculate_divisions=False)
 
     out = out.compute()
@@ -1377,7 +1390,7 @@ def test_filters_categorical(tmpdir, write_engine, read_engine):
         }
     )
     ddftest = dd.from_pandas(dftest, npartitions=4).set_index("dummy")
-    ddftest.to_parquet(tmpdir, partition_on="DatePart", engine=write_engine)
+    ddftest.to_parquet(DataParams_to_parquet_339(tmpdir, partition_on = "DatePart", engine = write_engine))
     ddftest_read = dd.read_parquet(
         tmpdir,
         index="dummy",
@@ -1394,7 +1407,7 @@ def test_filters(tmpdir, write_engine, read_engine):
     ddf = dd.from_pandas(df, npartitions=5)
     assert ddf.npartitions == 5
 
-    ddf.to_parquet(tmp_path, engine=write_engine, write_metadata_file=True)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path, engine = write_engine, write_metadata_file = True))
 
     a = dd.read_parquet(tmp_path, engine=read_engine, filters=[("x", ">", 4)])
     assert a.npartitions == 3
@@ -1443,8 +1456,7 @@ def test_filters_v0(tmpdir, write_engine, read_engine):
 
     # Ok with 1 partition and filters
     ddf.repartition(npartitions=1, force=True).to_parquet(
-        fn, write_index=False, engine=write_engine
-    )
+        DataParams_to_parquet_339(fn, write_index = False, engine = write_engine))
     ddf2 = dd.read_parquet(
         fn, index=False, engine=read_engine, filters=[("at", "==", "aa")]
     ).compute()
@@ -1457,12 +1469,12 @@ def test_filters_v0(tmpdir, write_engine, read_engine):
     assert_eq(ddf3, ddf[ddf["at"] == "aa"], check_index=False)
 
     # with >1 partition and no filters
-    ddf.repartition(npartitions=2, force=True).to_parquet(fn, engine=write_engine)
+    ddf.repartition(npartitions=2, force=True).to_parquet(DataParams_to_parquet_339(fn, engine = write_engine))
     ddf2 = dd.read_parquet(fn, engine=read_engine).compute()
     assert_eq(ddf2, ddf)
 
     # with >1 partition and filters
-    ddf.repartition(npartitions=2, force=True).to_parquet(fn, engine=write_engine)
+    ddf.repartition(npartitions=2, force=True).to_parquet(DataParams_to_parquet_339(fn, engine = write_engine))
     ddf2 = dd.read_parquet(
         fn, engine=read_engine, filters=[("at", "==", "aa")]
     ).compute()
@@ -1479,7 +1491,7 @@ def test_filtering_pyarrow_dataset(tmpdir, engine):
     fn = str(tmpdir)
     df = pd.DataFrame({"aa": range(100), "bb": ["cat", "dog"] * 50})
     ddf = dd.from_pandas(df, npartitions=10)
-    ddf.to_parquet(fn, write_index=False, engine=engine, write_metadata_file=True)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, write_index = False, engine = engine, write_metadata_file = True))
 
     # Filtered read
     aa_lim = 40
@@ -1503,7 +1515,7 @@ def test_filters_file_list(tmpdir, engine):
     df = pd.DataFrame({"x": range(10), "y": list("aabbccddee")})
     ddf = dd.from_pandas(df, npartitions=5)
 
-    ddf.to_parquet(str(tmpdir), engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(str(tmpdir), engine = engine))
     files = str(tmpdir.join("*.parquet"))
     ddf_out = dd.read_parquet(
         files, calculate_divisions=True, engine=engine, filters=[("x", ">", 3)]
@@ -1524,7 +1536,7 @@ def test_filters_file_list(tmpdir, engine):
     # Make sure files list can be read with filters when they don't have the same columns order
     pd.read_parquet(os.path.join(tmpdir, "part.4.parquet"), engine=engine)[
         reversed(df.columns)
-    ].to_parquet(os.path.join(tmpdir, "part.4.parquet"), engine=engine)
+    ].to_parquet(DataParams_to_parquet_339(os.path.join(tmpdir, "part.4.parquet"), engine = engine))
     ddf3 = dd.read_parquet(
         str(tmpdir.join("*.parquet")),
         calculate_divisions=True,
@@ -1542,8 +1554,8 @@ def test_pyarrow_filter_divisions(tmpdir):
     # In this case, we filter "a" <= 3 to get a sorted
     # index. Otherwise, "a" is NOT monotonically increasing.
     df = pd.DataFrame({"a": [0, 1, 10, 12, 2, 3, 8, 9], "b": range(8)}).set_index("a")
-    df.iloc[:4].to_parquet(str(tmpdir.join("file.0.parquet")), row_group_size=2)
-    df.iloc[4:].to_parquet(str(tmpdir.join("file.1.parquet")), row_group_size=2)
+    df.iloc[:4].to_parquet(DataParams_to_parquet_339(str(tmpdir.join("file.0.parquet"))), row_group_size = 2)
+    df.iloc[4:].to_parquet(DataParams_to_parquet_339(str(tmpdir.join("file.1.parquet"))), row_group_size = 2)
 
     # Only works for ArrowDatasetEngine.
     # Legacy code will not apply filters on individual row-groups
@@ -1571,7 +1583,7 @@ def test_to_parquet_lazy(tmpdir, scheduler, engine):
     df = pd.DataFrame({"a": [1, 2, 3, 4], "b": [1.0, 2.0, 3.0, 4.0]})
     df.index.name = "index"
     ddf = dd.from_pandas(df, npartitions=2)
-    value = ddf.to_parquet(tmpdir, compute=False, engine=engine)
+    value = ddf.to_parquet(DataParams_to_parquet_339(tmpdir, compute = False, engine = engine))
 
     assert hasattr(value, "dask")
     value.compute(scheduler=scheduler)
@@ -1589,7 +1601,7 @@ def test_to_parquet_calls_invalidate_cache(tmpdir, monkeypatch, compute):
 
     invalidate_cache = MagicMock()
     monkeypatch.setattr(LocalFileSystem, "invalidate_cache", invalidate_cache)
-    ddf.to_parquet(tmpdir, compute=compute)
+    ddf.to_parquet(DataParams_to_parquet_339(tmpdir, compute = compute))
     path = LocalFileSystem._strip_protocol(str(tmpdir))
     assert invalidate_cache.called
     assert invalidate_cache.call_args.args[0] == path
@@ -1609,7 +1621,7 @@ def test_parquet_select_cats(tmpdir, engine):
     )
 
     ddf = dd.from_pandas(df, 1)
-    ddf.to_parquet(fn, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
     rddf = dd.read_parquet(fn, columns=["ints"], engine=engine)
     assert list(rddf.columns) == ["ints"]
     rddf = dd.read_parquet(fn, engine=engine)
@@ -1622,7 +1634,7 @@ def test_columns_name(tmpdir, engine):
     df.columns.name = "cols"
     ddf = dd.from_pandas(df, 2)
 
-    ddf.to_parquet(tmp_path, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path, engine = engine))
     result = dd.read_parquet(tmp_path, engine=engine, index=["idx"])
     assert_eq(result, df)
 
@@ -1653,7 +1665,7 @@ def test_writing_parquet_with_compression(tmpdir, compression, engine):
     df.index.name = "index"
     ddf = dd.from_pandas(df, npartitions=3)
 
-    ddf.to_parquet(fn, compression=compression, engine=engine, write_metadata_file=True)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, compression = compression, engine = engine, write_metadata_file = True))
     out = dd.read_parquet(fn, engine=engine, calculate_divisions=True)
     assert_eq(out, ddf)
     check_compression(engine, fn, compression)
@@ -1668,12 +1680,7 @@ def test_writing_parquet_with_partition_on_and_compression(tmpdir, compression,
     ddf = dd.from_pandas(df, npartitions=3)
 
     ddf.to_parquet(
-        fn,
-        compression=compression,
-        engine=engine,
-        partition_on=["x"],
-        write_metadata_file=True,
-    )
+        DataParams_to_parquet_339(fn, compression = compression, engine = engine, partition_on = ["x"], write_metadata_file = True))
     check_compression(engine, fn, compression)
 
 
@@ -1790,13 +1797,13 @@ def test_writing_parquet_with_kwargs(tmpdir, engine):
         },
     }
 
-    ddf.to_parquet(path1, engine=engine, **write_kwargs[engine])
+    ddf.to_parquet(DataParams_to_parquet_339(path1, engine = engine), ConfigParams_to_parquet_339(write_kwargs[engine]))
     out = dd.read_parquet(path1, engine=engine, calculate_divisions=True)
     assert_eq(out, ddf, check_index=True)
 
     # Avoid race condition in pyarrow 0.8.0 on writing partitioned datasets
     with dask.config.set(scheduler="sync"):
-        ddf.to_parquet(path2, engine=engine, partition_on=["a"], **write_kwargs[engine])
+        ddf.to_parquet(DataParams_to_parquet_339(path2, engine = engine, partition_on = ["a"]), ConfigParams_to_parquet_339(write_kwargs[engine]))
     out = dd.read_parquet(path2, engine=engine).compute()
     for val in df.a.unique():
         assert set(df.b[df.a == val]) == set(out.b[out.a == val])
@@ -1806,7 +1813,7 @@ def test_writing_parquet_with_unknown_kwargs(tmpdir, engine):
     fn = str(tmpdir)
 
     with pytest.raises(TypeError):
-        ddf.to_parquet(fn, engine=engine, unknown_key="unknown_value")
+        ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine), unknown_key = "unknown_value")
 
 
 def test_to_parquet_with_get(tmpdir, engine):
@@ -1823,7 +1830,7 @@ def test_to_parquet_with_get(tmpdir, engine):
     df = pd.DataFrame({"x": ["a", "b", "c", "d"], "y": [1, 2, 3, 4]})
     ddf = dd.from_pandas(df, npartitions=2)
 
-    ddf.to_parquet(tmpdir, engine=engine, compute_kwargs={"scheduler": my_get})
+    ddf.to_parquet(DataParams_to_parquet_339(tmpdir, engine = engine, compute_kwargs = {"scheduler": my_get}))
     assert flag[0]
 
     result = dd.read_parquet(os.path.join(tmpdir, "*"), engine=engine)
@@ -1840,12 +1847,7 @@ def test_select_partitioned_column(tmpdir, engine):
     }
     df = dd.from_pandas(pd.DataFrame(d), 2)
     df.to_parquet(
-        fn,
-        compression="snappy",
-        write_index=False,
-        engine=engine,
-        partition_on=["fake_categorical1", "fake_categorical2"],
-    )
+        DataParams_to_parquet_339(fn, compression = "snappy", write_index = False, engine = engine, partition_on = ["fake_categorical1", "fake_categorical2"]))
 
     df_partitioned = dd.read_parquet(fn, engine=engine)
     df_partitioned[df_partitioned.fake_categorical1 == "A"].compute()
@@ -1863,7 +1865,7 @@ def test_arrow_partitioning(tmpdir):
     }
     pdf = pd.DataFrame(data)
     ddf = dd.from_pandas(pdf, npartitions=2)
-    ddf.to_parquet(path, write_index=False, partition_on="p")
+    ddf.to_parquet(DataParams_to_parquet_339(path, write_index = False, partition_on = "p"))
 
     ddf = dd.read_parquet(path, index=False)
 
@@ -1882,8 +1884,8 @@ def test_append_cat_fp(tmpdir, engine):
     df["x"] = df["x"].astype("category")
     ddf = dd.from_pandas(df, npartitions=1)
 
-    dd.to_parquet(ddf, path, engine=engine)
-    dd.to_parquet(ddf, path, append=True, ignore_divisions=True, engine=engine)
+    dd.to_parquet(DataParams_to_parquet_339(ddf, engine = engine), ConfigParams_to_parquet_339(path))
+    dd.to_parquet(DataParams_to_parquet_339(ddf, append = True, ignore_divisions = True, engine = engine), ConfigParams_to_parquet_339(path))
 
     d = dd.read_parquet(path, engine=engine).compute()
     assert d["x"].tolist() == ["a", "a", "b", "a", "b"] * 2
@@ -1925,7 +1927,7 @@ def test_roundtrip_arrow(tmpdir, df):
     if not df.index.name:
         df.index.name = "index"
     ddf = dd.from_pandas(df, npartitions=2)
-    dd.to_parquet(ddf, tmp_path, write_index=True)
+    dd.to_parquet(DataParams_to_parquet_339(ddf, write_index = True), ConfigParams_to_parquet_339(tmp_path))
     ddf2 = dd.read_parquet(tmp_path, calculate_divisions=True)
     assert_eq(ddf, ddf2)
 
@@ -1935,7 +1937,7 @@ def test_datasets_timeseries(tmpdir, engine):
     df = dask.datasets.timeseries(
         start="2000-01-01", end="2000-01-10", freq="1D"
     ).persist()
-    df.to_parquet(tmp_path, engine=engine)
+    df.to_parquet(DataParams_to_parquet_339(tmp_path, engine = engine))
 
     df2 = dd.read_parquet(tmp_path, engine=engine, calculate_divisions=True)
     assert_eq(df, df2)
@@ -1948,14 +1950,14 @@ def test_pathlib_path(tmpdir, engine):
     df.index.name = "index"
     ddf = dd.from_pandas(df, npartitions=2)
     path = pathlib.Path(str(tmpdir))
-    ddf.to_parquet(path, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(path, engine = engine))
     ddf2 = dd.read_parquet(path, engine=engine, calculate_divisions=True)
     assert_eq(ddf, ddf2)
 
 
 def test_read_glob_no_meta(tmpdir, write_engine, read_engine):
     tmp_path = str(tmpdir)
-    ddf.to_parquet(tmp_path, engine=write_engine)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path, engine = write_engine))
 
     ddf2 = dd.read_parquet(
         os.path.join(tmp_path, "*.parquet"),
@@ -1967,7 +1969,7 @@ def test_read_glob_no_meta(tmpdir, write_engine, read_engine):
 
 def test_read_glob_yes_meta(tmpdir, write_engine, read_engine):
     tmp_path = str(tmpdir)
-    ddf.to_parquet(tmp_path, engine=write_engine, write_metadata_file=True)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path, engine = write_engine, write_metadata_file = True))
     paths = glob.glob(os.path.join(tmp_path, "*.parquet"))
     paths.append(os.path.join(tmp_path, "_metadata"))
     ddf2 = dd.read_parquet(paths, engine=read_engine, calculate_divisions=False)
@@ -1978,7 +1980,7 @@ def test_read_glob_yes_meta(tmpdir, write_engine, read_engine):
 @pytest.mark.parametrize("remove_common", [True, False])
 def test_read_dir_nometa(tmpdir, write_engine, read_engine, divisions, remove_common):
     tmp_path = str(tmpdir)
-    ddf.to_parquet(tmp_path, engine=write_engine, write_metadata_file=True)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path, engine = write_engine, write_metadata_file = True))
     if os.path.exists(os.path.join(tmp_path, "_metadata")):
         os.unlink(os.path.join(tmp_path, "_metadata"))
     files = os.listdir(tmp_path)
@@ -1993,7 +1995,7 @@ def test_read_dir_nometa(tmpdir, write_engine, read_engine, divisions, remove_co
 
 def test_statistics_nometa(tmpdir, write_engine, read_engine):
     tmp_path = str(tmpdir)
-    ddf.to_parquet(tmp_path, engine=write_engine, write_metadata_file=False)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path, engine = write_engine, write_metadata_file = False))
 
     ddf2 = dd.read_parquet(tmp_path, engine=read_engine, calculate_divisions=True)
     assert_eq(ddf, ddf2)
@@ -2014,7 +2016,7 @@ def test_timeseries_nulls_in_schema(tmpdir, engine, schema):
     ddf2.name = ddf2.name.where(ddf2.timestamp == "2000-01-01", None)
 
     # Note: `append_row_groups` will fail with pyarrow>0.17.1 for _metadata write
-    ddf2.to_parquet(tmp_path, engine=engine, write_metadata_file=False, schema=schema)
+    ddf2.to_parquet(DataParams_to_parquet_339(tmp_path, engine = engine, write_metadata_file = False, schema = schema))
     ddf_read = dd.read_parquet(tmp_path, engine=engine)
 
     assert_eq(ddf_read, ddf2, check_divisions=False, check_index=False)
@@ -2029,7 +2031,7 @@ def test_graph_size_pyarrow(tmpdir, engine):
         start="2000-01-01", end="2000-01-02", freq="60s", partition_freq="1h"
     )
 
-    ddf1.to_parquet(fn, engine=engine)
+    ddf1.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
     ddf2 = dd.read_parquet(fn, engine=engine)
 
     assert len(pickle.dumps(ddf2.__dask_graph__())) < 25000
@@ -2039,7 +2041,7 @@ def test_getitem_optimization_multi(tmpdir, engine):
     df = pd.DataFrame({"A": [1] * 100, "B": [2] * 100, "C": [3] * 100, "D": [4] * 100})
     ddf = dd.from_pandas(df, 2)
     fn = os.path.join(str(tmpdir))
-    ddf.to_parquet(fn, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
 
     a = dd.read_parquet(fn, engine=engine)["B"]
     b = dd.read_parquet(fn, engine=engine)[["C"]]
@@ -2064,8 +2066,7 @@ def test_split_row_groups(tmpdir, engine):
 
     half = len(df) // 2
     dd.from_pandas(df.iloc[:half], npartitions=2).to_parquet(
-        tmp, engine="pyarrow", row_group_size=100
-    )
+        DataParams_to_parquet_339(tmp, engine = "pyarrow"), row_group_size = 100)
 
     ddf3 = dd.read_parquet(tmp, engine=engine, split_row_groups=True)
     assert ddf3.npartitions == 4
@@ -2076,8 +2077,7 @@ def test_split_row_groups(tmpdir, engine):
     assert ddf3.npartitions == 2
 
     dd.from_pandas(df.iloc[half:], npartitions=2).to_parquet(
-        tmp, append=True, engine="pyarrow", row_group_size=50
-    )
+        DataParams_to_parquet_339(tmp, append = True, engine = "pyarrow"), row_group_size = 50)
 
     ddf3 = dd.read_parquet(
         tmp,
@@ -2110,11 +2110,9 @@ def test_split_row_groups_int(tmpdir, split_row_groups, calculate_divisions, eng
     half = len(df) // 2
 
     dd.from_pandas(df.iloc[:half], npartitions=npartitions).to_parquet(
-        tmp, engine="pyarrow", row_group_size=row_group_size
-    )
+        DataParams_to_parquet_339(tmp, engine = "pyarrow"), row_group_size = row_group_size)
     dd.from_pandas(df.iloc[half:], npartitions=npartitions).to_parquet(
-        tmp, append=True, engine="pyarrow", row_group_size=row_group_size
-    )
+        DataParams_to_parquet_339(tmp, append = True, engine = "pyarrow"), row_group_size = row_group_size)
 
     ddf2 = dd.read_parquet(
         tmp,
@@ -2140,8 +2138,7 @@ def test_split_row_groups_int_aggregate_files(tmpdir, engine, split_row_groups):
         }
     )
     dd.from_pandas(df, npartitions=4).to_parquet(
-        str(tmpdir), engine="pyarrow", row_group_size=row_group_size, write_index=False
-    )
+        DataParams_to_parquet_339(str(tmpdir), engine = "pyarrow", write_index = False), row_group_size = row_group_size)
 
     # Read back with both `split_row_groups>1` and
     # `aggregate_files=True`
@@ -2182,7 +2179,7 @@ def test_filter_nulls(tmpdir, filters, op, length, split_row_groups, engine):
             "c": ["a", None] * 2 + [None] * 11,
         }
     )
-    df.to_parquet(path, engine="pyarrow", row_group_size=10)
+    df.to_parquet(DataParams_to_parquet_339(path, engine = "pyarrow"), row_group_size = 10)
 
     result = dd.read_parquet(
         path,
@@ -2198,7 +2195,7 @@ def test_filter_nulls(tmpdir, filters, op, length, split_row_groups, engine):
 @pytest.mark.parametrize("split_row_groups", [True, False])
 def test_filter_isna(tmpdir, split_row_groups):
     path = tmpdir.join("test.parquet")
-    pd.DataFrame({"a": [1, None] * 5 + [None] * 5}).to_parquet(path, row_group_size=10)
+    pd.DataFrame({"a": [1, None] * 5 + [None] * 5}).to_parquet(DataParams_to_parquet_339(path), row_group_size = 10)
 
     result_isna = dd.read_parquet(
         path,
@@ -2227,8 +2224,7 @@ def test_split_row_groups_filter(tmpdir, engine):
     filters = [("f", "==", search_val)]
 
     dd.from_pandas(df, npartitions=4).to_parquet(
-        tmp, append=True, engine="pyarrow", row_group_size=50
-    )
+        DataParams_to_parquet_339(tmp, append = True, engine = "pyarrow"), row_group_size = 50)
 
     ddf2 = dd.read_parquet(tmp, engine=engine)
     ddf3 = dd.read_parquet(
@@ -2252,7 +2248,7 @@ def test_optimize_getitem_and_nonblockwise(tmpdir, engine):
         {"a": [3, 4, 2], "b": [1, 2, 4], "c": [5, 4, 2], "d": [1, 2, 3]},
         index=["a", "b", "c"],
     )
-    df.to_parquet(path, engine=engine)
+    df.to_parquet(DataParams_to_parquet_339(path, engine = engine))
 
     df2 = dd.read_parquet(path, engine=engine)
     df2[["a", "b"]].rolling(3).max().compute()
@@ -2264,7 +2260,7 @@ def test_optimize_and_not(tmpdir, engine):
         {"a": [3, 4, 2], "b": [1, 2, 4], "c": [5, 4, 2], "d": [1, 2, 3]},
         index=["a", "b", "c"],
     )
-    df.to_parquet(path, engine=engine)
+    df.to_parquet(DataParams_to_parquet_339(path, engine = engine))
 
     df2 = dd.read_parquet(path, engine=engine)
     df2a = df2["a"].groupby(df2["c"]).first().to_delayed()
@@ -2286,7 +2282,7 @@ def test_optimize_and_not(tmpdir, engine):
 def test_split_adaptive_empty(tmpdir, write_engine, read_engine):
     df = pd.DataFrame({"a": pd.Series(dtype="int"), "b": pd.Series(dtype="float")})
     ddf1 = dd.from_pandas(df, npartitions=1)
-    ddf1.to_parquet(tmpdir, engine=write_engine, write_metadata_file=True)
+    ddf1.to_parquet(DataParams_to_parquet_339(tmpdir, engine = write_engine, write_metadata_file = True))
     ddf2 = dd.read_parquet(
         tmpdir,
         engine=read_engine,
@@ -2311,12 +2307,7 @@ def test_split_adaptive_files(tmpdir, blocksize, partition_on, metadata):
     ddf1 = dd.from_pandas(df1, npartitions=9)
 
     ddf1.to_parquet(
-        str(tmpdir),
-        engine="pyarrow",
-        partition_on=partition_on,
-        write_metadata_file=metadata,
-        write_index=False,
-    )
+        DataParams_to_parquet_339(str(tmpdir), engine = "pyarrow", partition_on = partition_on, write_metadata_file = metadata, write_index = False))
 
     aggregate_files = partition_on if partition_on else True
     ddf2 = dd.read_parquet(
@@ -2362,11 +2353,7 @@ def test_split_adaptive_aggregate_files(tmpdir, aggregate_files):
     ddf1 = dd.from_pandas(df1, npartitions=9)
 
     ddf1.to_parquet(
-        str(tmpdir),
-        engine="pyarrow",
-        partition_on=partition_on,
-        write_index=False,
-    )
+        DataParams_to_parquet_339(str(tmpdir), engine = "pyarrow", partition_on = partition_on, write_index = False))
     ddf2 = dd.read_parquet(
         str(tmpdir),
         engine="pyarrow",
@@ -2406,11 +2393,7 @@ def test_split_adaptive_blocksize(tmpdir, blocksize, engine, metadata):
 
     ddf1 = dd.from_pandas(df, npartitions=nparts)
     ddf1.to_parquet(
-        str(tmpdir),
-        engine="pyarrow",
-        row_group_size=row_group_size,
-        write_metadata_file=metadata,
-    )
+        DataParams_to_parquet_339(str(tmpdir), engine = "pyarrow", write_metadata_file = metadata), row_group_size = row_group_size)
 
     if metadata:
         path = str(tmpdir)
@@ -2463,11 +2446,7 @@ def test_blocksize(tmpdir, blocksize, engine, metadata):
 
     ddf1 = dd.from_pandas(df, npartitions=nparts)
     ddf1.to_parquet(
-        str(tmpdir),
-        engine="pyarrow",
-        row_group_size=row_group_size,
-        write_metadata_file=metadata,
-    )
+        DataParams_to_parquet_339(str(tmpdir), engine = "pyarrow", write_metadata_file = metadata), row_group_size = row_group_size)
 
     if metadata:
         path = str(tmpdir)
@@ -2491,7 +2470,7 @@ def test_blocksize(tmpdir, blocksize, engine, metadata):
         # Should get adaptive partitioning
         assert ddf2.npartitions > ddf1.npartitions
         outpath = os.path.join(str(tmpdir), "out")
-        ddf2.to_parquet(outpath, engine=engine)
+        ddf2.to_parquet(DataParams_to_parquet_339(outpath, engine = engine))
         for i in range(ddf2.npartitions):
             fn = os.path.join(outpath, f"part.{i}.parquet")
             md = pq.ParquetFile(fn).metadata
@@ -2508,7 +2487,7 @@ def test_roundtrip_pandas_blocksize(tmpdir, write_engine, read_engine):
     path = str(tmpdir.join("test.parquet"))
     pdf = df.copy()
     pdf.index.name = "index"
-    pdf.to_parquet(path, engine=write_engine)
+    pdf.to_parquet(DataParams_to_parquet_339(path, engine = write_engine))
 
     ddf_read = dd.read_parquet(
         path,
@@ -2536,8 +2515,7 @@ def test_filter_nonpartition_columns(
     )
     ddf_write = dd.from_pandas(df_write, npartitions=4)
     ddf_write.to_parquet(
-        tmpdir, write_index=False, partition_on=["id"], engine=write_engine
-    )
+        DataParams_to_parquet_339(tmpdir, write_index = False, partition_on = ["id"], engine = write_engine))
     ddf_read = dd.read_parquet(
         tmpdir,
         index=False,
@@ -2563,7 +2541,7 @@ def test_pandas_metadata_nullable_pyarrow(tmpdir):
         ),
         npartitions=1,
     )
-    ddf1.to_parquet(tmpdir)
+    ddf1.to_parquet(DataParams_to_parquet_339(tmpdir))
     ddf2 = dd.read_parquet(tmpdir, calculate_divisions=True)
 
     assert_eq(ddf1, ddf2, check_index=False)
@@ -2654,7 +2632,7 @@ def test_arrow_to_pandas(tmpdir, engine):
 
     df = pd.DataFrame({"A": [pd.Timestamp("2000-01-01")]})
     path = str(tmpdir.join("test.parquet"))
-    df.to_parquet(path, engine=engine)
+    df.to_parquet(DataParams_to_parquet_339(path, engine = engine))
 
     arrow_to_pandas = {"timestamp_as_object": True}
     expect = pq.ParquetFile(path).read().to_pandas(**arrow_to_pandas)
@@ -2680,8 +2658,8 @@ def test_partitioned_column_overlap(tmpdir, engine, write_cols):
     _df2 = pd.DataFrame({"part": "b", "kind": "x", "col": range(5)})
     df1 = _df1[write_cols]
     df2 = _df2[write_cols]
-    df1.to_parquet(path0, index=False)
-    df2.to_parquet(path1, index=False)
+    df1.to_parquet(DataParams_to_parquet_339(path0), index = False)
+    df2.to_parquet(DataParams_to_parquet_339(path1), index = False)
 
     path = str(tmpdir)
 
@@ -2742,8 +2720,8 @@ def test_pyarrow_directory_partitioning(tmpdir):
     path2 = os.path.join(path2, "data.parquet")
     _df1 = pd.DataFrame({"part": "a", "col": range(5)})
     _df2 = pd.DataFrame({"part": "b", "col": range(5)})
-    _df1.to_parquet(path1)
-    _df2.to_parquet(path2)
+    _df1.to_parquet(DataParams_to_parquet_339(path1))
+    _df2.to_parquet(DataParams_to_parquet_339(path2))
 
     # Check results
     expect = pd.concat([_df1, _df2], ignore_index=True)
@@ -2769,7 +2747,7 @@ def test_partitioned_preserve_index(tmpdir, write_engine, read_engine):
     ).set_index("myindex")
     data.index.name = None
     df1 = dd.from_pandas(data, npartitions=npartitions)
-    df1.to_parquet(tmp, partition_on="B", engine=write_engine)
+    df1.to_parquet(DataParams_to_parquet_339(tmp, partition_on = "B", engine = write_engine))
 
     expect = data[data["B"] == 1]
     got = dd.read_parquet(tmp, engine=read_engine, filters=[("B", "==", 1)])
@@ -2780,7 +2758,7 @@ def test_from_pandas_preserve_none_index(tmpdir, engine):
     fn = str(tmpdir.join("test.parquet"))
     df = pd.DataFrame({"a": [1, 2], "b": [4, 5], "c": [6, 7]}).set_index("c")
     df.index.name = None
-    df.to_parquet(fn, engine=engine, index=True)
+    df.to_parquet(DataParams_to_parquet_339(fn, engine = engine), index = True)
     expect = pd.read_parquet(fn)
     got = dd.read_parquet(fn, engine=engine)
     assert_eq(expect, got)
@@ -2791,7 +2769,7 @@ def test_multi_partition_none_index_false(tmpdir, engine):
     ddf1 = ddf.reset_index(drop=True)
     for i, part in enumerate(ddf1.partitions):
         path = tmpdir.join(f"test.{i}.parquet")
-        part.compute().to_parquet(str(path), engine=engine)
+        part.compute().to_parquet(DataParams_to_parquet_339(str(path), engine = engine))
 
     # Read back with index=False
     ddf2 = dd.read_parquet(str(tmpdir), index=False, engine=engine)
@@ -2802,7 +2780,7 @@ def test_from_pandas_preserve_none_rangeindex(tmpdir, write_engine, read_engine)
     # See GitHub Issue#6348
     fn = str(tmpdir.join("test.parquet"))
     df0 = pd.DataFrame({"t": [1, 2, 3]}, index=pd.RangeIndex(start=1, stop=4))
-    df0.to_parquet(fn, engine=write_engine)
+    df0.to_parquet(DataParams_to_parquet_339(fn, engine = write_engine))
 
     df1 = dd.read_parquet(fn, engine=read_engine)
     assert_eq(df0, df1.compute())
@@ -2820,19 +2798,19 @@ def test_illegal_column_name(tmpdir, engine):
     # If we don't want to preserve the None index name, the
     # write should work, but the user should be warned
     with pytest.warns(UserWarning, match=null_name):
-        ddf.to_parquet(fn, engine=engine, write_index=False)
+        ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine, write_index = False))
 
     # If we do want to preserve the None index name, should
     # get a ValueError for having an illegal column name
     with pytest.raises(ValueError) as e:
-        ddf.to_parquet(fn, engine=engine)
+        ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
     assert null_name in str(e.value)
 
 
 def test_divisions_with_null_partition(tmpdir, engine):
     df = pd.DataFrame({"a": [1, 2, None, None], "b": [1, 2, 3, 4]})
     ddf = dd.from_pandas(df, npartitions=2)
-    ddf.to_parquet(str(tmpdir), engine=engine, write_index=False)
+    ddf.to_parquet(DataParams_to_parquet_339(str(tmpdir), engine = engine, write_index = False))
 
     ddf_read = dd.read_parquet(str(tmpdir), engine=engine, index="a")
     assert ddf_read.divisions == (None, None, None)
@@ -2844,7 +2822,7 @@ def test_pyarrow_dataset_simple(tmpdir, engine):
     df = pd.DataFrame({"a": [4, 5, 6], "b": ["a", "b", "b"]})
     df = df.set_index("a", drop=True)
     ddf = dd.from_pandas(df, npartitions=2)
-    ddf.to_parquet(fn, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
     read_df = dd.read_parquet(fn, engine="pyarrow", calculate_divisions=True)
     read_df.compute()
     assert_eq(ddf, read_df)
@@ -2857,7 +2835,7 @@ def test_pyarrow_dataset_partitioned(tmpdir, engine, test_filter):
     df = pd.DataFrame({"a": [4, 5, 6], "b": ["a", "b", "b"]})
     df["b"] = df["b"].astype("category")
     ddf = dd.from_pandas(df, npartitions=2)
-    ddf.to_parquet(fn, engine=engine, partition_on="b", write_metadata_file=True)
+    ddf.to_parquet(DataParams_to_parquet_339(fn, engine = engine, partition_on = "b", write_metadata_file = True))
     read_df = dd.read_parquet(
         fn,
         engine="pyarrow",
@@ -2881,7 +2859,7 @@ def test_null_partition_pyarrow(tmpdir, scheduler):
         }
     )
     ddf = dd.from_pandas(df, npartitions=1)
-    ddf.to_parquet(str(tmpdir), partition_on="id")
+    ddf.to_parquet(DataParams_to_parquet_339(str(tmpdir), partition_on = "id"))
     fns = glob.glob(os.path.join(tmpdir, "id=*/*.parquet"))
     assert len(fns) == 3
 
@@ -2915,7 +2893,7 @@ def test_pyarrow_dataset_read_from_paths(tmpdir):
     df = pd.DataFrame({"a": [4, 5, 6], "b": ["a", "b", "b"]})
     df["b"] = df["b"].astype("category")
     ddf = dd.from_pandas(df, npartitions=2)
-    ddf.to_parquet(fn, partition_on="b")
+    ddf.to_parquet(DataParams_to_parquet_339(fn, partition_on = "b"))
 
     read_df_1 = dd.read_parquet(fn, filters=[("b", "==", "a")], read_from_paths=False)
 
@@ -2938,7 +2916,7 @@ def test_pyarrow_dataset_filter_partitioned(tmpdir, split_row_groups):
     )
     df["b"] = df["b"].astype("category")
     ddf = dd.from_pandas(df, npartitions=2)
-    ddf.to_parquet(fn, partition_on=["b", "c"])
+    ddf.to_parquet(DataParams_to_parquet_339(fn, partition_on = ["b", "c"]))
 
     # Filter on a a non-partition column
     read_df = dd.read_parquet(
@@ -2960,7 +2938,7 @@ def test_pyarrow_dataset_filter_on_partitioned(tmpdir, engine):
         lambda i: df.iloc[i : i + 1],
         range(7),
     )
-    ddf.to_parquet(tmpdir, engine=engine, partition_on=["part"])
+    ddf.to_parquet(DataParams_to_parquet_339(tmpdir, engine = engine, partition_on = ["part"]))
 
     # Check that List[Tuple] filters are applied
     read_ddf = dd.read_parquet(
@@ -2998,11 +2976,7 @@ def test_parquet_pyarrow_write_empty_metadata(tmpdir):
 
     df = dd.from_delayed([df_a, df_b, df_c])
     df.to_parquet(
-        tmpdir,
-        partition_on=["x"],
-        append=False,
-        write_metadata_file=True,
-    )
+        DataParams_to_parquet_339(tmpdir, partition_on = ["x"], append = False, write_metadata_file = True))
 
     # Check that metadata files where written
     files = os.listdir(tmpdir)
@@ -3032,11 +3006,7 @@ def test_parquet_pyarrow_write_empty_metadata_append(tmpdir):
 
     df1 = dd.from_delayed([df_a, df_b])
     df1.to_parquet(
-        tmpdir,
-        partition_on=["x"],
-        append=False,
-        write_metadata_file=True,
-    )
+        DataParams_to_parquet_339(tmpdir, partition_on = ["x"], append = False, write_metadata_file = True))
 
     df_c = dask.delayed(pd.DataFrame.from_dict)(
         {"x": [], "y": []}, dtype=("int64", "int64")
@@ -3047,12 +3017,7 @@ def test_parquet_pyarrow_write_empty_metadata_append(tmpdir):
 
     df2 = dd.from_delayed([df_c, df_d])
     df2.to_parquet(
-        tmpdir,
-        partition_on=["x"],
-        append=True,
-        ignore_divisions=True,
-        write_metadata_file=True,
-    )
+        DataParams_to_parquet_339(tmpdir, partition_on = ["x"], append = True, ignore_divisions = True, write_metadata_file = True))
 
 
 @PYARROW_MARK
@@ -3065,11 +3030,7 @@ def test_create_metadata_file(tmpdir, partition_on):
     df1.index.name = "myindex"
     ddf1 = dd.from_pandas(df1, npartitions=10)
     ddf1.to_parquet(
-        tmpdir,
-        write_metadata_file=False,
-        partition_on=partition_on,
-        engine="pyarrow",
-    )
+        DataParams_to_parquet_339(tmpdir, write_metadata_file = False, partition_on = partition_on, engine = "pyarrow"))
 
     # Add global _metadata file
     if partition_on:
@@ -3125,14 +3086,14 @@ def test_read_write_overwrite_is_true(tmpdir, engine):
         npartitions=5,
     )
     ddf = ddf.reset_index(drop=True)
-    dd.to_parquet(ddf, tmpdir, engine=engine, overwrite=True)
+    dd.to_parquet(DataParams_to_parquet_339(ddf, engine = engine, overwrite = True), ConfigParams_to_parquet_339(tmpdir))
 
     # Keep the contents of the DataFrame constant but change the # of partitions
     ddf2 = ddf.repartition(npartitions=3)
 
     # Overwrite the existing Dataset with the new dataframe and evaluate
     # the number of files against the number of dask partitions
-    dd.to_parquet(ddf2, tmpdir, engine=engine, overwrite=True)
+    dd.to_parquet(DataParams_to_parquet_339(ddf2, engine = engine, overwrite = True), ConfigParams_to_parquet_339(tmpdir))
 
     # Assert the # of files written are identical to the number of
     # Dask DataFrame partitions (we exclude _metadata and _common_metadata)
@@ -3157,7 +3118,7 @@ def test_read_write_partition_on_overwrite_is_true(tmpdir, engine):
     )
     df.columns = ["A", "B", "C"]
     ddf = dd.from_pandas(df, npartitions=5)
-    dd.to_parquet(ddf, tmpdir, engine=engine, partition_on=["A", "B"], overwrite=True)
+    dd.to_parquet(DataParams_to_parquet_339(ddf, engine = engine, partition_on = ["A", "B"], overwrite = True), ConfigParams_to_parquet_339(tmpdir))
 
     # Get the total number of files and directories from the original write
     files_ = Path(tmpdir).rglob("*")
@@ -3168,7 +3129,7 @@ def test_read_write_partition_on_overwrite_is_true(tmpdir, engine):
     # Overwrite the existing Dataset with the new dataframe and evaluate
     # the number of files against the number of dask partitions
     # Get the total number of files and directories from the original write
-    dd.to_parquet(ddf2, tmpdir, engine=engine, partition_on=["A", "B"], overwrite=True)
+    dd.to_parquet(DataParams_to_parquet_339(ddf2, engine = engine, partition_on = ["A", "B"], overwrite = True), ConfigParams_to_parquet_339(tmpdir))
     files2_ = Path(tmpdir).rglob("*")
     files2 = [f.as_posix() for f in files2_]
     # After reducing the # of partitions and overwriting, we expect
@@ -3180,15 +3141,15 @@ def test_to_parquet_overwrite_adaptive_round_trip(tmpdir, engine):
     df = pd.DataFrame({"a": range(128)})
     ddf = dd.from_pandas(df, npartitions=8)
     path = os.path.join(str(tmpdir), "path")
-    ddf.to_parquet(path, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(path, engine = engine))
     ddf2 = dd.read_parquet(
         path,
         engine=engine,
         split_row_groups="adaptive",
     ).repartition(partition_size="1GB")
     path_new = os.path.join(str(tmpdir), "path_new")
-    ddf2.to_parquet(path_new, engine=engine, overwrite=True)
-    ddf2.to_parquet(path_new, engine=engine, overwrite=True)
+    ddf2.to_parquet(DataParams_to_parquet_339(path_new, engine = engine, overwrite = True))
+    ddf2.to_parquet(DataParams_to_parquet_339(path_new, engine = engine, overwrite = True))
     assert_eq(
         ddf2,
         dd.read_parquet(
@@ -3206,9 +3167,9 @@ def test_to_parquet_overwrite_raises(tmpdir, engine):
     df = pd.DataFrame({"a": range(12)})
     ddf = dd.from_pandas(df, npartitions=3)
     with pytest.raises(ValueError):
-        dd.to_parquet(ddf, "./", engine=engine, overwrite=True)
+        dd.to_parquet(DataParams_to_parquet_339(ddf, engine = engine, overwrite = True), ConfigParams_to_parquet_339("./"))
     with pytest.raises(ValueError):
-        dd.to_parquet(ddf, tmpdir, engine=engine, append=True, overwrite=True)
+        dd.to_parquet(DataParams_to_parquet_339(ddf, engine = engine, append = True, overwrite = True), ConfigParams_to_parquet_339(tmpdir))
 
 
 @pytest.mark.xfail(
@@ -3221,8 +3182,7 @@ def test_to_parquet_overwrite_files_from_read_parquet_in_same_call_raises(
     subdir = tmpdir.mkdir("subdir")
 
     dd.from_pandas(pd.DataFrame({"x": range(20)}), npartitions=2).to_parquet(
-        subdir, engine=engine
-    )
+        DataParams_to_parquet_339(subdir, engine = engine))
 
     ddf = dd.read_parquet(subdir)
 
@@ -3231,21 +3191,21 @@ def test_to_parquet_overwrite_files_from_read_parquet_in_same_call_raises(
         with pytest.raises(
             ValueError, match="same parquet file|Cannot overwrite a path"
         ):
-            ddf.to_parquet(target, overwrite=True)
+            ddf.to_parquet(DataParams_to_parquet_339(target, overwrite = True))
 
         ddf2 = ddf.assign(y=ddf.x + 1)
 
         with pytest.raises(
             ValueError, match="same parquet file|Cannot overwrite a path"
         ):
-            ddf2.to_parquet(target, overwrite=True)
+            ddf2.to_parquet(DataParams_to_parquet_339(target, overwrite = True))
 
 
 def test_to_parquet_errors_non_string_column_names(tmpdir, engine):
     df = pd.DataFrame({"x": range(10), 1: range(10)})
     ddf = dd.from_pandas(df, npartitions=2)
     with pytest.raises(ValueError, match="non-string column names"):
-        ddf.to_parquet(str(tmpdir.join("temp")), engine=engine)
+        ddf.to_parquet(DataParams_to_parquet_339(str(tmpdir.join("temp")), engine = engine))
 
 
 def test_dir_filter(tmpdir, engine):
@@ -3274,7 +3234,7 @@ def test_dir_filter(tmpdir, engine):
         }
     )
     ddf = dask.dataframe.from_pandas(df, npartitions=1)
-    ddf.to_parquet(tmpdir, partition_on="year", engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(tmpdir, partition_on = "year", engine = engine))
     ddf2 = dd.read_parquet(tmpdir, filters=[("year", "==", 2020)], engine=engine)
     ddf2["year"] = ddf2.year.astype("int64")
     assert_eq(ddf2, df[df.year == 2020])
@@ -3294,7 +3254,7 @@ def test_roundtrip_decimal_dtype(tmpdir):
     ]
     ddf1 = dd.from_pandas(pd.DataFrame(data), npartitions=1)
 
-    ddf1.to_parquet(path=tmpdir, schema={"col1": pa.decimal128(5, 2)})
+    ddf1.to_parquet(DataParams_to_parquet_339(schema = {"col1": pa.decimal128(5, 2)}), ConfigParams_to_parquet_339(path = tmpdir))
     ddf2 = dd.read_parquet(tmpdir)
 
     if pyarrow_strings_enabled():
@@ -3323,7 +3283,7 @@ def test_roundtrip_date_dtype(tmpdir):
     ]
     ddf1 = dd.from_pandas(pd.DataFrame(data), npartitions=1)
 
-    ddf1.to_parquet(path=tmpdir, schema={"col1": pa.date32()})
+    ddf1.to_parquet(DataParams_to_parquet_339(schema = {"col1": pa.date32()}), ConfigParams_to_parquet_339(path = tmpdir))
     ddf2 = dd.read_parquet(tmpdir)
 
     if pyarrow_strings_enabled():
@@ -3342,7 +3302,7 @@ def test_roundtrip_rename_columns(tmpdir, engine):
 
     path = os.path.join(str(tmpdir), "test.parquet")
     df1 = pd.DataFrame(columns=["a", "b", "c"], data=np.random.uniform(size=(10, 3)))
-    df1.to_parquet(path)
+    df1.to_parquet(DataParams_to_parquet_339(path))
 
     # read it with dask and rename columns
     ddf2 = dd.read_parquet(path, engine=engine)
@@ -3362,11 +3322,7 @@ def test_custom_metadata(tmpdir, engine):
     path = str(tmpdir)
     df = pd.DataFrame({"a": range(10), "b": range(10)})
     dd.from_pandas(df, npartitions=2).to_parquet(
-        path,
-        engine=engine,
-        custom_metadata=custom_metadata,
-        write_metadata_file=True,
-    )
+        DataParams_to_parquet_339(path, engine = engine, custom_metadata = custom_metadata, write_metadata_file = True))
 
     # Check that data is correct
     assert_eq(df, dd.read_parquet(path, engine=engine))
@@ -3387,10 +3343,7 @@ def test_custom_metadata(tmpdir, engine):
     custom_metadata = {b"pandas": b"my_new_pandas_md"}
     with pytest.raises(ValueError) as e:
         dd.from_pandas(df, npartitions=2).to_parquet(
-            path,
-            engine=engine,
-            custom_metadata=custom_metadata,
-        )
+            DataParams_to_parquet_339(path, engine = engine, custom_metadata = custom_metadata))
     assert "User-defined key/value" in str(e.value)
 
 
@@ -3404,11 +3357,9 @@ def test_ignore_metadata_file(tmpdir, engine, calculate_divisions):
     df1 = pd.DataFrame({"a": range(100), "b": ["dog", "cat"] * 50})
     ddf1 = dd.from_pandas(df1, npartitions=2)
     ddf1.to_parquet(
-        path=dataset_with_bad_metadata, engine=engine, write_metadata_file=False
-    )
+        DataParams_to_parquet_339(engine = engine, write_metadata_file = False), ConfigParams_to_parquet_339(path = dataset_with_bad_metadata))
     ddf1.to_parquet(
-        path=dataset_without_metadata, engine=engine, write_metadata_file=False
-    )
+        DataParams_to_parquet_339(engine = engine, write_metadata_file = False), ConfigParams_to_parquet_339(path = dataset_without_metadata))
 
     # Copy "bad" metadata into `dataset_with_bad_metadata`
     assert "_metadata" not in os.listdir(dataset_with_bad_metadata)
@@ -3442,8 +3393,7 @@ def test_metadata_task_size(tmpdir, engine, write_metadata_file, metadata_task_s
     df1 = pd.DataFrame({"a": range(100), "b": ["dog", "cat"] * 50})
     ddf1 = dd.from_pandas(df1, npartitions=10)
     ddf1.to_parquet(
-        path=str(tmpdir), engine=engine, write_metadata_file=write_metadata_file
-    )
+        DataParams_to_parquet_339(engine = engine, write_metadata_file = write_metadata_file), ConfigParams_to_parquet_339(path = str(tmpdir)))
 
     # Read back
     ddf2a = dd.read_parquet(
@@ -3480,11 +3430,7 @@ def test_extra_file(tmpdir, engine, partition_on):
     df = df.assign(b=df.b.astype("category"))
     ddf = dd.from_pandas(df, npartitions=2)
     ddf.to_parquet(
-        tmpdir,
-        engine=engine,
-        write_metadata_file=True,
-        partition_on=partition_on,
-    )
+        DataParams_to_parquet_339(tmpdir, engine = engine, write_metadata_file = True, partition_on = partition_on))
     open(os.path.join(tmpdir, "_SUCCESS"), "w").close()
     open(os.path.join(tmpdir, "part.0.parquet.crc"), "w").close()
     os.remove(os.path.join(tmpdir, "_metadata"))
@@ -3537,7 +3483,7 @@ def test_unsupported_extension_file(tmpdir, engine):
     # (See: https://github.com/dask/dask/issues/8349)
     fn = os.path.join(str(tmpdir), "multi.foo")
     df0 = pd.DataFrame({"a": range(10)})
-    df0.to_parquet(fn, engine=engine)
+    df0.to_parquet(DataParams_to_parquet_339(fn, engine = engine))
     assert_eq(
         df0, dd.read_parquet(fn, engine=engine, index=False, calculate_divisions=True)
     )
@@ -3550,11 +3496,7 @@ def test_unsupported_extension_dir(tmpdir, engine):
     path = str(tmpdir)
     ddf0 = dd.from_pandas(pd.DataFrame({"a": range(10)}), 1)
     ddf0.to_parquet(
-        path,
-        engine=engine,
-        name_function=lambda i: f"part.{i}.foo",
-        write_metadata_file=True,
-    )
+        DataParams_to_parquet_339(path, engine = engine, name_function = lambda i: f"part.{i}.foo", write_metadata_file = True))
     assert_eq(ddf0, dd.read_parquet(path, engine=engine, calculate_divisions=True))
 
 
@@ -3565,11 +3507,7 @@ def test_custom_filename(tmpdir, engine):
     )
     df = dd.from_pandas(pdf, npartitions=2)
     df.to_parquet(
-        fn,
-        write_metadata_file=True,
-        name_function=lambda x: f"hi-{x}.parquet",
-        engine=engine,
-    )
+        DataParams_to_parquet_339(fn, write_metadata_file = True, name_function = lambda x: f"hi-{x}.parquet", engine = engine))
 
     files = os.listdir(fn)
     assert "_common_metadata" in files
@@ -3587,21 +3525,14 @@ def test_custom_filename_works_with_pyarrow_when_append_is_true(tmpdir):
     )
     df = dd.from_pandas(pdf, npartitions=2)
     df.to_parquet(
-        fn,
-        write_metadata_file=True,
-        name_function=lambda x: f"hi-{x * 2}.parquet",
-    )
+        DataParams_to_parquet_339(fn, write_metadata_file = True, name_function = lambda x: f"hi-{x * 2}.parquet"))
 
     pdf = pd.DataFrame(
         {"num1": [33], "num2": [44]},
     )
     df = dd.from_pandas(pdf, npartitions=1)
     df.to_parquet(
-        fn,
-        name_function=lambda x: f"hi-{x * 2}.parquet",
-        append=True,
-        ignore_divisions=True,
-    )
+        DataParams_to_parquet_339(fn, name_function = lambda x: f"hi-{x * 2}.parquet", append = True, ignore_divisions = True))
     files = os.listdir(fn)
     assert "_common_metadata" in files
     assert "_metadata" in files
@@ -3624,12 +3555,12 @@ def test_throws_error_if_custom_filename_is_invalid(tmpdir, engine):
     with pytest.raises(
         ValueError, match="``name_function`` must be a callable with one argument."
     ):
-        df.to_parquet(fn, name_function="whatever.parquet", engine=engine)
+        df.to_parquet(DataParams_to_parquet_339(fn, name_function = "whatever.parquet", engine = engine))
 
     with pytest.raises(
         ValueError, match="``name_function`` must produce unique filenames."
     ):
-        df.to_parquet(fn, name_function=lambda x: "whatever.parquet", engine=engine)
+        df.to_parquet(DataParams_to_parquet_339(fn, name_function = lambda x: "whatever.parquet", engine = engine))
 
 
 def test_custom_filename_with_partition(tmpdir, engine):
@@ -3642,12 +3573,7 @@ def test_custom_filename_with_partition(tmpdir, engine):
     )
     df = dd.from_pandas(pdf, npartitions=4)
     df.to_parquet(
-        fn,
-        engine=engine,
-        partition_on=["country"],
-        name_function=lambda x: f"{x}-cool.parquet",
-        write_index=False,
-    )
+        DataParams_to_parquet_339(fn, engine = engine, partition_on = ["country"], name_function = lambda x: f"{x}-cool.parquet", write_index = False))
 
     for _, dirs, files in os.walk(fn):
         for dir in dirs:
@@ -3681,7 +3607,7 @@ def test_roundtrip_partitioned_pyarrow_dataset(tmpdir, engine):
     # Write partitioned dataset with dask
     dask_path = tmpdir.mkdir("foo-dask")
     ddf = dd.from_pandas(df, npartitions=2)
-    ddf.to_parquet(dask_path, engine=engine, partition_on=["col1"], write_index=False)
+    ddf.to_parquet(DataParams_to_parquet_339(dask_path, engine = engine, partition_on = ["col1"], write_index = False))
 
     # Write partitioned dataset with pyarrow
     pa_path = tmpdir.mkdir("foo-pyarrow")
@@ -3717,7 +3643,7 @@ def test_in_predicate_can_use_iterables(tmp_path, engine, filter_value):
     df = pd.DataFrame(
         {"A": [1, 2, 3, 4], "B": [1, 1, 2, 2]},
     )
-    df.to_parquet(path, engine=engine)
+    df.to_parquet(DataParams_to_parquet_339(path, engine = engine))
     filters = [("B", "in", filter_value)]
     result = dd.read_parquet(path, engine=engine, filters=filters)
     expected = pd.read_parquet(path, engine=engine, filters=filters)
@@ -3727,7 +3653,7 @@ def test_in_predicate_can_use_iterables(tmp_path, engine, filter_value):
     # metadata that changes the filtering code path
     ddf = dd.from_pandas(df, npartitions=2)
     path = tmp_path / "in_predicate_iterable_dask.parquet"
-    ddf.to_parquet(path, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(path, engine = engine))
     result = dd.read_parquet(path, engine=engine, filters=filters)
     expected = pd.read_parquet(path, engine=engine, filters=filters)
     assert_eq(result, expected, check_index=False)
@@ -3738,7 +3664,7 @@ def test_not_in_predicate(tmp_path, engine):
         {"A": range(8), "B": [1, 1, 2, 2, 3, 3, 4, 4]},
         npartitions=4,
     )
-    ddf.to_parquet(tmp_path, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(tmp_path, engine = engine))
     filters = [[("B", "not in", (1, 2))]]
     result = dd.read_parquet(tmp_path, engine=engine, filters=filters)
     expected = pd.read_parquet(tmp_path, engine=engine, filters=filters)
@@ -3773,7 +3699,7 @@ def test_in_predicate_requires_an_iterable(tmp_path, engine, filter_value):
     df = pd.DataFrame(
         {"A": [1, 2, 3, 4], "B": [1, 1, 2, 2]},
     )
-    df.to_parquet(path, engine=engine)
+    df.to_parquet(DataParams_to_parquet_339(path, engine = engine))
     with pytest.raises(TypeError, match="Value of 'in' filter"):
         dd.read_parquet(path, engine=engine, filters=filter_value)
 
@@ -3781,7 +3707,7 @@ def test_in_predicate_requires_an_iterable(tmp_path, engine, filter_value):
     # metadata that changes the filtering code path
     ddf = dd.from_pandas(df, npartitions=2)
     path = tmp_path / "gh_8720_dask.parquet"
-    ddf.to_parquet(path, engine=engine)
+    ddf.to_parquet(DataParams_to_parquet_339(path, engine = engine))
     with pytest.raises(TypeError, match="Value of 'in' filter"):
         dd.read_parquet(path, engine=engine, filters=filter_value)
 
@@ -3804,7 +3730,7 @@ def test_gpu_write_parquet_simple(tmpdir):
         }
     )
     ddf = dask_cudf.from_cudf(df, 3)
-    ddf.to_parquet(fn)
+    ddf.to_parquet(DataParams_to_parquet_339(fn))
     got = dask_cudf.read_parquet(fn)
     assert_eq(df, got)
 
@@ -3833,12 +3759,12 @@ def test_fsspec_to_parquet_filesystem_option(tmp_path):
 
     df = pd.DataFrame({"a": range(10)})
     fs = get_filesystem_class("memory")(use_instance_cache=False)
-    df.to_parquet(key1, filesystem=fs)
+    df.to_parquet(DataParams_to_parquet_339(key1, filesystem = fs))
 
     # read in prepared data
     ddf = dd.read_parquet(key1, filesystem=fs)
     assert_eq(ddf, df)
-    ddf.to_parquet(key2, filesystem=fs)
+    ddf.to_parquet(DataParams_to_parquet_339(key2, filesystem = fs))
 
     # make sure we didn't write to local fs
     assert len(list(tmp_path.iterdir())) == 0, "wrote to local fs"
@@ -3847,7 +3773,7 @@ def test_fsspec_to_parquet_filesystem_option(tmp_path):
     assert len(fs.ls(key2, detail=False)) == 1
 
     # ensure append functionality works
-    ddf.to_parquet(key2, append=True, filesystem=fs)
+    ddf.to_parquet(DataParams_to_parquet_339(key2, append = True, filesystem = fs))
     assert len(fs.ls(key2, detail=False)) == 2, "should have two parts"
 
     rddf = dd.read_parquet(key2, filesystem=fs)
@@ -3858,7 +3784,7 @@ def test_select_filtered_column(tmp_path, engine):
     df = pd.DataFrame({"a": range(10), "b": ["cat"] * 10})
     path = tmp_path / "test_select_filtered_column.parquet"
 
-    df.to_parquet(path, index=False, write_statistics=True)
+    df.to_parquet(DataParams_to_parquet_339(path), index = False, write_statistics = True)
     ddf = dd.read_parquet(path, engine=engine, filters=[("b", "==", "cat")])
     assert_eq(df, ddf)
     ddf = dd.read_parquet(path, engine=engine, filters=[("b", "is not", None)])
@@ -3869,7 +3795,7 @@ def test_select_filtered_column_no_stats(tmp_path, engine):
     df = pd.DataFrame({"a": range(10), "b": ["cat"] * 10})
     path = tmp_path / "test_select_filtered_column_no_stats.parquet"
 
-    df.to_parquet(path, write_statistics=False)
+    df.to_parquet(DataParams_to_parquet_339(path), write_statistics = False)
 
     ddf = dd.read_parquet(path, engine=engine, filters=[("b", "==", "cat")])
     assert_eq(df, ddf)
@@ -3886,7 +3812,7 @@ def test_read_parquet_convert_string(tmp_path, convert_string, engine):
     ).set_index("C")
 
     outfile = tmp_path / "out.parquet"
-    df.to_parquet(outfile, engine=engine)
+    df.to_parquet(DataParams_to_parquet_339(outfile, engine = engine))
 
     with dask.config.set({"dataframe.convert-string": convert_string}):
         ddf = dd.read_parquet(outfile, engine=engine)
@@ -3913,7 +3839,7 @@ def test_read_parquet_convert_string_nullable_mapper(tmp_path, engine):
     ).set_index("I")
 
     outfile = tmp_path / "out.parquet"
-    df.to_parquet(outfile, engine=engine)
+    df.to_parquet(DataParams_to_parquet_339(outfile, engine = engine))
 
     types_mapper = {
         pa.float32(): pd.Float64Dtype(),
@@ -3979,7 +3905,7 @@ def test_read_parquet_preserve_categorical_column_dtype(tmp_path):
     df = pd.DataFrame({"a": [1, 2], "b": ["x", "y"]})
 
     outdir = tmp_path / "out.parquet"
-    df.to_parquet(outdir, partition_cols=["a"])
+    df.to_parquet(DataParams_to_parquet_339(outdir), partition_cols = ["a"])
     ddf = dd.read_parquet(outdir)
 
     expected = pd.DataFrame(
@@ -3993,7 +3919,7 @@ def test_read_parquet_preserve_categorical_column_dtype(tmp_path):
 def test_dtype_backend_categoricals(tmp_path):
     df = pd.DataFrame({"a": pd.Series(["x", "y"], dtype="category"), "b": [1, 2]})
     outdir = tmp_path / "out.parquet"
-    df.to_parquet(outdir)
+    df.to_parquet(DataParams_to_parquet_339(outdir))
     ddf = dd.read_parquet(outdir, dtype_backend="pyarrow")
     pdf = pd.read_parquet(outdir, dtype_backend="pyarrow")
     # Set sort_results=False because of pandas bug up to 2.0.1
@@ -4007,7 +3933,7 @@ def test_non_categorical_partitioning_pyarrow(tmpdir, filters):
 
     df1 = pd.DataFrame({"a": range(100), "b": ["cat", "dog"] * 50})
     ddf1 = dd.from_pandas(df1, npartitions=2)
-    ddf1.to_parquet(path=tmpdir, partition_on=["b"], write_index=False)
+    ddf1.to_parquet(DataParams_to_parquet_339(partition_on = ["b"], write_index = False), ConfigParams_to_parquet_339(path = tmpdir))
 
     schema = pa.schema([("b", pa.string())])
     partitioning = dict(flavor="hive", schema=schema)
@@ -4024,7 +3950,7 @@ def test_non_categorical_partitioning_pyarrow(tmpdir, filters):
 @PYARROW_MARK
 def test_read_parquet_lists_not_converting(tmpdir):
     df = pd.DataFrame({"a": [1, 2], "b": [{"a": 2}, {"a": 4}]})
-    df.to_parquet(tmpdir + "test.parquet")
+    df.to_parquet(DataParams_to_parquet_339(tmpdir + "test.parquet"))
     result = dd.read_parquet(tmpdir + "test.parquet").compute()
     assert_eq(df, result)
 
@@ -4032,7 +3958,7 @@ def test_read_parquet_lists_not_converting(tmpdir):
 @PYARROW_MARK
 def test_parquet_string_roundtrip(tmpdir):
     pdf = pd.DataFrame({"a": ["a", "b", "c"]}, dtype="string[pyarrow]")
-    pdf.to_parquet(tmpdir + "string.parquet")
+    pdf.to_parquet(DataParams_to_parquet_339(tmpdir + "string.parquet"))
     if not pyarrow_strings_enabled():
         # If auto pyarrow strings aren't enabled, we match pandas behavior.
         # Pandas currently doesn't roundtrip `string[pyarrow]` through parquet.
@@ -4051,7 +3977,7 @@ def test_parquet_botocore_exception(tmpdir):
     from botocore.exceptions import BotoCoreError
 
     pdf = pd.DataFrame({"a": ["a", "b", "c"]}, dtype="string[pyarrow]")
-    pdf.to_parquet(tmpdir + "string.parquet")
+    pdf.to_parquet(DataParams_to_parquet_339(tmpdir + "string.parquet"))
 
     def mock_get_engine(engine):
         raise BotoCoreError
diff --git a/dask/tests/test_distributed.py b/dask/tests/test_distributed.py
index ec6beff23..a6b5bbd5e 100644
--- a/dask/tests/test_distributed.py
+++ b/dask/tests/test_distributed.py
@@ -58,6 +58,27 @@ ignore_sync_scheduler_warning = pytest.mark.filterwarnings(
     "ignore:Running on a single-machine scheduler when a distributed client "
     "is active might lead to unexpected results."
 )
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 
 def test_can_import_client():
@@ -540,7 +561,7 @@ def test_blockwise_dataframe_io(c, tmpdir, io, from_futures):
 
     if io == "parquet":
         pytest.importorskip("pyarrow")
-        ddf0.to_parquet(str(tmpdir))
+        ddf0.to_parquet(DataParams_to_parquet_339(str(tmpdir)))
         ddf = dd.read_parquet(str(tmpdir))
     elif io == "csv":
         ddf0.to_csv(str(tmpdir), index=False)
@@ -565,7 +586,7 @@ async def test_client_compute_parquet(c, s, a, b, tmpdir):
     ddf0 = dd.from_pandas(df, npartitions=3)
     with dask.config.set({"admin.async-client-fallback": "sync"}):
         with pytest.warns(UserWarning, match="asynchronous"):
-            ddf0.to_parquet(str(tmpdir))
+            ddf0.to_parquet(DataParams_to_parquet_339(str(tmpdir)))
 
         ddf = dd.read_parquet(str(tmpdir))
         await c.gather(c.compute(ddf))
@@ -573,7 +594,7 @@ async def test_client_compute_parquet(c, s, a, b, tmpdir):
             dask.compute(ddf)
     with dask.config.set({"admin.async-client-fallback": None}):
         with pytest.raises(RuntimeError, match="asynchronous"):
-            ddf0.to_parquet(str(tmpdir))
+            ddf0.to_parquet(DataParams_to_parquet_339(str(tmpdir)))
         with pytest.raises(RuntimeError, match="asynchronous"):
             dask.compute(ddf)
 
diff --git a/dask/tests/test_layers.py b/dask/tests/test_layers.py
index 80241958b..03a5c7ca4 100644
--- a/dask/tests/test_layers.py
+++ b/dask/tests/test_layers.py
@@ -32,6 +32,27 @@ class SchedulerImportCheck(SchedulerPlugin):
             else:
                 # Manually remove the target library
                 sys.modules.pop(mod)
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 
 def test_array_chunk_shape_dep():
@@ -140,8 +161,7 @@ def _pq_pyarrow(tmpdir):
     dd = pytest.importorskip("dask.dataframe")
 
     dd.from_pandas(pd.DataFrame({"a": range(10)}), npartitions=2).to_parquet(
-        str(tmpdir)
-    )
+        DataParams_to_parquet_339(str(tmpdir)))
     filters = [("a", "<=", 2)]
 
     ddf1 = dd.read_parquet(str(tmpdir), filters=filters)
diff --git a/dask/tests/test_spark_compat.py b/dask/tests/test_spark_compat.py
index 24800a230..6b1a3beab 100644
--- a/dask/tests/test_spark_compat.py
+++ b/dask/tests/test_spark_compat.py
@@ -30,6 +30,27 @@ pytestmark = [
     # we only test with pyarrow strings and pandas 2.0
     pytest.mark.skip_with_pyarrow_strings,  # pyspark doesn't support pandas 2.0
 ]
+class DataParams_to_parquet_339:
+    def __init__(self, df, compression = "snappy", write_index = True, append = False, overwrite = False, ignore_divisions = False, partition_on = None, custom_metadata = None, write_metadata_file = None, compute = True, compute_kwargs = None, schema = "infer", name_function = None, filesystem = None, engine = None):
+        self.df = df
+        self.compression = compression
+        self.write_index = write_index
+        self.append = append
+        self.overwrite = overwrite
+        self.ignore_divisions = ignore_divisions
+        self.partition_on = partition_on
+        self.custom_metadata = custom_metadata
+        self.write_metadata_file = write_metadata_file
+        self.compute = compute
+        self.compute_kwargs = compute_kwargs
+        self.schema = schema
+        self.name_function = name_function
+        self.filesystem = filesystem
+        self.engine = engine
+class ConfigParams_to_parquet_339:
+    def __init__(self, path, storage_options = None):
+        self.path = path
+        self.storage_options = storage_options
 
 
 @pytest.fixture(
@@ -115,7 +136,7 @@ def test_roundtrip_parquet_dask_to_spark(spark_session, npartitions, tmpdir, eng
     tmpdir = str(tmpdir)
     ddf = dd.from_pandas(pdf, npartitions=npartitions)
 
-    ddf.to_parquet(tmpdir, engine=engine, write_index=False)
+    ddf.to_parquet(DataParams_to_parquet_339(tmpdir, engine = engine, write_index = False))
 
     sdf = spark_session.read.parquet(tmpdir)
     sdf = sdf.toPandas()
