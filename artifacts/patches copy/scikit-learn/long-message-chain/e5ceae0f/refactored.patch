diff --git a/sklearn/impute/_base.py b/sklearn/impute/_base.py
index 689ba8ace..feb080d61 100644
--- a/sklearn/impute/_base.py
+++ b/sklearn/impute/_base.py
@@ -18,6 +18,7 @@ from ..utils._param_validation import MissingValues, StrOptions
 from ..utils.fixes import _mode
 from ..utils.sparsefuncs import _get_median
 from ..utils.validation import (
+from codecarbon import EmissionsTracker
     FLOAT_DTYPES,
     _check_feature_names_in,
     _check_n_features,
@@ -570,7 +571,11 @@ class SimpleImputer(_BaseImputer):
         elif strategy == "constant":
             # TODO(1.8): Remove FutureWarning and add `np.nan` as a statistic
             # for empty features to drop them later.
-            if not self.keep_empty_features and ma.getmask(masked_X).all(axis=0).any():
+            with EmissionsTracker(project_name='scikit-learn-benchmark', experiment_id='scikit-learn_LMC001_e5ceae0f', output_file='/root/ecooptimizer_benchmark/emissions/scikit-learn/long-message-chain/e5ceae0f_refactored.csv') as tracker:
+                intermediate_0 = ma.getmask(masked_X)
+                intermediate_1 = intermediate_0.all(axis=0)
+                _carbon_wrapped_bde15738 = intermediate_1.any()
+            if not self.keep_empty_features and _carbon_wrapped_bde15738:
                 warnings.warn(
                     "Currently, when `keep_empty_feature=False` and "
                     '`strategy="constant"`, empty features are not dropped. '
