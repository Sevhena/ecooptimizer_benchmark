diff --git a/nltk/ccg/lexicon.py b/nltk/ccg/lexicon.py
index 699d1d82b..e5a04ff02 100644
--- a/nltk/ccg/lexicon.py
+++ b/nltk/ccg/lexicon.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 # Natural Language Toolkit: Combinatory Categorial Grammar
 #
 # Copyright (C) 2001-2025 NLTK Project
@@ -138,13 +139,14 @@ def matchBrackets(string):
     rest = string[1:]
     inside = "("
 
-    while rest != "" and not rest.startswith(")"):
-        if rest.startswith("("):
-            (part, rest) = matchBrackets(rest)
-            inside = inside + part
-        else:
-            inside = inside + rest[0]
-            rest = rest[1:]
+    with EmissionsTracker(project_name='nltk-benchmark', experiment_id='nltk_SCL001_5829a76e', output_file='/root/ecooptimizer_benchmark/emissions/nltk/string-concat-loop/5829a76e.csv') as tracker:
+        while rest != "" and not rest.startswith(")"):
+            if rest.startswith("("):
+                (part, rest) = matchBrackets(rest)
+                inside = inside + part
+            else:
+                inside = inside + rest[0]
+                rest = rest[1:]
     if rest.startswith(")"):
         return (inside + ")", rest[1:])
     raise AssertionError("Unmatched bracket in string '" + string + "'")
