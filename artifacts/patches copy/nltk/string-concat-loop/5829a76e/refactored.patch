diff --git a/nltk/ccg/lexicon.py b/nltk/ccg/lexicon.py
index 699d1d82b..1fd3d44a0 100644
--- a/nltk/ccg/lexicon.py
+++ b/nltk/ccg/lexicon.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 # Natural Language Toolkit: Combinatory Categorial Grammar
 #
 # Copyright (C) 2001-2025 NLTK Project
@@ -136,15 +137,17 @@ def matchBrackets(string):
     the input.
     """
     rest = string[1:]
-    inside = "("
+    inside = ['(']
 
-    while rest != "" and not rest.startswith(")"):
-        if rest.startswith("("):
-            (part, rest) = matchBrackets(rest)
-            inside = inside + part
-        else:
-            inside = inside + rest[0]
-            rest = rest[1:]
+    with EmissionsTracker(project_name='nltk-benchmark', experiment_id='nltk_SCL001_5829a76e', output_file='/root/ecooptimizer_benchmark/emissions/nltk/string-concat-loop/5829a76e_refactored.csv') as tracker:
+        while rest != "" and not rest.startswith(")"):
+            if rest.startswith("("):
+                (part, rest) = matchBrackets(rest)
+                inside = [inside + part]
+            else:
+                inside.append(rest[0])
+                rest = rest[1:]
+        inside = ''.join(inside)
     if rest.startswith(")"):
         return (inside + ")", rest[1:])
     raise AssertionError("Unmatched bracket in string '" + string + "'")
@@ -335,4 +338,4 @@ openccg_tinytiny = fromstring(
     see => TransVpl
     sees => TransVsg
     """
-)
+)
\ No newline at end of file
