diff --git a/nltk/util.py b/nltk/util.py
index 411e3132f..853317b4d 100644
--- a/nltk/util.py
+++ b/nltk/util.py
@@ -28,6 +28,7 @@ from urllib.request import (
 
 from nltk.collections import *
 from nltk.internals import deprecated, raise_unorderable_types, slice_bounds
+from codecarbon import EmissionsTracker
 
 ######################################################################
 # Short usage message
@@ -159,17 +160,18 @@ def cut_string(s, width=70):
 
     abs_width = abs(width)
     max_chars = len(s)
-    while width_sofar < abs_width and chars_sofar < max_chars:
-        if width < 0:
-            char = s[-(chars_sofar + 1)]
-            result = char + result
-        else:
-            char = s[chars_sofar]
-            result = result + char
-
-        chars_sofar += 1
-        if not unicodedata.combining(char):
-            width_sofar += 1
+    with EmissionsTracker(project_name='nltk-benchmark', experiment_id='nltk_SCL001_48b77d42', output_file='/root/ecooptimizer_benchmark/emissions/nltk/string-concat-loop/48b77d42.csv') as tracker:
+        while width_sofar < abs_width and chars_sofar < max_chars:
+            if width < 0:
+                char = s[-(chars_sofar + 1)]
+                result = char + result
+            else:
+                char = s[chars_sofar]
+                result = result + char
+    
+            chars_sofar += 1
+            if not unicodedata.combining(char):
+                width_sofar += 1
 
     return result
 
