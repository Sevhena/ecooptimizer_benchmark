diff --git a/nltk/featstruct.py b/nltk/featstruct.py
index d6c339a98..7fd6e31e8 100644
--- a/nltk/featstruct.py
+++ b/nltk/featstruct.py
@@ -1,3 +1,4 @@
+from codecarbon import track_emissions
 # Natural Language Toolkit: Feature Structures
 #
 # Copyright (C) 2001-2025 NLTK Project
@@ -569,6 +570,19 @@ class FeatStruct(SubstituteBindingsI):
 # Mutation: disable if frozen.
 _FROZEN_ERROR = "Frozen FeatStructs may not be modified."
 _FROZEN_NOTICE = "\n%sIf self is frozen, raise ValueError."
+class DataParams__unify_feature_values_1580:
+    def __init__(self, fname, fval1, fval2, bindings, forward, trace, fail, fs_class):
+        self.fname = fname
+        self.fval1 = fval1
+        self.fval2 = fval2
+        self.bindings = bindings
+        self.forward = forward
+        self.trace = trace
+        self.fail = fail
+        self.fs_class = fs_class
+class ConfigParams__unify_feature_values_1580:
+    def __init__(self, fpath):
+        self.fpath = fpath
 
 
 def _check_frozen(method, indent=""):
@@ -1527,16 +1541,7 @@ def _destructively_unify(
         for fname, fval2 in sorted(fstruct2.items()):
             if fname in fstruct1:
                 fstruct1[fname] = _unify_feature_values(
-                    fname,
-                    fstruct1[fname],
-                    fval2,
-                    bindings,
-                    forward,
-                    trace,
-                    fail,
-                    fs_class,
-                    path + (fname,),
-                )
+                    DataParams__unify_feature_values_1580(fname, fstruct1[fname], fval2, bindings, forward, trace, fail, fs_class), ConfigParams__unify_feature_values_1580(path + (fname,)))
             else:
                 fstruct1[fname] = fval2
 
@@ -1551,16 +1556,7 @@ def _destructively_unify(
         # Unify corresponding values in fstruct1 and fstruct2.
         for findex in range(len(fstruct1)):
             fstruct1[findex] = _unify_feature_values(
-                findex,
-                fstruct1[findex],
-                fstruct2[findex],
-                bindings,
-                forward,
-                trace,
-                fail,
-                fs_class,
-                path + (findex,),
-            )
+                DataParams__unify_feature_values_1580(findex, fstruct1[findex], fstruct2[findex], bindings, forward, trace, fail, fs_class), ConfigParams__unify_feature_values_1580(path + (findex,)))
 
         return fstruct1  # Contains the unified value.
 
@@ -1575,9 +1571,9 @@ def _destructively_unify(
     raise TypeError("Expected mappings or sequences")
 
 
+@track_emissions(project_name='nltk-benchmark', experiment_id='nltk_R0913_1b71d437', output_file='/root/ecooptimizer_benchmark/emissions/nltk/too-many-arguments/1b71d437_refactored.csv')
 def _unify_feature_values(
-    fname, fval1, fval2, bindings, forward, trace, fail, fs_class, fpath
-):
+    data_params, config_params):
     """
     Attempt to unify ``fval1`` and and ``fval2``, and return the
     resulting unified value.  The method of unification will depend on
@@ -1594,72 +1590,72 @@ def _unify_feature_values(
       5. If they're both base values, then unify them.  By default,
          this will succeed if they are equal, and fail otherwise.
     """
-    if trace:
-        _trace_unify_start(fpath, fval1, fval2)
+    if data_params.trace:
+        _trace_unify_start(config_params.fpath, data_params.fval1, data_params.fval2)
 
     # Look up the "canonical" copy of fval1 and fval2
-    while id(fval1) in forward:
-        fval1 = forward[id(fval1)]
-    while id(fval2) in forward:
-        fval2 = forward[id(fval2)]
+    while id(data_params.fval1) in data_params.forward:
+        data_params.fval1 = data_params.forward[id(data_params.fval1)]
+    while id(data_params.fval2) in data_params.forward:
+        data_params.fval2 = data_params.forward[id(data_params.fval2)]
 
     # If fval1 or fval2 is a bound variable, then
     # replace it by the variable's bound value.  This
     # includes aliased variables, which are encoded as
     # variables bound to other variables.
     fvar1 = fvar2 = None
-    while isinstance(fval1, Variable) and fval1 in bindings:
-        fvar1 = fval1
-        fval1 = bindings[fval1]
-    while isinstance(fval2, Variable) and fval2 in bindings:
-        fvar2 = fval2
-        fval2 = bindings[fval2]
+    while isinstance(data_params.fval1, Variable) and data_params.fval1 in data_params.bindings:
+        fvar1 = data_params.fval1
+        data_params.fval1 = data_params.bindings[data_params.fval1]
+    while isinstance(data_params.fval2, Variable) and data_params.fval2 in data_params.bindings:
+        fvar2 = data_params.fval2
+        data_params.fval2 = data_params.bindings[data_params.fval2]
 
     # Case 1: Two feature structures (recursive case)
-    if isinstance(fval1, fs_class) and isinstance(fval2, fs_class):
+    if isinstance(data_params.fval1, data_params.fs_class) and isinstance(data_params.fval2, data_params.fs_class):
         result = _destructively_unify(
-            fval1, fval2, bindings, forward, trace, fail, fs_class, fpath
+            data_params.fval1, data_params.fval2, data_params.bindings, data_params.forward, data_params.trace, data_params.fail, data_params.fs_class, config_params.fpath
         )
 
     # Case 2: Two unbound variables (create alias)
-    elif isinstance(fval1, Variable) and isinstance(fval2, Variable):
-        if fval1 != fval2:
-            bindings[fval2] = fval1
-        result = fval1
+    elif isinstance(data_params.fval1, Variable) and isinstance(data_params.fval2, Variable):
+        if data_params.fval1 != data_params.fval2:
+            data_params.bindings[data_params.fval2] = data_params.fval1
+        result = data_params.fval1
 
     # Case 3: An unbound variable and a value (bind)
-    elif isinstance(fval1, Variable):
-        bindings[fval1] = fval2
-        result = fval1
-    elif isinstance(fval2, Variable):
-        bindings[fval2] = fval1
-        result = fval2
+    elif isinstance(data_params.fval1, Variable):
+        data_params.bindings[data_params.fval1] = data_params.fval2
+        result = data_params.fval1
+    elif isinstance(data_params.fval2, Variable):
+        data_params.bindings[data_params.fval2] = data_params.fval1
+        result = data_params.fval2
 
     # Case 4: A feature structure & a base value (fail)
-    elif isinstance(fval1, fs_class) or isinstance(fval2, fs_class):
+    elif isinstance(data_params.fval1, data_params.fs_class) or isinstance(data_params.fval2, data_params.fs_class):
         result = UnificationFailure
 
     # Case 5: Two base values
     else:
         # Case 5a: Feature defines a custom unification method for base values
-        if isinstance(fname, Feature):
-            result = fname.unify_base_values(fval1, fval2, bindings)
+        if isinstance(data_params.fname, Feature):
+            result = data_params.fname.unify_base_values(data_params.fval1, data_params.fval2, data_params.bindings)
         # Case 5b: Feature value defines custom unification method
-        elif isinstance(fval1, CustomFeatureValue):
-            result = fval1.unify(fval2)
+        elif isinstance(data_params.fval1, CustomFeatureValue):
+            result = data_params.fval1.unify(data_params.fval2)
             # Sanity check: unify value should be symmetric
-            if isinstance(fval2, CustomFeatureValue) and result != fval2.unify(fval1):
+            if isinstance(data_params.fval2, CustomFeatureValue) and result != data_params.fval2.unify(data_params.fval1):
                 raise AssertionError(
                     "CustomFeatureValue objects %r and %r disagree "
                     "about unification value: %r vs. %r"
-                    % (fval1, fval2, result, fval2.unify(fval1))
+                    % (data_params.fval1, data_params.fval2, result, data_params.fval2.unify(data_params.fval1))
                 )
-        elif isinstance(fval2, CustomFeatureValue):
-            result = fval2.unify(fval1)
+        elif isinstance(data_params.fval2, CustomFeatureValue):
+            result = data_params.fval2.unify(data_params.fval1)
         # Case 5c: Simple values -- check if they're equal.
         else:
-            if fval1 == fval2:
-                result = fval1
+            if data_params.fval1 == data_params.fval2:
+                result = data_params.fval1
             else:
                 result = UnificationFailure
 
@@ -1668,30 +1664,30 @@ def _unify_feature_values(
         # Feature or if either value is a CustomFeatureValue.)
         if result is not UnificationFailure:
             if fvar1 is not None:
-                bindings[fvar1] = result
+                data_params.bindings[fvar1] = result
                 result = fvar1
             if fvar2 is not None and fvar2 != fvar1:
-                bindings[fvar2] = result
+                data_params.bindings[fvar2] = result
                 result = fvar2
 
     # If we unification failed, call the failure function; it
     # might decide to continue anyway.
     if result is UnificationFailure:
-        if fail is not None:
-            result = fail(fval1, fval2, fpath)
-        if trace:
-            _trace_unify_fail(fpath[:-1], result)
+        if data_params.fail is not None:
+            result = data_params.fail(data_params.fval1, data_params.fval2, config_params.fpath)
+        if data_params.trace:
+            _trace_unify_fail(config_params.fpath[:-1], result)
         if result is UnificationFailure:
             raise _UnificationFailureError
 
     # Normalize the result.
-    if isinstance(result, fs_class):
-        result = _apply_forwards(result, forward, fs_class, set())
+    if isinstance(result, data_params.fs_class):
+        result = _apply_forwards(result, data_params.forward, data_params.fs_class, set())
 
-    if trace:
-        _trace_unify_succeed(fpath, result)
-    if trace and isinstance(result, fs_class):
-        _trace_bindings(fpath, bindings)
+    if data_params.trace:
+        _trace_unify_succeed(config_params.fpath, result)
+    if data_params.trace and isinstance(result, data_params.fs_class):
+        _trace_bindings(config_params.fpath, data_params.bindings)
 
     return result
 
