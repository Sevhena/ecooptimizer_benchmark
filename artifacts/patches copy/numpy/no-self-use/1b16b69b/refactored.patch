diff --git a/numpy/distutils/command/build_src.py b/numpy/distutils/command/build_src.py
index cfcc80caec..c1b9547acf 100644
--- a/numpy/distutils/command/build_src.py
+++ b/numpy/distutils/command/build_src.py
@@ -21,6 +21,7 @@
     )
 from numpy.distutils.from_template import process_file as process_f_file
 from numpy.distutils.conv_template import process_file as process_c_file
+from codecarbon import track_emissions
 
 def subst_vars(target, source, d):
     """Substitute any occurrence of @foo@ by d['foo'] from source file into
@@ -388,12 +389,14 @@ def generate_sources(self, sources, extension):
         return new_sources
 
     def filter_py_files(self, sources):
-        return self.filter_files(sources, ['.py'])
+        return build_src.filter_files(sources, ['.py'])
 
     def filter_h_files(self, sources):
-        return self.filter_files(sources, ['.h', '.hpp', '.inc'])
+        return build_src.filter_files(sources, ['.h', '.hpp', '.inc'])
 
-    def filter_files(self, sources, exts = []):
+    @track_emissions(project_name='numpy-benchmark', experiment_id='numpy_R6301_1b16b69b', output_file='/root/ecooptimizer_benchmark/emissions/numpy/no-self-use/1b16b69b_refactored.csv')
+    @staticmethod
+    def filter_files(sources, exts = []):
         new_sources = []
         files = []
         for source in sources:
