diff --git a/numpy/_core/einsumfunc.py b/numpy/_core/einsumfunc.py
index 8e71e6d4b1..af5d56f21e 100644
--- a/numpy/_core/einsumfunc.py
+++ b/numpy/_core/einsumfunc.py
@@ -1,3 +1,4 @@
+from codecarbon import EmissionsTracker
 """
 Implementation of optimized einsum.
 
@@ -689,11 +690,12 @@ def _parse_einsum_input(operands):
             # Special care for outputless ellipses
             output_subscript = ""
             tmp_subscripts = subscripts.replace(",", "")
-            for s in sorted(set(tmp_subscripts)):
-                if s not in (einsum_symbols):
-                    raise ValueError(f"Character {s} is not a valid symbol.")
-                if tmp_subscripts.count(s) == 1:
-                    output_subscript += s
+            with EmissionsTracker(project_name='numpy-benchmark', experiment_id='numpy_SCL001_2a61e075', output_file='/root/ecooptimizer_benchmark/emissions/numpy/string-concat-loop/2a61e075.csv') as tracker:
+                for s in sorted(set(tmp_subscripts)):
+                    if s not in (einsum_symbols):
+                        raise ValueError(f"Character {s} is not a valid symbol.")
+                    if tmp_subscripts.count(s) == 1:
+                        output_subscript += s
             normal_inds = ''.join(sorted(set(output_subscript) -
                                          set(out_ellipse)))
 
