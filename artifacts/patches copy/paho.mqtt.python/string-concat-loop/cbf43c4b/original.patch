diff --git a/src/paho/mqtt/properties.py b/src/paho/mqtt/properties.py
index f307b86..529bacb 100644
--- a/src/paho/mqtt/properties.py
+++ b/src/paho/mqtt/properties.py
@@ -17,6 +17,7 @@
 import struct
 
 from .packettypes import PacketTypes
+from codecarbon import EmissionsTracker
 
 
 class MQTTException(Exception):
@@ -292,13 +293,14 @@ class Properties:
     def __str__(self):
         buffer = "["
         first = True
-        for name in self.names.keys():
-            compressedName = name.replace(' ', '')
-            if hasattr(self, compressedName):
-                if not first:
-                    buffer += ", "
-                buffer += f"{compressedName} : {getattr(self, compressedName)}"
-                first = False
+        with EmissionsTracker(project_name='paho.mqtt.python-benchmark', experiment_id='paho.mqtt.python_SCL001_cbf43c4b', output_file='/root/ecooptimizer_benchmark/emissions/paho.mqtt.python/string-concat-loop/cbf43c4b.csv') as tracker:
+            for name in self.names.keys():
+                compressedName = name.replace(' ', '')
+                if hasattr(self, compressedName):
+                    if not first:
+                        buffer += ", "
+                    buffer += f"{compressedName} : {getattr(self, compressedName)}"
+                    first = False
         buffer += "]"
         return buffer
 
