diff --git a/src/paho/mqtt/client.py b/src/paho/mqtt/client.py
index 4ccc869..3488c1a 100644
--- a/src/paho/mqtt/client.py
+++ b/src/paho/mqtt/client.py
@@ -1,3 +1,4 @@
+from codecarbon import track_emissions
 # Copyright (c) 2012-2019 Roger Light and others
 #
 # All rights reserved. This program and the accompanying materials
@@ -3342,8 +3343,10 @@ class Client:
         self._easy_log(MQTT_LOG_DEBUG, "Sending PUBCOMP (Mid: %d)", mid)
         return self._send_command_with_mid(PUBCOMP, mid, False)
 
+    @track_emissions(project_name='paho.mqtt.python-benchmark', experiment_id='paho.mqtt.python_R6301_26129bdd', output_file='/root/ecooptimizer_benchmark/emissions/paho.mqtt.python/no-self-use/26129bdd_refactored.csv')
+    @staticmethod
     def _pack_remaining_length(
-        self, packet: bytearray, remaining_length: int
+        packet: bytearray, remaining_length: int
     ) -> bytearray:
         remaining_bytes = []
         while True:
@@ -3429,7 +3432,7 @@ class Client:
                 packed_properties = properties.pack()
             remaining_length += len(packed_properties)
 
-        self._pack_remaining_length(packet, remaining_length)
+        Client._pack_remaining_length(packet, remaining_length)
         self._pack_str16(packet, topic)
 
         if qos > 0:
@@ -3518,7 +3521,7 @@ class Client:
         if self._client_mode == MQTT_BRIDGE:
             proto_ver |= 0x80
 
-        self._pack_remaining_length(packet, remaining_length)
+        Client._pack_remaining_length(packet, remaining_length)
         packet.extend(struct.pack(
             f"!H{len(protocol)}sBBH",
             len(protocol), protocol, proto_ver, connect_flags, keepalive,
@@ -3599,7 +3602,7 @@ class Client:
                     packed_props = properties.pack()
                     remaining_length += len(packed_props)
 
-        self._pack_remaining_length(packet, remaining_length)
+        Client._pack_remaining_length(packet, remaining_length)
 
         if self._protocol == MQTTv5:
             if reasoncode is not None:
@@ -3628,7 +3631,7 @@ class Client:
         command = SUBSCRIBE | (dup << 3) | 0x2
         packet = bytearray()
         packet.append(command)
-        self._pack_remaining_length(packet, remaining_length)
+        Client._pack_remaining_length(packet, remaining_length)
         local_mid = self._mid_generate()
         packet.extend(struct.pack("!H", local_mid))
 
@@ -3670,7 +3673,7 @@ class Client:
         command = UNSUBSCRIBE | (dup << 3) | 0x2
         packet = bytearray()
         packet.append(command)
-        self._pack_remaining_length(packet, remaining_length)
+        Client._pack_remaining_length(packet, remaining_length)
         local_mid = self._mid_generate()
         packet.extend(struct.pack("!H", local_mid))
 
