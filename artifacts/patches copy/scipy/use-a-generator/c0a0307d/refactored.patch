diff --git a/scipy/special/_mptestutils.py b/scipy/special/_mptestutils.py
index 9e519093d..829e893e7 100644
--- a/scipy/special/_mptestutils.py
+++ b/scipy/special/_mptestutils.py
@@ -8,6 +8,7 @@ from numpy.testing import assert_
 import pytest
 
 from scipy.special._testutils import assert_func_equal
+from codecarbon import EmissionsTracker
 
 try:
     import mpmath
@@ -209,9 +210,10 @@ class MpmathData:
         if isinstance(self.arg_spec, np.ndarray):
             self.is_complex = np.issubdtype(self.arg_spec.dtype, np.complexfloating)
         else:
-            self.is_complex = any(
-                [isinstance(arg, ComplexArg) for arg in self.arg_spec]
-            )
+            with EmissionsTracker(project_name='scipy-benchmark', experiment_id='scipy_R1729_c0a0307d', output_file='/root/ecooptimizer_benchmark/emissions/scipy/use-a-generator/c0a0307d_refactored.csv') as tracker:
+                self.is_complex = any(
+                    isinstance(arg, ComplexArg) for arg in self.arg_spec
+                )
         self.ignore_inf_sign = ignore_inf_sign
         self.distinguish_nan_and_inf = distinguish_nan_and_inf
         if not name or name == '<lambda>':
