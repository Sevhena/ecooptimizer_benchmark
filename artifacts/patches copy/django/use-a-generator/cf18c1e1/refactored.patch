diff --git a/django/forms/formsets.py b/django/forms/formsets.py
index c2663154d4..23477be0c4 100644
--- a/django/forms/formsets.py
+++ b/django/forms/formsets.py
@@ -7,6 +7,7 @@ from django.forms.widgets import CheckboxInput, HiddenInput, NumberInput
 from django.utils.functional import cached_property
 from django.utils.translation import gettext_lazy as _
 from django.utils.translation import ngettext_lazy
+from codecarbon import EmissionsTracker
 
 __all__ = ("BaseFormSet", "formset_factory", "all_valid")
 
@@ -384,13 +385,12 @@ class BaseFormSet(RenderableFormMixin):
         self.errors
         # List comprehension ensures is_valid() is called for all forms.
         # Forms due to be deleted shouldn't cause the formset to be invalid.
-        forms_valid = all(
-            [
+        with EmissionsTracker(project_name='django-benchmark', experiment_id='django_R1729_cf18c1e1', output_file='/root/ecooptimizer_benchmark/emissions/django/use-a-generator/cf18c1e1_refactored.csv') as tracker:
+            forms_valid = all(
                 form.is_valid()
-                for form in self.forms
-                if not (self.can_delete and self._should_delete_form(form))
-            ]
-        )
+                    for form in self.forms
+                    if not (self.can_delete and self._should_delete_form(form))
+            )
         return forms_valid and not self.non_form_errors()
 
     def full_clean(self):
